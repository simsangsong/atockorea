# PERF-LEDGER — 스마트앱 성능 감사 원장 (2026-08)

> 플랜: `docs/smartapp-performance-audit-plan-2026-08-03.md`
> **감사와 수정은 분리한다.** 여기엔 발견만 적고, 수정은 P6 에서 한 건 = 한 PR 로 한다.
> 모든 숫자에 **[실측] / [계산] / [추정]** 을 표시한다. 추정은 원칙적으로 쓰지 않는다.

## 측정 조건 (이걸 안 적으면 숫자가 쓸모없어진다)

| 항목 | 값 |
|---|---|
| 코드 | `origin/main` @ `a8dc24a8` · 브랜치 `claude/smartapp-perf-audit-2026-08-03` |
| 서버 | `npm run build`(exit 0) + `npx next start -p 3182` — **프로덕션 빌드** |
| 클라이언트 | Playwright Chromium · 390×844 모바일 · **콜드 캐시(라우트마다 새 컨텍스트)** |
| 4G 프로파일 | 4 Mbps↓ · 3 Mbps↑ · RTT 70ms · **CPU 4x 스로틀** |
| slow4g 프로파일 | 1.6 Mbps↓ · 0.75 Mbps↑ · RTT 150ms · CPU 4x |
| 데이터 | `sim-tour-day` 2방 + `sim-lobby-booking` 1방 (실데이터, `sim_tag` 격리) |
| 통계 | 3회 중앙값 |
| 🔴 한계 | **실기기 아님.** 스로틀은 재현 가능한 대리값이다. 실기기 측정은 사람 게이트로 남는다 |

---

## 예산 초과 항목 한눈에 (표면 · 상호작용 · 빈도)

| 항목 | 표면 | 상호작용 | 초과 | 빈도 |
|---|---|---|---|---|
| **P-01** 룸 진입 | 손님 룸 | 링크 열기 → 사용 가능 | 4G **+16~27%** · slow4g **2.2배** | 룸 진입마다 |
| **P-07** 컨시어지 Tier1 | 손님 룸(컨시어지) | 질문 전송 → 답변 | **2~3배** (9~13초, 게다가 실패) | 질문마다 |
| **P-08** 서랍 첫 열기 | 손님 룸(홈 탭) | `room-drawer-open` 탭 | **2.2~2.3배** (≈340ms vs 150ms) | 🔴 **룸 세션당 1회**(실측, 아래) |

*스태프·관제 표면에는 §F 숫자 예산이 없어 베이스라인만 만든다(가이드 콘솔 · 콕핏 · `/tour-ops`).*

---

## P-01 🔴 룸 진입이 4G 성능 예산을 넘는다 — §F #4 최초 실측, 미달

**증상.** 손님이 초대 링크를 열고 실제로 쓸 수 있게 되기까지 **4G에서 2.9~3.2초**.
§F 예산은 **< 2.5s (4G)** 다. 이 지표는 그동안 **한 번도 측정된 적이 없었다**
(`A0-perf-baseline.md` 가 "미측정 6/10" 로 남겨둔 항목).

| 라우트 | 4G 첫 유의미 | 예산 | 판정 | slow4g 첫 유의미 |
|---|---|---|---|---|
| 룸(진행 중 투어) | **2,909 ms** | 2,500 ms | ❌ **+409 ms (16% 초과)** | **5,553 ms** |
| 룸(로비, D-1) | **3,164 ms** | 2,500 ms | ❌ **+664 ms (27% 초과)** | **5,454 ms** |
| 가이드 콘솔 | 2,454 ms | (예산 없음) | — | 4,929 ms |

[실측] 3회 중앙값 · `scripts/qa-perf-throttled.mjs`

🔴 **연결이 나빠지면 선형이 아니라 2배로 나빠진다** — 4G 2.9초 → slow4g **5.6초**(1.9배).
JS 페이로드가 지배적일 때 나타나는 모양이다(대역폭이 절반이면 다운로드가 두 배).
제주 시골 구간·버스 이동 중이 정확히 slow4g 쪽이고, 예산의 근거가
*"버스 하차 직전 30초 안에 정보 확인"* 이므로 **실사용에서 더 나쁜 쪽이 기본값에 가깝다.**

**분해 — 병목은 서버가 아니라 전부 클라이언트다.**

| 구간 | 룸(live) | 읽는 법 |
|---|---|---|
| TTFB | **7 ms** | 서버는 즉답한다. 여기엔 문제가 없다 |
| FCP | 2,832 ms | 첫 픽셀까지 2.8초 — **이 구간이 사실상 전부** |
| 첫 유의미 | 2,909 ms | FCP 직후 바로 사용 가능해진다 |
| LCP | 3,548 ms | 최대 요소는 더 늦다 |

**TTFB 7ms → FCP 2,832ms.** 그 사이에 서버 작업은 없다. 오직 **612 KB JS 의 다운로드·파싱·실행**과
그 뒤의 `join` 왕복뿐이다 [실측 + 코드 실측].

**원인 (코드 실측).** `app/(app)/tour-mode/room/[bookingId]/page.tsx` 는 `force-dynamic` 인데
**서버에서 데이터를 하나도 읽지 않고** `TourRoomClient` 만 렌더한다. 즉 손님은
**HTML → JS 다운로드 → 파싱/실행 → join API → snapshot → 렌더** 를 **직렬로** 기다린다.
서버가 7ms 에 답할 수 있다는 것은, 그 시간에 스냅샷을 같이 실어 보낼 여지가 있다는 뜻이다.

**영향.** 손님 전원. 그리고 §F 예산의 근거가 *"버스 하차 직전 30초 안에 정보 확인"* 이므로
이 초과는 실사용 시나리오를 직접 침해한다.

**게이트 가능:** ✅ `qa-perf-throttled.mjs --check` 로 예산 게이트화 가능(현재 `--check` 미구현, P6).

---

## P-00 하니스 자체를 뮤테이션으로 검증했다 (이 원장의 숫자를 믿을 근거)

이 트랙의 숫자는 전부 하니스가 낸 것이므로, **하니스가 거짓 초록을 낼 수 있는지**를 먼저 시험했다.
방법: 존재하지 않는 예약 id 로 픽스처를 위조하고 같은 하니스를 돌린다.

| 하니스 | 죽은 데이터 | 정상 데이터 |
|---|---|---|
| `qa-perf-throttled` | **exit 1** — 룸·가이드는 셀렉터 대기 실패, **플랜은 신규 내용 단언이 발동**(`도달 실패 … chars 47 · nodes 50 · "찾을 수 없음" 문구`) | exit 0 |
| `qa-perf-idle` | **exit 1** | exit 0 |

🔴 **플랜 라우트는 이 가드가 없었으면 깨끗한 숫자를 냈다.** 대기 셀렉터가 없는 라우트라
오류 화면을 그대로 쟀을 것이고, **오류 화면은 빨리 뜨므로 회귀가 개선으로 보였을 것이다.**

⚠ 반대 방향도 확인했다 — 정상 데이터에서 오탐 0. 한쪽만 보면 "엄격해서 통과 못 함"과
"제대로 잡음"을 구분할 수 없다.

**왜 이걸 원장 맨 앞에 두는가:** 같은 병이 오늘 다섯 번 나왔다(두 트랙 합산) —
유휴 요청 0을 초록으로 · 403 을 "40ms 빠름"으로 · 예약 없는 DB 를 "위반 0"으로 ·
가장 잘 아는 결함을 깨끗함으로 · `fixed` 를 찾다 0px 를 합격으로.
전부 **"측정 실패가 합격처럼 생겼다"** 이다.

---

## P-02 룸 JS 612 KB — 예산은 지키지만 P-01 의 직접 원인

| 값 | 출처 |
|---|---|
| 룸 first-load JS **205 KB (gzip)** | `A0-perf-baseline.md` [실측, 2026-07-25] |
| 룸 라우트 JS 전송 **612.2 KB** | 이번 실측 (무스로틀 완주, `qa-bundle-baseline`) |
| §F 예산 | < 350 KB gzip |

두 숫자는 **모순이 아니다** — 205 KB 는 gzip first-load, 612 KB 는 소켓이 실어 온 전체 JS(26 요청).
**예산 자체는 통과**이나, P-01 의 2.8초가 이 페이로드의 다운로드·파싱·실행 시간이다.
⚠ 이전 베이스라인 606 KB → **612.2 KB** 로 6.2 KB 늘었다(main 이동분). 게이트가 없어 조용히 늘었다.

**게이트 가능:** ✅ 라우트별 JS KB 상한.

⚠ **그 게이트가 왜 필요한지 이 세션 안에서 증거가 하나 더 나왔다.** 같은 날 같은 브랜치에서
룸 JS 가 **612.2 → 622.3 KB (+10.1)** 로 움직였다 [실측, n=1]. 그 사이 내가 만진 것은
서버 코드(`lib/ai/router.ts`)·테스트·스크립트뿐이라 **원인을 모른다.**
숫자를 세 번 재는 동안 두 번 달랐다는 사실 자체가 요점이다 —
**상한이 없으면 이런 이동은 아무에게도 안 보인다.**

---

## P-03 가이드 콘솔 — FCP 676ms 인데 사용 가능까지 2,454ms

[실측, 4G] 첫 픽셀은 0.7초에 뜨는데 `staff-shell` 이 준비되기까지 **1.8초가 더** 걸린다.
손님 룸(FCP 2,832 → 유의미 2,909, 격차 77ms)과 **모양이 완전히 다르다** —
룸은 "늦게 뜨고 바로 쓸 수 있음", 가이드는 "빨리 뜨고 오래 못 씀".
후자가 사용자에게 더 나쁘다(뜬 화면을 눌렀는데 반응이 없다).

**원인:** 미판별. P5 에서 렌더 프로파일 필요.

---

## P-04 관제 콘솔 TBT≈ 848ms · 요청 105건

🔴 **UI/UX 트랙과 겹쳐 읽으면 심각도가 올라간다(상호 참조).**
그 트랙의 렌더 감사가 `/admin/tour-ops` 에서 **가시 컨트롤 18개 중 accent 배경 0개** —
즉 **primary 가 없어 아무것도 이기지 않는** 상태를 찾았다.
여기 수치와 합치면 관제는 **"눌러야 할 게 뭔지 안 보이는데, 눌러도 0.85초 동안 안 먹는" 화면**이다.
각각은 중간 심각도지만 **겹치면 다르다** — 위계 부재는 사용자를 헤매게 하고, 그 헤맴의 각 시도가
차단된 메인 스레드를 만난다. 수정은 두 트랙이 같이 잡아야 한다(그쪽 U9 / 이쪽 P6).

[실측, 4G] 앱에서 가장 무거운 표면. long task 6건, 총 차단 근사 **848 ms**.
무스로틀 전송량도 **JS 768 KB / 총 3,179 KB / 87 요청** 으로 최대 [실측].
⚠ 다만 관제는 **손님이 아니라 사무실 데스크톱**에서 쓴다 — 우선순위는 손님 < 스태프 < 관제 순.

---

## P-05 중복 API 3건 (2건은 기존 발견 재현, 1건은 신규)

| 호출 | 라우트 | 상태 |
|---|---|---|
| `…/join ×2` | 손님 룸(live·lobby 둘 다) | **기존 발견 재현.** 설계된 동작(TTS 능력 보고)이나 effect deps 에 `locale` 이 있어 **로케일 변경마다 1회 추가** — 앞 감사가 P3 로 남김 |
| `/api/currency/rate ×2` | 관제 | **기존 발견 재현.** 미판별 |
| `/api/analytics/experiments/active ×2` | 관제 | 🆕 **이번에 처음 관측**(1회차 런에서만 나타남 — 재현성 확인 필요) |

---

## P-06 🔴 성능 아님 — 차량이 배정된 예약은 **삭제가 구조적으로 불가능하다**

성능 감사 중 시뮬 드레인이 실패하면서 드러났다. 성능 문제가 아니라 **데이터 무결성 결함**이라
여기 적어 두고, 수정은 이 트랙 밖이다.

**스키마 (라이브 실측, `pg_constraint`):**
```
ops_room_vehicles_anchor_present   CHECK (group_id IS NOT NULL OR room_id IS NOT NULL)
ops_room_vehicles_room_id_fkey     FOREIGN KEY (room_id)  → tour_rooms(id)      ON DELETE SET NULL
ops_room_vehicles_group_id_fkey    FOREIGN KEY (group_id) → ops_tour_groups(id) ON DELETE CASCADE
tour_rooms_booking_id_fkey         FOREIGN KEY (booking_id) → bookings(id)      ON DELETE CASCADE
```

**체인:** `bookings` 삭제 → CASCADE → `tour_rooms` 삭제 → `ops_room_vehicles.room_id` **SET NULL** →
그 행은 `group_id` 도 NULL 이라 **CHECK 위반** → 트랜잭션 롤백 → **예약 삭제 전체가 실패한다.**

**즉 룸에 차량이 배정된 예약은 지울 수 없다.** 방-앵커 차량(`group_id` NULL, `room_id` 있음)은
전세/프라이빗 투어의 정상 형태다.

**실제 발생:** 오늘 `sim-tour-day.ts --cleanup` 이 이 제약으로 **exit 1** 로 죽었다 [실측].
에러 원문: `new row for relation "ops_room_vehicles" violates check constraint
"ops_room_vehicles_anchor_present"`. 문서화된 드레인 경로가 막힌 것이므로
**이 함정은 시뮬을 쓰는 다음 세션마다 재발한다.**

**영향:** 예약 취소·삭제 경로 · 개인정보 삭제 요청(GDPR 식) · 시뮬 드레인.
현재 `ops_room_vehicles` 총 2행뿐이라 운영 폭발이 안 났을 뿐이다 [실측].

**우회(오늘 쓴 것):** 해당 `ops_room_vehicles` 행을 먼저 지우고 드레인 재실행 → 성공
(15건 제거 · 잔여 0 · 고아 0). ⚠ 사장님의 `manual-test-2026-07` 11건은 건드리지 않았다(확인함).

### ✅ 수정 완료 (2026-08-03, 사장님 승인 후 라이브 DDL)

🔴 **처음 제안한 "`room_id` 를 ON DELETE CASCADE 로" 는 틀렸다.**
라이브에 **`group_id` 와 `room_id` 를 둘 다 가진 행이 실재한다** [실측]. 그 행은 룸이 사라져도
그룹 앵커로 유효하고 지금은 정상 생존한다(room_id 만 NULL). 통짜 CASCADE 는 **그것까지 지워
현재 동작을 퇴행**시킨다. FK 액션으로는 "앵커가 이것 하나뿐일 때만"을 표현할 수 없다.

**적용한 것** (`supabase/migrations/20260803120000_room_vehicles_deletable.sql`):
`tour_rooms` 에 **BEFORE DELETE 트리거** — 그 룸에만 매인(`group_id IS NULL`) 배정만 함께 지운다.
이중 앵커 행은 기존 FK `SET NULL` 경로로 그대로 생존한다. CHECK 는 손대지 않고 위반 상황만 없앤다.

**검증** [실측]:
| 확인 | 결과 |
|---|---|
| 실패했던 경로 재실행(`sim-tour-day` 시드 → `--cleanup`) | **exit 0** (이전 exit 1) |
| 이중 앵커 행 생존 | ✅ 사장님 `manual-test-2026-07` 행 무사(room·group 둘 다 유지) |
| 어드바이저 재실행 | 🔴 **내 변경이 새 지적을 만들었다** — `anon_security_definer_function_executable`. 트리거 함수가 PostgREST 로 노출됨 → `revoke execute` from public·anon·authenticated 적용 → **재실행 시 0건** |
| 회수 후에도 트리거 동작 | ✅ 시드→드레인 다시 exit 0 |

**게이트:** ⚠ **구조 검사는 못 붙였다.** `qa-ops-center-queries.ts` 에서 `pg_trigger` 를 읽으려면
raw SQL 이 필요한데 이 레포엔 범용 SQL RPC 가 없다(DDL 은 전부 MCP 수동 경로).
**행동 게이트는 시드→드레인 사이클**이다 — 회귀하면 `sim-tour-day.ts --cleanup` 이 **exit 1** 로 죽는다.
시뮬을 쓰는 세션마다 밟는 경로라 실효는 있으나 **CI 자동 게이트는 아니다.**
🔴 마이그레이션 파일 존재로 검증하지 말 것 — **레포에 있다 ≠ 적용됐다.**

---

## P-07 🔴 컨시어지 Tier1 — 예산 4초짜리 기능이 공급자 하나당 8초를 기다리게 돼 있다

**구조 (코드 실측, 환경 무관):**
`lib/ai/router.ts` `timeoutMsFor()` 는 `caption` 3,000ms · `vision` 20,000ms 를 **명시적으로 정해 두고**,
나머지는 기본값 **8,000ms** 로 떨어진다. `concierge` 는 그 기본값을 물려받는다.
공급자 체인은 `concierge: ['gemini', 'openai']` — **둘**이다.

> §F 예산은 **Tier1 < 4s**. 즉 **첫 공급자만 지연돼도 예산의 2배를 기다리고**,
> 폴백까지 가면 최대 16초다. vision 은 20초로 올리며 근거를 주석에 길게 적었는데,
> **컨시어지는 예산이 있는데도 아무도 이 둘을 맞춰보지 않았다** — §B-6 "연결 문제"의 전형.

**실측 (LLM 실호출 3회, 사장님 승인):**
| # | 응답시간 | 결과 |
|---|---|---|
| 1 | **13,078 ms** | HTTP 500 |
| 2 | 8,952 ms | HTTP 500 |
| 3 | 9,077 ms | HTTP 500 |

**3회 전부 답변 없음.** 서버 로그 원문:
`All AI providers failed for concierge — gemini/gemini-2.5-flash-lite: http_403 | openai/gpt-5-mini: timeout_8000ms`
(2회차는 `empty_completion`). 그리고 **매 호출마다**
`[ai-router] openai/gpt-5-mini rejected "temperature" — retrying without it` — 왕복이 한 번씩 더 든다.

### 🔴 후속 조사 결과 — 원인은 예산도 타임아웃도 아니었다. 둘 다 딴 것이었다.

**원인 1 — Gemini 는 키 문제가 아니라 결제 정지다.** 직접 호출해 본문을 읽었다:

```
HTTP 403  PERMISSION_DENIED
"Lightning dunning decision is deny for project: projects/389319064084"
```

`dunning` = **미납 추심**. 키가 잘못된 게 아니라 **Google Cloud 프로젝트가 결제 문제로 차단**됐다.
즉 **로컬 환경 문제가 아니고 프로덕션에서도 같은 키가 막힌다** [실측].
⚠ 영향 범위는 컨시어지가 아니다 — `GEMINI_API_KEY` 는 `translate` · `caption` · `vision` ·
`concierge` **전부의 1차 공급자**이고 `batch` 의 2차다(`DEFAULT_LADDERS`). 전부 폴백으로 떨어진다.
✅ `GOOGLE_MAPS_API_KEY` 는 **다른 키/프로젝트**이고 정상이다(Geocoding `status: OK` 실측) — 지도는 무관.
🔴 **사장님 조치 필요: Google Cloud 프로젝트 `389319064084` 결제.**

**원인 2 — OpenAI 폴백은 상한 400 때문에 빈 답을 낸다(코드 버그).**
타임아웃을 60초로 올리자 `timeout_8000ms` 가 **`empty_completion`** 으로 바뀌었다. 직접 재현:

| 설정 (gpt-5-mini, 같은 질문 3개) | 결과 |
|---|---|
| **cap 400 (현행)** | **3/3 빈 응답** — `finish_reason: length`, `reasoning_tokens 400/400`, 4.4~6.2초 |
| cap 400 + `reasoning_effort: low` | 1/3 성공 |
| cap 400 + `reasoning_effort: minimal` | 2/3 성공 |
| **cap 1200 + low** | **3/3 성공** (reasoning 0·192·128), 1.9~7.3초 |

**gpt-5 계열에서 `max_completion_tokens` 는 추론 토큰을 포함한다.** `DEFAULT_MAX_OUTPUT_TOKENS.concierge`
= 400 은 §L-D5 에 따라 **"보이는 답의 길이"** 로 정해진 값인데, 그걸 그대로 상한으로 주니 **추론이 예산을
전부 먹고 본문이 0자**로 돌아왔다. 라우터는 그걸 `empty_completion` 으로 읽어 공급자를 강등시키므로,
겉보기 증상은 "모델이 응답을 거부함"이 된다.
⚠ **한 문항만 테스트했으면 `low` 로 고쳐진 줄 알았을 것이다**(1/3 성공).

### ✅ 수정 완료 (코드)

`lib/ai/router.ts` — 목적별 상한의 의미(보이는 답의 길이)는 그대로 두고, **openai 에만**
추론 여유분 `+800` 과 `reasoning_effort: 'low'` 를 더한다. 다른 공급자는 `max_tokens` 를 출력에만
쓰므로 무영향. 미지원 모델이면 기존 `unsupported_parameter` 재시도가 필드를 떨군다.

**검증 [실측, 앱 통과, 기본 8초 타임아웃]:** HTTP **500 → 201**, `답변있음: true` ×3.
지연 **4,619 / 5,641 / 5,641 ms**. (4회차 429 는 앱 자체 일일예산 가드로 정상 동작.)
테스트 99개 통과 · tsc 0. 회귀 테스트는 숫자가 아니라 **관계**를 고정한다 —
"openai 상한 > 보이는 답 상한" · "openai 방언을 남에게 보내지 않는다".

### 🔴 그래서 예산은 지금 정하면 안 된다

사장님 지시는 **"예산 올릴거"** 였고, 그 판단은 *"타임아웃 8초 vs 예산 4초"* 라는 **내 불완전한 진단**
위에서 내려졌다. 실제로는 **예산이 아니라 버그**였고, 고치니 답이 온다.
지금 남은 **4.6~5.6초는 폴백 경로의 지연**이다 — 1차 공급자가 결제로 죽어 있어서 openai 가 대신 답한다.
**결제를 복구하면 Gemini flash-lite 가 1차로 돌아오고 그 지연은 다시 재야 안다.**
지금 수치로 예산을 올리면 **고장난 상태를 기준선으로 박제**하는 셈이다.
→ 권고: **① 결제 복구 → ② 재측정 → ③ 그 값으로 예산 확정.**

---

## P-08 상호작용 예산 — #5 통과, #6 은 **첫 탭만** 초과

[실측] CPU 4x 스로틀 · 네트워크 무스로틀(옵티미스틱 성질 검증 목적) · n=12 · `qa-perf-interactions.mjs`

| §F 지표 | 예산 | 첫 회 | warm p50 | 판정 |
|---|---|---|---|---|
| #5 메시지 전송 → 화면 반영 | 300 ms | 22~33 ms | **16.7~19.9 ms** | ✅ **PASS** (여유 10배 이상) |
| #6 카드 탭 → 시트 열림(서랍) | 150 ms | **334~349 ms** | **17.6~22.6 ms** | ❌ **첫 탭만 FAIL** (2.2~2.3배) |

🔴 **n 이 작을 때 p95 를 그대로 읽으면 오독한다.** #6 의 p95 는 사실상 **첫 회 그 자체**다.
두 번째부터는 ~20ms 로 예산의 1/7 이다. 즉 **"서랍이 상시 느리다"가 아니라 "첫 탭이 느리다"** 이고,
원인은 지연 로드되는 1회성 비용으로 보인다(코드 스플릿·이미지 — 미확정, P5 에서 프로파일 필요).
손님은 그 첫 탭을 **한 번은 반드시** 겪는다.

⚠ **재현성 확인함(중요).** 한 번은 첫 회가 **149.7ms** 로 나와 통과처럼 보였다. 그 값을 믿을 뻔했으나
**콜드 컨텍스트로 6회 반복하니 348.9 · 341.1 · 334.0 · 336.6 · 344.2 · 341.9** 로 촘촘했다 —
**149.7 이 이상치**다. 단발 측정으로 예산 통과를 선언하지 말 것.

#### 어느 표면 · 어느 상호작용 · 몇 번 (UI/UX 트랙 U5 흐름 감사용)

| 항목 | 값 |
|---|---|
| 표면 | **손님 룸** (`/tour-mode/room/[id]`) — 스태프 표면 아님 |
| 상호작용 | 홈 탭의 **서랍 열기**(`room-drawer-open` → `room-drawer`) |
| 조건 | CPU 4x 스로틀 · 네트워크 무스로틀 · 콜드 컨텍스트 |
| **빈도** | 🔴 **룸 세션당 1회** — 아래 실측 |

🔴 **첫 열기 비용은 되돌아오지 않는다 [실측].**
같은 컨텍스트에서 `[콜드 → warm → 탭 왕복(홈→채팅→홈) → 재열기]`:
**342.4 ms → 24.0 ms → 28.5 ms.**
탭을 옮겼다 돌아와도 warm 이 유지된다 ⇒ **스톱마다 겪는 비용이 아니라 룸 진입당 한 번**이다.
따라서 심각도는 "매번 340ms 느림"이 아니라 **"룸에 들어와 서랍을 처음 열 때 한 번 340ms"** 다.
(빈도가 판정에 들어가는 UI/UX 트랙 요청에 대한 답이다.)

### 콕핏 진입 전환 — 신규 베이스라인 (§F 예산 없음)

| 측정 | 값 |
|---|---|
| 운전 모드 → `driver-feed` 렌더 | **751~761 ms** |
| 전환 구간 TBT≈ | **949~954 ms** |
| 전환 구간 CLS | 0.000 |
| LCP · TTFB | **해당 없음** |

🔴 **콕핏은 라우트가 아니라 앱 내부 전환이다.** `staff-shell` 안에서 화면만 바뀌므로 **문서 로드가 없고,
따라서 LCP·TTFB 가 발생하지 않는다.** "콕핏 LCP" 를 적으면 없는 지표를 지어내는 것이다.
⚠ **진입 경로가 방 개수에 따라 갈린다**(`GuideConsole.tsx:588`): 방 1개면 `drive-hero` 가 콕핏을
**바로 열고**, 여러 개면 운행 탭으로 바꾸기만 하고 방마다 붙은 `ops-drive` 가 진짜 진입이다.
`qa-cockpit-walk.mjs` 주석은 후자만 적어 뒀는데 **보편 규칙이 아니다** — 시뮬(가이드 1방)에서
`ops-drive` 를 기다리면 그냥 60초 타임아웃이고, 그건 앱 실패처럼 보인다(초판에서 실제로 겪음).

---

## P-09 라우트 p50/p95 (#10) — 부분 측정에 그침, 원인은 픽스처

[실측] 브라우저 세션 내 fetch · 7회 · 로컬 프로덕션 서버 · 네트워크 스로틀 없음

| 라우트 | p50 | p95 | 응답 KB |
|---|---|---|---|
| `dietary` | 92 | 126 | 0 |
| `vehicle-eta` | 124 | 136 | 0 |
| `messages` | 131 | 140 | 0.3 |
| `extras` | 137 | 141 | 0 |
| `tour-itinerary` | 144 | 150 | **32.4** |
| `my-seat` | 210 | 252 | 0.1 |
| `plan` | 249 | **973** | 1.6 |
| `region-scripts` | 297 | 356 | **30.3** |

**8/15 가 2xx.** 나머지는 `405 GET 미지원` 4건(approach·captions·dining·morning-briefing 은 POST 전용) ·
`403` 2건(arrival-bundle·day-summary — 역할 게이트) · `400` 1건(media, 파라미터 필요).

⚠ **`plan` 은 p50 249ms 인데 p95 973ms** — 4배 편차다. 원인 미판별.
⚠ `tour-itinerary` **32.4KB** · `region-scripts` **30.3KB** — 로컬은 빠르지만 4G 에서는 전송이 붙는다.
⚠ 이 숫자는 **네트워크 스로틀 없는 로컬 서버**라 서버 처리 시간에 가깝다. 손님 체감은 여기에 전송이 더해진다.

🔴 **처음엔 이 라우트들이 404 였다.** 시드 직후 상태에 따라 데이터가 없으면 404 가 나온다 —
같은 하니스가 조건에 따라 "성능 미측정"과 "정상"을 오간다는 뜻이라, **2xx 여부를 표에 같이 적는 이유**다.

🔴 **비-2xx 응답시간을 라우트 성능으로 적지 않는다.** 초판 하니스는 `x-tour-room-auth` 헤더를
몰라서 전 라우트가 403 이었는데 **숫자는 멀쩡히 나왔다**(40~60ms) — 그대로 실었으면
"손님 라우트는 전부 40ms대"라는 **정반대 결론**을 낼 뻔했다. 앱은 쿠키가 아니라 이 헤더로 인증한다.

**막는 것:** 더 두꺼운 픽스처(일정 확정·좌석·지역 스크립트가 들어간 방). `k4-seed.ts` 20방이 후보.

---

## P-10 다이닝 — #8 HIT 통과(계약 포함) · #9 MISS 최초 실측 **9.5초**

[실측] `scripts/qa-perf-dining.mjs` · 브라우저 세션 내 POST · 로컬 프로덕션 서버 · 네트워크 스로틀 없음

| §F 지표 | 예산 | 실측 | 판정 |
|---|---|---|---|
| **#8 HIT** | < 600ms · **외부 API 0회** | p50 **454ms** · p95 **475ms** (n=3) | ✅ **PASS** |
| **#9 MISS** | **미확정**(측정 후 쓰기로 되어 있었다) | **9,490 ms** (n=1) | 아래 |

### #8 의 진짜 계약("외부 API 0회")도 검증했다 — 시간이 아니라 **카운터**로

`ops_kakao_cell_index` 가 셀마다 `kakao_calls` · `google_calls` 를 들고 있어서 직접 대조했다.
HIT 호출 14회(429 포함) 전후:

| | 전 | 후 |
|---|---|---|
| kakao_calls 합 | 22 | **22** |
| google_calls 합 | 9 | **9** |
| 셀 수 | 9 | **9** |
| `ops_kakao_place_cache` | 324 | **324** |
| `ops_ai_usage` | 1,980 | **1,980** |

**한 칸도 안 움직였다.** §5.7 핵심 계약은 지켜지고 있다 [실측].

### #9 MISS 는 진짜 MISS 였다 (같은 카운터로 확인)

MISS 1회 전후: kakao **22→24**(+2) · google **9→10**(+1) · 셀 **9→10**(+1, 새 셀 생성) ·
장소 **324→330**(+6) · `ops_ai_usage` **1,980→1,981**(+1).
⇒ **MISS 1회 = 외부 호출 4회**(Kakao 2 + Google 1 + 번역 LLM 1), **9.5초**, 카드는 정상적으로 5곳 반환.

🔴 **플랜이 폐기했던 추정치 "8초"는 방향은 맞았고 값은 낮았다**(실측 9.5초).
플랜 본문의 *"8s는 근거 없는 추정이었다 … 예산은 측정 후에 쓴다"* 가 옳았다.

⚠ **그런데 이 9.5초로 예산을 확정하면 안 된다** — 포함된 번역 LLM 1콜이 **P-07 의 죽은 Gemini 를 거쳐
openai 폴백으로** 갔다. Tier1 과 같은 이유로, **결제 복구 후 재측정해야 진짜 MISS 비용이 나온다.**

⚠ **레이트 리밋을 모르고 재면 숫자가 거짓말한다** [실측]. 이 라우트는 참가자 **3회/분·20회/시간**,
룸 **6회/분·40회/시간** 이다(MISS 가 유료라 의도된 설계). 3초 간격 연타는 **5/5 가 429** 였는데
**응답 시간은 멀쩡히 나왔다** — 그걸 통계에 넣었으면 "거절 속도"가 "다이닝 카드 속도"가 된다.
하니스는 이제 2xx 만 통계에 넣고 429 를 따로 센다.

---

## P-11 유휴 상시 비용 — 손님은 **0건**, 가이드는 **분당 10건**

[실측] `scripts/qa-perf-idle.mjs` · 룸을 열고 **아무것도 클릭하지 않은 채** 90초 · 진입 후 8초 제외

| 표면 | HTTP 요청 | 전송량 | WebSocket |
|---|---|---|---|
| **손님 룸** | **0건** | 0 KB | **1개** (Supabase realtime) |
| **가이드 콘솔** | **15건 (분당 10.0)** | 21.8 KB (분당 14.5) | **0개** |

✅ **손님 쪽은 설계대로다.** 90초 유휴에 HTTP 폴링이 **한 건도 없고** WebSocket 하나가 일을 다 한다.
K1(SSE 폴백 증폭) 우려의 정상 경로는 이렇게 생겼다.

### 🔴 가이드 콘솔은 realtime 이 안 붙어 폴링이 전부를 지고 있다

유휴 90초에 나간 것:

| 엔드포인트 | 횟수 | 실측 간격 | 소스의 상수 |
|---|---|---|---|
| `/api/ops/bookings/{id}/manifest` | 9 | **10.0초** | `useTourManifest.ts:52` `POLL_MS = 10_000` |
| `/api/tour-mode/guide/overview` | 6 | **15.0초** | `GuideConsole.tsx:80` `POLL_MS = 15_000` |

**두 폴링 다 소스와 일치한다.** 문제는 10초짜리가 **"폴백"이라는 것**이다 —
`useTourManifest` 주석: *"seat_update Realtime 구독 + **폴백 폴링**으로 신선하게 유지한다"*.

**그 폴백이 100% 를 지고 있다** [실측]:
`WebSocket 0개` · manifest 응답의 **`channelTopic: null`** · `anchorRoomId: null` · `vehicles: 0`.

**인과 (소스 실측):**
`lib/ops/seating/manifest.ts` — `anchorRoomId` = *"차량이 붙은 룸"* · `channelTopic` = *"앵커 룸
Realtime Broadcast 토픽"*. `useSeatChannel` 은 토픽이 없으면 **아무것도 구독하지 않는다**
(주석에 *"토픽이 없으면(차량 미배정 등) … 폴백 폴링으로 degrade"* 라고 그대로 적혀 있다).

⇒ **차량 미배정 → 앵커 룸 없음 → 토픽 없음 → 좌석 realtime 미구독 → 10초 폴링이 유일 경로.**

🔴 **그리고 차량 미배정이 예외가 아니라 기본값이다** — 라이브 실측: **룸 16개 중 차량이 붙은 룸 1개.**
(사장님 회사는 **차량 미소유·전부 렌트**라 배차가 룸에 붙는 일 자체가 드물다.)
즉 **좌석 realtime 경로는 사실상 항상 죽어 있고**, 그 사실이 "degrade" 라는 단어 뒤에 가려져 있다.

**심각도 판단은 아직 이르다.** 분당 10건은 서버를 위협하는 수치가 아니다. 문제는 두 가지다 —
① **선언과 실제가 다르다**(realtime 이 주, 폴링이 보조인 줄 알고 읽게 된다)
② 가이드 콘솔은 **투어 내내 열려 있는 화면**이라 8시간이면 **4,800건 / 7 MB** 다 [계산].
→ 수정 여부는 P6 에서, **먼저 "이게 의도된 degrade 인지"를 사람이 판단해야 한다.**

**게이트 가능:** ✅ 유휴 요청 수 상한(표면별). 지금 값을 상한으로 박으면 폴링이 늘 때 빨간불이 난다.

---

## P-12 ⚠ 룸 진입이 하루 만에 2,909 → 4,100ms 로 움직였다 (원인 미분리)

측정 조건은 동일하다(4G · 프로덕션 빌드 · 콜드 캐시 · 3회 중앙값). 바뀐 것은 **베이스**다.

| 시점 | 룸(live) 첫 유의미 | 룸 총 전송 | 베이스 |
|---|---|---|---|
| 오전 | **2,909 ms** | 1,336 KB | `a8dc24a8` |
| 오후 | **4,048 / 4,168 / 4,176 ms** | **1,784 KB** | `8ec0e4c3`(+14커밋) + 이 브랜치 |

🔴 **잡음이 아니다.** 재측정 3회가 4,048~4,176 으로 촘촘했다. 그리고 **같은 페이지 컴포넌트인
로비 라우트는 오히려 내려갔다**(3,164 → 2,863) — 균일한 기계 부하로는 설명되지 않는다.

**원인은 분리하지 못했다.** 후보 셋: ① main 의 14커밋(타 세션 기능 감사 PR #683~#688)
② 내 `lib/ai/router.ts` 변경(서버 측이라 첫 페인트와 무관해야 한다) ③ **시드 데이터 양**
— live 룸의 전송량이 +448 KB 늘었고, 그건 코드가 아니라 내용일 수 있다.
로비만 내려간 것은 ③ 쪽을 시사하지만 **확인하지 않았다.**

**이 항목이 원장에 있는 이유:** 이 값을 그대로 회귀로 단정하면 틀릴 수 있고,
그렇다고 안 적으면 **다음 세션이 4,100ms 를 정상으로 알고 시작한다.** 둘 다 피하려고 적는다.
분리는 P6 에서 — 시드를 고정한 뒤 베이스만 바꿔 두 번 재면 답이 나온다.

🔴 **그리고 이 건이 예산 게이트 설계를 한 번 고치게 했다.** 첫 천장을 **오전(다른 베이스)**
측정에서 뽑아 **오후 코드**에 걸었더니 즉시 초과였다 — 회귀가 아니라 **게이트가 자기 자신을
잡은 것**이다. 천장은 **그것이 지킬 코드에서 재야 한다.**

---

## P-13 ✅ 상설 회귀 게이트 신설 (§F 목표와 분리)

`docs/audit/PERF-BUDGET.json` + `qa-perf-throttled.mjs --check`.

🔴 **§F 예산을 게이트로 쓰지 않았다.** §F 는 목표이고 룸 진입은 지금 미달이다(P-01).
목표를 게이트로 걸면 첫날부터 빨간불이고, **늘 빨간 게이트는 아무도 안 본다.**
그래서 천장은 **오늘 실측 + 여유**이고, 역할은 좋아지게 만드는 게 아니라
**더 나빠지는 걸 조용히 넘기지 않는 것**이다. 고치면 값을 내리고, 올리는 커밋은 이유를 적는다.

| 대상 | 폭 | 왜 |
|---|---|---|
| **jsKB** | 좁게 | 반복측정에서 **0.1KB 단위로 재현**된다 — 회귀 감시의 주력 |
| 시간(첫유의미·LCP) | 넉넉히(+20%) | **데이터 양에 따라 움직인다**(시드마다 다름). 좁게 잡으면 헛불이 난다 |

**가드 3개를 넣었다:** ① 예산에 **없는 라우트는 통과가 아니라 실패**(라우트가 늘면 게이트가 비어버림)
② **프로파일 불일치 시 실패**(다른 조건의 숫자를 비교하지 않는다)
③ **대조 항목 0개면 실패**(비-공허 단정).

**검증:** 좁은 천장(3,200)에서 실제로 **exit 1 + 초과 항목 2건**을 냈고, 실측 기반 천장으로 고치니
**15항목 대조 통과**. 양방향 확인.

---

## 재현 방법

```bash
npm run build && npx next start -p 3182
ALLOW_SIM_SEED=1 npx tsx scripts/sim-tour-day.ts
ALLOW_SIM_SEED=1 npx tsx scripts/sim-lobby-booking.ts
WALK_BASE=http://localhost:3182 node scripts/qa-perf-throttled.mjs --profile 4g --runs 3 --out
WALK_BASE=http://localhost:3182 node scripts/qa-bundle-baseline.mjs        # 무스로틀 바이트 정본
npx tsx scripts/sim-tour-day.ts --cleanup                                   # 🔴 드레인
```

---

## 아직 미측정 (§F 예산 중)

| 지표 | 상태 |
|---|---|
| ~~메시지 전송 → 화면 반영~~ | ✅ 측정 완료 — **PASS** (P-08) |
| ~~카드 탭 → 시트 열림~~ | ✅ 측정 완료 — 첫 탭만 초과 (P-08) |
| ~~컨시어지 Tier1~~ | ✅ 측정 완료 — 원인 규명·코드 수정, **예산은 결제 복구 후 확정** (P-07) |
| ~~다이닝 HIT / MISS~~ | ✅ 측정 완료 — HIT PASS · MISS 9.5초, **예산은 결제 복구 후 확정** (P-10) |
| ~~라우트 p50/p95~~ | ◐ 8/15 실측 (P-09). 나머지는 POST 전용·역할 게이트 |
| 투어당 LLM/Kakao 호출 | ⏸ **계측은 켜져 있고 실투어 트래픽이 0** — 판정 불가 |

**§F 10지표 중 9개 측정 완료.** 남은 1개는 코드가 아니라 트래픽을 기다린다.

**시뮬 불가(구조상)** — 동시 리얼타임 연결 상한 · SSE 폴백 증폭 · 콜드스타트 ·
실기기 백그라운드 타이머 스로틀. `BUNDLE-BASELINE.md` 의 자백과 동일하다.
