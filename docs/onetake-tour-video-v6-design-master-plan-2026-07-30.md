# 원테이크 길안내 영상 — 완성형 설계 + 스킬 완성 플랜 (v6.1)

작성 2026-07-30 · **v6.1 개정 같은 날** — 사장님 확정: **세로가 주 포맷이다.**
이 개정으로 "사장님 결정 대기"(구 W0)는 소멸했다. 남은 설계 결정은 전부 이 문서가
내렸고(§B0 원장), 이후 세션은 재론 없이 만든다. 만들어서 보여드리고 노트를 받는다 —
중간에 결정을 맡기지 않는다.

파이프라인 현재 상태의 정본은 `docs/onetake-tour-video-v5-rebuild-2026-07-30.md`(브랜치
`claude/yonggungsa-video-edit-c14007`)와 스킬 `~/.claude/skills/onetake-tour-video/`.
결정 원장·함정 원장·워크플로우는 스킬 쪽이 정본이다(`references/decisions.md` ·
`references/traps.md` · SKILL.md §0).

**사장님이 준 통치 규칙 4개 (2026-07-30, 이 트랙의 헌장):**
① 다운그레이드 금지 ② 고객 길안내에 충실 ③ 정보 전달은 명확하게 + 눈길을 사로잡게
④ 밟은 함정·오류·미흡은 기록으로 남겨 재발 금지, 작업 방식은 스킬로 묶어 워크플로우화.

> **완성의 정의(DoD):** 새 관광지 = 스펙 JSON 1개 + 음악 베드 1개 + 사람 판정
> (루트·스톱 선정·클레임 문안)만으로, 코드 변경 0으로 **세로 풀 가이드 + 티저 +
> 와이드 소스 + 포스터 + 유튜브 챕터 + 앱 큐 트랙**이 한 번에 나오고 게이트 전부 초록.

---

## §A 실측 (설계의 근거 — v6에서 확정, 변경 없음)

### A-1 현재 산출물 (프레임 추출로 확인)

| | 실측 |
|---|---|
| 와이드 마스터 | **215.2s (3:35)**, 1080p, 그레이딩·배속·폴라로이드 작동, 화면 글자 0개 |
| 세로본 | 54s/90s 하이라이트 컷. 브랜드 셸 완비(kicker · POINT n/N 칩 · 세리프 타이틀 · 서브 · 캡션 · 워터마크) — **타이포 아이덴티티 이미 존재**, 반려 이력 없음 |
| 음악 베드 | Suno 3트랙 크로스페이드, 상업 허가 — 존재 |
| build.mjs `--locale` | 헤더 주석에만 있는 죽은 플래그(renderHold에 텍스트 렌더 없음) |

### A-2 Kraig Adams — 포맷 일치 레퍼런스, 검증 완료

구독 **83.2만 · 영상 311개**. 최고작 `Hiking Alone in Kyrgyzstan`(17:27 · 120.7만회):
설명란 2,068자에 루트 사실+**챕터 8개**+일자별 GPS 링크 · 오프닝 0–1:23은 **전체 여정
하이라이트 몽타주**(무텍스트) · 제목은 정량형("Hiking 120 miles Alone in …") ·
편집본과 (Ambient Footage) 무편집판 쌍 발행.
**화면 텍스트 시스템**: 도착지 라벨(`Manas International Airport, Bishkek`) · 비용
라벨(`Taxi $60`) · 구간 스탯 카드(`Ak-Suu Traverse - Day 1 / 10 miles (16km) /
4,000ft gain (1,200m)`) · 지구본 오리엔테이션 1회 · 내레이션 자막 없음 ·
**프레임당 정보 1개**.

### A-3 서재로36 #3 — 포맷 불일치 최종 확정

30:19 · 218.8만회 · 설명란 0자. 오프닝 스토리보드 판독: t≈10s부터 매 프레임 진행자 +
내레이션 하드자막(하단 중앙 ~88%, 검정 칩 위 흰 볼드 한 줄 ≤ ~18자). 가져올 것은
**제목=주장 · 고정 썸네일 템플릿 · 하단 중앙 흰 글자+스크림이라는 가독 관례** 셋뿐.

### A-4 🔴 레퍼런스 분석 방법 — `t=` 스크린샷은 틀린 도구

페인 미표시 상태에서 screenshot 자체가 타임아웃. 대체: watch 페이지에서
`ytInitialPlayerResponse`를 읽어 **스토리보드 시트**(L3 = 320×180 타일, `M0..Mn`,
10초 간격)를 curl로 받아 판독한다. 전 러닝타임 커버, 영상당 ~5분. 상세 절차는 스킬
`references/editing-craft.md`. 한계: 컷 타이밍은 하한만(타일이 매번 다르면 컷<10s).

---

## §B0 확정 결정 원장 — V6-D1 ~ V6-D11 (재론 금지, 정본은 스킬 `references/decisions.md`)

| # | 결정 | 이유 |
|---|---|---|
| D1 | **세로 1080×1920 풀렝스 가이드가 주 산출물.** 러닝타임 = 와이드와 동일(비트 미러). 별도로 60–90s 티저를 뽑는다 | 손님은 폰으로 본다(앱 임베드·쇼츠 유통). 사장님 확정 |
| D2 | **와이드 16:9는 클린 렌더 소스로 유지 — 글자를 굽지 않는다.** 유튜브 와이드 업로드는 클린본 그대로(선택) | 🔴 세로 밴드가 와이드에서 리프트된다. 와이드에 글자를 구우면 밴드 안에서 2중 표기 + 저해상 축소(함정 T-11). v6의 "와이드 4존" 안은 이 함정 때문에 폐기 |
| D3 | **세로 존 계약: 위 = 길, 아래 = 뜻.** 상단 존 = 영어 웨이파인딩(POI kicker · POINT n/N · 스톱 이름 · 트랜짓 칩), 중단 = 16:9 밴드, 하단 존 = 로케일 해설 전용 | 웨이파인딩(고유명사+숫자)은 로케일 민감도가 낮고, 해설은 손님 언어가 생명. 층이 갈라져야 로케일이 성립 |
| D4 | **노안 플로어가 기본값 — 별도 큰글씨 변형을 만들지 않는다.** 하단 해설 ≥64px·최대 2줄, 스톱 이름 ≥84px, 칩 ≥44px, 흰 글자+스크림 대비 ≥7:1, 표시 ≥ max(2.8s, 글자수/12초) | 가장 약한 눈을 기본값으로 설계하면 변형 렌더 자체가 불필요. 큰 글자는 "눈길 사로잡기"와도 일치 |
| D5 | **제목 register = 주장** (§B5 표 채택). 영상 제목은 정량+주장 교배, 스톱 문안은 주장 | A-2/A-3 실측 |
| D6 | **포스터 = 세로 1080×1920 주 + 1280×720 부, 템플릿 고정** (머니 프레임 풀블리드 + 세리프 클레임 + 네이비 칩) | 서재로36 교훈: 경직된 템플릿이 다양성을 이긴다 |
| D7 | **러닝타임 밴드 3:00–4:30** | Kraig 밀도 실측(구간당 3–4분)과 일치 |
| D8 | **내레이션은 굽지 않는다. 청각 채널은 앱 TTS** — cues.json에 스톱별 `tts` 마커, 앱이 손님 로케일로 읽어준다 | 무내레이션이 포맷 자산 + 노안 대비의 제2채널. 도착 해설 TTS 인프라가 이미 있다 |
| D9 | **로케일 번 순서: en 마스터 먼저, 이후 예약 로케일 상위순으로 추가.** 전 로케일 커버는 앱 오버레이(큐 트랙)가 담당 | 번은 유통용, 오버레이는 완전 커버용. 렌더 비용을 수요에 비례시킨다 |
| D10 | **밴드는 16:9 유지** (4:5/풀블리드 크롭 안 함), 여백은 블러 필 | 크롭은 루트 맥락(길의 좌우)을 자른다. "크게 보고 싶다"는 클로즈업·폴라로이드가 이미 담당 |
| D11 | **앱 계약: 하단 존 텍스트는 앱 글자 크기 설정을 따른다** + 스톱 리스트 텍스트 뷰 병행 제공(큐 트랙이 데이터원) | 노안 대비를 렌더 하나에 다 지우지 않는다 — 오버레이는 유저 스케일 가능 |

**다운그레이드 없음**: v5의 페이싱·화살표·클로즈업·폴라로이드·그레이드·게이트 전부
유지. 와이드 렌더도 계속 나온다(소스+선택 유통). 이 개정은 순수 추가다.

---

## §B 완성형 세로 길안내 영상 — 설계

### B-1 이 영상의 일 (불변)

**① 길을 외우게 한다** ② **가고 싶게 한다.** 시청 맥락: 투어 전날/차량(2–4분 집중) ·
현장 프리타임(스톱 점프 — 챕터가 UI) · 쇼츠/OTA(훅 지배). 무내레이션 유지(D8).

### B-2 정보 6층 모델 → 세로 존 매핑

| 층 | 질문 | 세로에서의 자리 |
|---|---|---|
| L1 약속 | 전체가 뭐냐 | 콜드 오픈 클레임(상단 존, 대형) + 약속 카드 "8 stops · a 10-minute walk" |
| L2 방향 | 어디서 꺾냐 | 밴드 안 감속 1.5x(기존 문법 — 화살표 아님) |
| L3 정체 | 지금 뭐냐 | 상단 존: POINT n/N + 영어 스톱 이름 |
| L4 이유 | 왜 보냐 | **하단 존: 로케일 해설 1줄** (클레임) |
| L5 사진 | 어디서 찍냐 | 폴라로이드 리추얼 + PHOTO SPOT 배지(상단 존) |
| L6 비용 | 얼마나 걷냐 | 상단 존 트랜짓 칩 `next · ≈1½ min` — 소스 타임코드 실측만 |

### B-3 구조 (시간 축 — 와이드 소스와 세로가 비트 미러)

```
0:00–0:10  콜드 오픈: 머니샷 3컷(각 ~2.8s, 디졸브). 상단 존에 클레임 타이틀 대형 안착
0:10–0:16  루트 약속 카드: "8 stops · a 10-minute walk · ends where it starts"
0:16–      본편: v5 비트 시트 그대로. 스톱 리추얼 = 접근 감속 → 상단 라벨 in →
           응시(클로즈업/1x) → [폴라로이드+배지] → 라벨 out → 가속 + 트랜짓 칩
끝-8s      리캡: 폴라로이드 월(그 회차 프린트 4–6장) + 현판 북엔드 + © AtoC Korea
```

### B-4 세로 존 계약 (D3·D4의 수치화 — 캔버스 1080×1920, 밴드 y656–1264)

| 존 | y 범위 | 내용 | 규격 |
|---|---|---|---|
| 상단 | 0–656 | kicker(POI명 대문자) 40px · POINT n/N 칩 44px · **스톱 이름 84px 세리프** · 서브/트랜짓 칩 44px | 전부 영어. 흰 글자, 필요시 스크림(휘도 실측 `pathLuma()` 재사용) |
| 밴드 | 656–1264 | 와이드 마스터 리프트(16:9), 여백 블러 필 | 글자 절대 없음(D2가 소스에서 보장) |
| 하단 | 1264–1840 | **로케일 해설 64px · 줄높이 1.35 · 최대 2줄** | 마스터(en 번)·로케일 번(D9)·앱 오버레이(D11) 전부 이 존만 사용 |
| 최하단 | 1840–1920 | © AtoC Korea 보호 라인 23px(기존, 가독 규격 예외) | |

읽기 시간: 하단 해설 표시 ≥ max(2.8s, 글자수/12초) — 정지·폴라로이드 비트의 hold와
합치. CJK 로케일 번은 글자 단위 줄바꿈 금지(어절/구 단위 클램프 — 저장소 CJK 불변
규칙의 영상판, 게이트 E-8).

### B-5 제목·문안 — 라벨을 주장으로 (스펙 `title`/`sub`/`caption`만 수정)

| 비트 | 지금 (라벨) | 확정 (주장) |
|---|---|---|
| 영상 제목 | Haedong Yonggungsa | **The temple where Korea meets the sea** (sub: One walk · 8 stops · Busan) |
| 13 계단 | The 108 Steps | 108 steps — one for each human desire ⚠`content_locales`로 검증 후 |
| 14 전망대 | Ocean Viewpoint | Walk out over the surf |
| 16 파노라마 | Temple Panorama | The only spot where the whole temple fits in one frame |
| 19 다리 | Crossing Yongmun Bridge | Cross the dragon's bridge |
| 26 관음상 | Haesu Gwaneum | She faces the sea — ask for exactly one thing |
| 30 석탑 | Stone Pagoda | You have come full circle |

발명 금지 유지: 클레임 소스 1순위 = `match_pois.content_locales`(en 5,215자, 검수됨).

### B-6 소리

베드(기존) + 셔터음 1개(폴라로이드 블룸 동기 — 유일 SFX) + 1.5x 이하 현장음. 끝.

### B-7 티저 (구 "세로 90초"의 후계)

**세그먼트 1 = 그 회차 최고 머니 비트**(연대기 아님 — 훅 지배), 이후 연대기 복원,
1.2x 하이라이트를 6s 상한으로 채워 예산 80% 이상. 풀 가이드와 같은 셸.

### B-8 배포 산출물 (빌드 부산물)

`<slug>-vertical.mp4`(주) · `<slug>-teaser.mp4` · `<slug>.mp4`(클린 소스) ·
`<slug>.chapters.txt`(인트로+스톱들+피날레, Kraig 패턴) · `<slug>.cues.json`
(`{beat, kind, t0, t1, poiKey, textKey, tts}` — 앱 오버레이+TTS 계약) ·
`<slug>-poster.jpg` 2종(D6). 챕터·큐의 시각은 손으로 쓰지 않는다 — 빌드 오프셋 산출.

### B-9 용궁사 적용 스펙 델타 (요약, 불변)

콜드 오픈 3컷 = `B 28–42`(사찰+바다 파노라마) · `C 26–34`(용문교) · `C 209–217`(관음
전망) → `A 0` 진입 디졸브 · 약속 카드 "8 stops · a 10-minute walk"(597s 실측 반올림) ·
트랜짓 칩: 상가 87s→`≈1½ min`, 석굴길 37s→`≈40 sec`, 관음 계단 43s→`≈45 sec` ·
리캡 월 4장(전망대·파노라마·다리·관음) + 현판 북엔드(기존 32번).

---

## §C 현재 대비 격차

| 층 | 현재 | 완성형 | 크기 |
|---|---|---|---|
| 페이싱·화살표·클로즈업·폴라로이드·그레이드 | ✅ v5 | 유지(셔터음만 추가) | — |
| 모션 컷 | ❌ | 팬 극대점 스냅 | **중 — 최대 미구현(기존 진단 유지)** |
| 세로 풀렝스 + 존 계약 + 노안 플로어 | ❌ (54s 하이라이트, 38px 캡션) | B-4 | **대 — 이번 본체** |
| 구조(훅·약속·리캡) | ❌ | B-3 | **대** |
| 티저 | 🔶 연대기 54/90s | B-7 | 중 |
| 배포 산출물 | ❌ mp4 2개뿐 | B-8 6종 | 중(부산물) |
| 안정화 | ❌ | 1x 비트 opt-in vidstab | 소 |
| 로케일 | ❌ 죽은 플래그 | 큐 트랙 + `--locale` 실구현 | 중 |

---

## §D WBS — 결정 대기 없음, 전부 실행

작업 워크트리 = `claude/yonggungsa-video-edit-c14007`(렌더 캐시·소스 존재).
각 웨이브 종료 시 산출을 만들어 **보여드린다**(허락이 아니라 시사). 노트 → 스킬 반영.

| 웨이브 | 내용 | 게이트 |
|---|---|---|
| **W1 모션 컷** | `motioncut.mjs`: 같은 소스 클립↔클립 경계를 `panScore()` ±1.5s/0.2s 스텝 팬 극대점으로 스냅(양쪽 동시 이동). freeze 인접 제외. 스펙 재기입 방식 | E-4 |
| **W2 세로 풀 가이드 엔진 (본체)** | 전 비트 밴드 리프트 + B-4 존 셸. 셸 프레임은 비트당 진입 애니 ~1.5s만 렌더하고 정지 구간은 마지막 프레임 루프(전 프레임 스크린샷 금지 — 성능 함정 예방). 스펙 필드 `label`/`transit`(수치는 in/out 델타 자동 계산만)/`chapter`/`poster` | E-1 · E-3 · E-8 |
| **W3 구조 비트** | 콜드 오픈·약속 카드·리캡 월(기존 폴라로이드 PNG 재사용) — B-9 델타 반영 | E-2 |
| **W4 배포 산출물** | chapters.txt · cues.json(+`tts`) · poster.mjs 2종 | E-6 · E-7 |
| **W5 티저 v2** | B-7 선별 규칙 교체 | E-5 |
| **W6 안정화 + 로케일 번** | vidstab 2패스 `stab:true` 비트만(다리·대나무길·오션뷰) A/B 프레임 첨부 · `--locale` 실구현 후 예약 상위 로케일부터 번(D9) | E-8 |
| **W7 레퍼런스 잔여 스터디 (병렬)** | A-4 방법으로 지정 9영상 오프닝 + 해외 후보 잔여 검증 → `editing-craft.md`에 실측 표 추가, B-4 수치 보정판 납품 | — |

---

## §E 게이트 (validate.mjs — 전부 계산 가능)

| # | 게이트 | 식 |
|---|---|---|
| E-1 | 노안 가독 | 상단 이름 ≥84px · 칩 ≥44px · 하단 해설 ≥64px·≤2줄 · 스크림 대비 ≥7:1 · hold ≥ max(2.8, chars/12)s · 존 침범 0(밴드에 글자 0) |
| E-2 | 훅 | 클레임 완전 표시 ≤4.0s · 콜드 오픈 ≤12s · 약속 카드 ≤18s |
| E-3 | 사실 검증 | 번 텍스트의 모든 수사(분·초·m·km)는 스펙 계산 필드에서만 유도 — 자유 숫자 리터럴 FAIL |
| E-4 | 모션 컷 | 클립↔클립 경계 = 팬 극대점 ±0.2s 또는 `nosnap`+사유 |
| E-5 | 티저 | 사용 ≥0.8×예산 · 세그먼트 1 rank = money(폴라로이드/1x) |
| E-6 | 챕터 패리티 | 챕터 수 = 스톱+2 · 단조 · duration 안 |
| E-7 | 포스터 | 프레임 풀해상도 재추출(K-2) · 타이틀 == 클레임 · 템플릿 상수 무변경 |
| E-8 | CJK 줄바꿈 | 번 CJK 텍스트 글자 단위 꺾임 0(계획 줄 수 == 렌더 줄 수, bbox 실측) — 폰트 침묵 폴백도 이 게이트가 잡는다 |

---

## §F 하지 말 것 (기존 + 이번 개정 신설)

- 🔴 **사장님 결정 게이트를 만들지 말 것** — v6이 W0으로 이걸 만들었다가 반려됐다.
  결정은 원장(§B0)에서 스스로 내리고, 만들어서 보여드리고, 노트를 받는다
- 🔴 **와이드 마스터에 글자를 굽지 말 것** — 세로 밴드에서 2중 표기·저해상 축소(T-11)
- 글자를 젊은 눈 기준으로 줄이지 말 것 — 노안 플로어(D4)가 기본값이고 E-1이 막는다
- 서재로36의 자막 밀도·30분 러닝타임·진행자 화법 수입 금지 (A-3 실측)
- 내레이션·TTS를 마스터에 굽지 말 것 — 청각은 앱 TTS(D8)
- 스톱당 텍스트 상한 = 상단 라벨 1 + 하단 해설 1줄 — "더 설명"은 앱 큐 트랙으로
- 전환 추가 금지 · 화살표 방향 추정기 재작성 금지 · 눈 검수로 게이트 대체 금지 (기존)
- `t=` 스크린샷으로 레퍼런스 분석 금지 — A-4 방법 사용

---

## §G 기록·워크플로우 체계 (사장님 헌장 ④의 이행)

- **워크플로우**: 스킬 SKILL.md **§0**이 관광지 1건의 처음→끝 런북(스테이지별 명령·
  게이트·적용 규칙·산출물). 새 세션은 §0을 따라가면 된다
- **결정 원장**: `references/decisions.md` — 날짜·결정·이유. 재론하려면 원장을 고치는
  PR급 사유가 필요하다
- **함정 원장**: `references/traps.md` — 스테이지별 전 함정 통합(측정→저작→렌더→검수→
  레퍼런스→프로세스). 새 함정은 여기 + 계산 가능하면 게이트로
- 이 문서(§B0·§E·§F)와 스킬 원장이 어긋나면 **스킬 원장이 이긴다**(스킬이 매 세션
  로드되는 쪽이므로)
