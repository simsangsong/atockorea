# Namecheap Custom MX 设置指南

## 🎯 使用 Custom MX 选项添加 MX 记录

从你的截图可以看到 Namecheap 提供了邮件服务配置选项，包括 **"Custom MX"**。这是添加自定义 MX 记录的正确方法。

---

## 📋 步骤 1：找到邮件服务设置

### 在 Advanced DNS 页面：

1. **查找邮件服务配置部分**
   - 可能在页面顶部或中间位置
   - 标题可能是 "Email Settings"、"Mail Settings" 或类似

2. **找到下拉菜单**
   - 当前可能显示 "Email Forwarding" 或其他选项
   - 点击下拉菜单查看所有选项

---

## 📋 步骤 2：选择 Custom MX

### 下拉菜单选项包括：

- **No Email Service** - 无邮件服务
- **Email Forwarding** - 邮件转发
- **MXE Record** - MXE 记录
- **Custom MX** ← **选择这个！**
- **Private Email** - 私有邮件
- **Gmail** - Gmail 配置

### 操作：

1. **点击下拉菜单**
2. **选择 "Custom MX"**
3. 选择后，界面会显示添加 MX 记录的选项

---

## 📋 步骤 3：添加 Enable Sending MX 记录

### 从 Resend Dashboard 获取信息：

- **Type:** MX
- **Name:** `send`
- **Content:** `feedback-smtp.ap-north...`（完整值）
- **Priority:** `10`

### 在 Namecheap 填写：

1. **Host 字段**
   - 输入：`send`

2. **Value 字段**
   - 输入从 Resend Dashboard 复制的完整 Content 值
   - 例如：`feedback-smtp.ap-northeast-1.amazonses.com`

3. **Priority 字段**
   - 输入：`10`

4. **TTL 字段**
   - 选择 `Automatic` 或 `3600`

5. **保存**
   - 点击 "Save" 或 "Save All Changes"

---

## 📋 步骤 4：添加 Enable Receiving MX 记录（可选）

### 从 Resend Dashboard 获取信息：

- **Type:** MX
- **Name:** `@`
- **Content:** `inbound-smtp.ap-northe...`（完整值）
- **Priority:** `9`

### 在 Namecheap 填写：

1. **Host 字段**
   - 输入：`@`

2. **Value 字段**
   - 输入从 Resend Dashboard 复制的完整 Content 值
   - 例如：`inbound-smtp.ap-northeast-1.amazonaws.com`

3. **Priority 字段**
   - 输入：`9`

4. **TTL 字段**
   - 选择 `Automatic` 或 `3600`

5. **保存**

---

## ⚠️ 重要提示

### 1. 确保选择 Custom MX

- 不要选择 "Email Forwarding" 或其他选项
- 必须选择 **"Custom MX"** 才能添加自定义 MX 记录

### 2. 可以添加多个 MX 记录

- 如果 Custom MX 界面允许添加多条记录
- 可以同时添加：
  - Host: `send`, Priority: `10`
  - Host: `@`, Priority: `9`

### 3. 如果 Custom MX 界面不显示

- 可能需要先保存 "Custom MX" 选择
- 然后刷新页面
- 或查找 "Add MX Record" 按钮

---

## ✅ 完成检查清单

- [ ] 已找到邮件服务配置下拉菜单
- [ ] 已选择 "Custom MX"
- [ ] 已添加 Enable Sending MX 记录（Host: `send`, Priority: `10`）
- [ ] 已添加 Enable Receiving MX 记录（Host: `@`, Priority: `9`）（可选）
- [ ] 所有记录已保存
- [ ] 等待 5-30 分钟让 DNS 传播

---

## 🎯 快速操作流程

```
Advanced DNS 页面
  ↓
找到邮件服务配置下拉菜单
  ↓
选择 "Custom MX"
  ↓
添加 MX 记录：
  - Host: send, Value: feedback-smtp..., Priority: 10
  - Host: @, Value: inbound-smtp..., Priority: 9
  ↓
保存所有更改
  ↓
等待 DNS 传播
  ↓
在 Resend Dashboard 验证
```

---

## 🆘 如果遇到问题

### Q: 选择 Custom MX 后没有显示添加界面？

**A:**
1. 尝试刷新页面
2. 查找页面上的 "Add MX Record" 或 "Manage MX Records" 按钮
3. 或联系 Namecheap 支持

### Q: 可以添加多条 MX 记录吗？

**A:**
- 是的，通常可以添加多条
- 每条记录有不同的 Host 和 Priority
- 确保每条记录都正确填写

### Q: Priority 值重要吗？

**A:**
- 是的，非常重要
- Enable Sending MX: Priority `10`
- Enable Receiving MX: Priority `9`
- 数字越小，优先级越高

---

祝你设置顺利！🎉

