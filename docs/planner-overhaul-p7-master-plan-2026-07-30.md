# 플래너 전면 재편 P7 — 재리뷰 마스터 플랜 (2026-07-30)

> **SoT.** 사장님 지시(`docs/OWNER-DECISIONS-2026-07-29.md` §8)는 "가독성 최상 · UI 디자인 감성 최상"이고,
> 이번 재리뷰의 렌즈는 **사용자 체험 최우선 · 감각적 · 여행 감성**이다.
> 기존 플랜의 P2~P6 및 §N-9와 어긋나는 곳이 있고, **어긋나는 이유를 실측으로 적었다.**
>
> 선행 문서: 마스터 플랜 `ops-staff-design-unification-master-plan-2026-07-27.md` §D-3(Part D) · §N-9

---

## §A 착수 전에 다시 잰 것 — 그리고 플랜이 틀린 곳 세 군데

### A-1 🔴 P2·P3·P4는 **이미 배포돼 있다** (P0·P1에 이어 세 번째)

플랜은 P2~P6을 "P7이 흡수한다"고 적어 뒀다. 코드를 열어 보니 셋은 이미 살아 있다.

| 티켓 | 플랜이 말하는 것 | 실제 |
|---|---|---|
| **P2** 스톱 행 한 줄 + `⋯` 시트 | 미착수 | `PlanEditorClient:2308` `expandedStopId` — 한 번에 하나만 펼침. **실측 행 높이 44px** |
| **P3** `TimeWheel` 이관 | 미착수 | `:49` import · `:3132` 렌더. 네이티브 `<input type="time">` 없음 |
| **P4** 좌석 3상태 | 미착수 | `seatAssigned` / `seatPending` / `seatNone` 카피 존재 |
| **P5** `PlanEditorClient` 분할 | 미착수 | ✅ 진짜 미착수 — **3,591줄**(플랜의 3,081보다 510줄 늘었다) |
| **P6** 게이트 | 미착수 | ✅ 진짜 미착수 |

**그래서 "P2~P6 흡수"라는 P7의 정의는 대부분 이미 있는 것을 흡수한다고 적고 있다.**
진짜 잔여는 P5와 P6뿐이고, 그 둘은 **감성과 아무 상관이 없다.**

⚠ 이건 이 레포에서 **세 번째**다(P0·P1이 두 번). 착수 전 5분 grep이 라운드를 아낀다.

### A-2 🔴 그래서 "컴팩트하게"는 **이미 실행됐고, 불만은 남았다**

사장님 원문(2026-07-27)은 *"플래너 UI가 너무 복잡하고 조잡함"* 이었다.
플랜은 그것을 **밀도 문제**로 읽고 P-D20(스톱 행 ~56px 한 줄)을 처방했다.
처방은 실행됐고 **실측 44px로 목표보다 더 줄었다.**

그리고 사장님은 그 뒤에 다시 말씀하셨다 — *"UI 전면 재편해서라도 가독성 최상에 감성 최상."*

**처방이 틀렸다는 뜻이다.** 이유는 단어 하나에 있다:

> **조잡함 ≠ 큼.** 조잡함은 **위계가 없다**는 뜻이다.
> 줄이면 위계가 생기는 게 아니라 **전부 똑같이 작아진다.**

그리고 이 판단은 이미 기록돼 있던 사장님 취향과도 어긋난다 —
*"프리미엄 감사의 기본 추론은 어디에 공예를 더할지이고, 축소는 다운그레이드다."*

### A-3 §N-9(스킨 A/B)는 이 티켓의 핵심이 **아니다**

§N-9는 "플래너에 스킨이 안 먹는다"를 풀려고 특이도 문제를 파고들었고, 추천은 B(배경만 스킨)였다.
맞는 분석이지만 **배경색 문제**다. 지금 플래너가 여행처럼 안 느껴지는 이유는 배경이 회녹색이어서가 아니다.
아래 §B가 진짜 이유다. **N-9는 P7의 마지막 단계이지 첫 단계가 아니다.**

---

## §B 실측 — 왜 여행처럼 안 느껴지는가

프로덕션 빌드 · 390×844 · 전세(vehicle) 상품 예약 · lead 권한 획득 후.

### B-1 🔴 사진이 없다. 한 장도.

| 화면 | 실측 |
|---|---|
| 코스 고르기(첫 탭) | **사진 0장 · 첫 화면 이미지 면적 0%** |
| 장소 피커 | 썸네일 **슬롯은 있는데 비어 있다**(연녹색 빈 사각형) |
| **내 하루** | 스톱 행 **44px 텍스트 줄** · `loadedImages: 0` |

손님은 이 화면에서 **여행에서 가장 감성적인 결정**을 한다 —
「East Route: UNESCO & K-Drama Coast」 · 「West Route: Beaches, Tea & Cafe」 · 「South Route: Waterfalls & Volcanic Coast」.
그런데 카드 3장이 **같은 아이콘 · 같은 레이아웃 · 같은 버튼**이고, 그 장소들의 사진은 **한 장도 없다.**
고르는 근거가 **이름뿐**이다.

**대조 증거:** 같은 플래너의 **고정 일정(읽기전용) 뷰는 사진이 있고, 훨씬 낫다.**
(스크린샷 비교: `planner-01-before.png` = 사진 3장 · `planner-01-editor.png` = 0장)
즉 이 앱은 **이미 좋은 버전을 갖고 있고, 손님이 편집하는 쪽에만 없다.**

### B-2 🔴 그리고 원인은 디자인이 아니라 **배선**이다

```
match_pois                                        124
  default_image_url 있음                           12  ( 9.7%)   ← 플래너가 읽는 유일한 컬럼
    …그중 파일이 실제로 존재                        11              (1건은 404, 아래)
  images[0] 있음                                   75  (60.5%)
    …그중 파일이 실제로 존재                        72
  default_image_url || images[0] 로 해결 가능       73  (58.9%)   ← 오늘 당장 가능
  그래도 사진 없음                                  51  (41.1%)   ← 사람 게이트(콘텐츠)
```

- 같은 레포의 `components/itinerary-builder/POICatalogGrid.tsx:121`은 **이미**
  `poi.default_image_url || poi.images?.[0]` 로 읽는다.
- 피커가 쓰는 API `/api/itinerary-builder/pois`는 **이미 `images`를 내려주고 있다**(select에 포함).
- 플래너만 `default_image_url` 하나만 읽는다(`PlanEditorClient:2816`, `:3473`).

> **한 줄 폴백으로 플래너 사진 커버리지 8.9% → 58.9% (6.6배).**
> 새 데이터 수집 0 · 새 API 0 · 이미 전송되고 있는 필드.

🔴 이건 이 레포에 기록된 사고와 **같은 유형**이다 — POI 해설이 `match_pois`를 안 읽어
커버리지가 11이었다가 리졸버를 고치자 122가 된 건. **"없다"가 아니라 "안 읽는다"였다.**

**부수 발견:** `yongmeori_coast`의 `default_image_url`이 `/images/itinerary/yongmeori-coast-wikimedia-cc0.jpg`
를 가리키는데 **그 파일이 레포에 없다.** 라이브 피커에서 404가 난다.

### B-3 "내 하루"가 화면의 주인공이 아니다

손님이 이 화면에 온 이유 = **내 하루를 만드는 것.** 그런데 「Your day」는
히어로 → 탭 3개 → 검색창 → 스크롤되는 장소 목록 → 「Google 지도에서 찾기」 **아래**,
스크롤 1,860px 중 **거의 바닥**에 있다.

만들고 있는 물건이 화면의 맨 밑에 있다.

### B-4 그 밖에 잰 것

| 항목 | 값 | 메모 |
|---|---|---|
| 첫 탭 스크롤 높이 | 4,496px | 코스 3개를 보는 데 5.3화면 |
| 코스 적용 후 | 1,860px | |
| 첫 화면 탭 가능 요소 | 7 | 나쁘지 않다 |
| `.tr-card` | 16 | 카드가 16장 — 전부 같은 무게 |
| `data-tr-skin` | **없음** | N-i 그대로 |
| `.tr-atmos`(광원) | **없음** | N-j 그대로 |
| 콘솔 에러 | 1 (위 404) | |
| 제목 잘림 | 「Jeju Tangerine Picking Experi…」 | |

---

## §C 재리뷰한 설계 원칙 (P7-D1 ~ P7-D7)

- **P7-D1 플래너는 폼이 아니라 예고편이다.**
  "내일 뭘 채우지"가 아니라 **"내일 이걸 보게 된다"**를 보여주는 화면.
  이 한 문장이 아래 전부를 결정한다.

- **P7-D2 🔴 줄이지 말고 위계를 줘라 — P-D20을 여기서 뒤집는다.**
  44px 행 8개는 위계가 아니라 **균일함**이다. 조잡함의 반대는 작음이 아니라 **주인공이 있음**이다.
  ⚠ 단, 되돌리는 것이 아니다. 한 줄 행은 **접힌 상태의 기본값으로 유지**하되
  (그건 스캔에 좋다) **지금 중요한 하나**는 크게 연다.

- **P7-D3 🔴 사진을 1급 시민으로.**
  ① 배선 먼저(`default_image_url || images[0]`) ② 코스 카드에 구성 POI 사진
  ③ 피커 행 썸네일 ④ 내 하루의 스톱마다 썸네일.
  **사진 없는 41%는 빈 사각형이 아니라 스와치 폴백**(#335에서 이미 만든 패턴 — 이니셜+카테고리 색).
  빈 사각형은 "미완성"으로 읽히고, 스와치는 "이 장소는 사진이 아직"으로 읽힌다.

- **P7-D4 「내 하루」가 화면의 주인공.**
  고르는 도구(검색·목록)보다 **만들어진 결과**가 위에 온다.
  비어 있을 때도 자리를 지킨다(N4에서 배운 것 — 빈 것은 숨길 자리가 아니라 말할 자리).

- **P7-D5 하루의 결(N2)을 플래너에도.**
  아침/한낮/해질녘 톤은 **룸에 이미 있는 층**이다. 새로 만들지 않고 스코프만 넓힌다.
  ⚠ N2의 실측 교훈: **다크는 제외**(색조 없는 기준선이 이미 4.39라 층을 얹을 여유가 없었다).

- **P7-D6 §N-9는 B안(배경만 스킨) — 단 P7의 마지막 단계.**
  게이트(`resolvePlan()`에 스킨 인자) 선행은 그대로 유효하다. 순서만 뒤로 민다.

- **P7-D7 🔴 정보를 줄이지 않는다.**
  U-D25(I2)의 교훈: 손님이 알던 것을 지우면 "기능이 사라졌다"가 된다.
  탭 3개 구조·모든 목적지·니즈 체크리스트는 **전부 유지**한다. 바뀌는 것은 **무게와 순서**다.

---

## §D WBS — 재정렬

**원칙: 제일 싼 감성 개선을 제일 먼저.** P7.1은 재설계가 아니라 배선이고, 그 한 단계가
이 화면의 인상을 가장 크게 바꾼다.

| 단계 | 무엇 | 규모 | 왜 이 순서 |
|---|---|---|---|
| **P7.0** | 게이트 선행 — `skinContrast`의 `resolvePlan()`에 스킨 인자, 플래너 쌍을 10스킨×2테마로 | S | 플래너는 **이미 한 번 게이트 밖에서 `ink-3` 미달로 배포**된 적이 있다 |
| **P7.1** 🔴 | **사진 배선** — `default_image_url \|\| images[0]` 폴백(2곳) · 스와치 폴백 · 깨진 default 1건 정리 · 상설 게이트(커버리지 회귀 금지) | **S** | **8.9% → 58.9%.** 이 티켓 전체에서 감성 대비 비용이 가장 좋다 |
| **P7.2** | 코스 선택을 **예고편**으로 — 구성 POI 사진 히어로, 3장이 서로 달라 보이게 | M | 손님이 하루를 고르는 그 화면 |
| **P7.3** | **「내 하루」를 주인공으로** — 위치를 위로, 스톱에 썸네일, 지금/다음 하나만 크게 | M | P7-D2·D4 |
| **P7.4** | 피커 리듬 — 썸네일 채워진 행, 제목 잘림 해소, 스크롤 4,496px 정리 | S | |
| **P7.5** | 하루의 결(N2 스코프 확장) + 광원(N8 플래너분) | S | 층은 이미 있다 |
| **P7.6** | `PlanEditorClient` 3,591줄 분할 (구 P5) | M | **여기서 한다** — 앞 단계가 무엇을 만질지 안 뒤에 쪼개야 두 번 안 쪼갠다 |
| **P7.7** | §N-9 B안 — 배경만 스킨 | M | P7.0 게이트 위에서만 |
| **P7.8** | 게이트 + 10로케일 + 전후 컷 (구 P6) | S | |

### 🔴 사람 게이트 (사장님)

1. **사진 없는 POI 51개(41%)** — 배선을 고쳐도 남는다. 레포에 파이프라인은 이미 있다
   (`scripts/wire-poi-images.mjs` · `import-atoc-modified-photos.mjs`).
   제주 61곳 중 사진 있는 곳이 몇 곳인지가 이 화면의 상한이다.
2. **P7-D2 확인** — 스톱 행을 "더 줄이는" 방향을 **뒤집는다**는 것. 이 문서의 판단이지만
   원 지시가 "조잡함"이었으므로 한 번 확인받는 것이 맞다.

---

## §E 안 하는 것 (명시)

- **스톱 행을 더 줄이지 않는다.** 이미 44px이고, 더 줄이면 목록이 아니라 표가 된다.
- **탭 3개를 없애지 않는다.** I2에서 배운 것 — 접는 건 되고 지우는 건 안 된다.
- **니즈 체크리스트·실행가능성 경고·좌석 3상태를 빼지 않는다.** 전부 살아 있는 기능이다.
- **P2/P3/P4를 다시 만들지 않는다.** 이미 있다(§A-1).

---

# §G 디자인 감사 — 실측 (2026-07-30, 2차)

> 지시: *"카드, 버튼 테두리 음영 모서리, 폰트, 위계, 배치, 애니메이션 효과, 버튼 효과 등등
> 디테일하게."* 아래는 **전부 브라우저 계산값**이다. 같은 브라우저에서 플래너와 룸을 나란히
> 열어 1:1로 비교했다(프로덕션 빌드 · 390×844 · 전세 예약 · lead 획득 후).
>
> 판정 기준은 취향이 아니라 **이 앱이 이미 갖고 있는 시스템**이다. 룸은 성숙한 재질 체계를
> 갖고 있고(승강 `--tr-elev-1/2/3` + 광택 `--tr-rim`, 버튼 물리 M-D4, 모션 어휘 M-D3),
> 플래너는 그 옆에서 **자기만의 두 번째 체계**를 키웠다.

## G-1 🔴 승강(elevation)이 두 벌이고, 플래너 쪽엔 빛이 없다

| | 룸 | 플래너 |
|---|---|---|
| 카드 그림자 | `rgba(255,255,255,.7) 0 1px 0 inset` **+ 링 + elev-2** | `rgba(31,58,48,.04) 0 1px 2px, …06 0 10px 24px` |
| 광택(`--tr-rim`) | **있음** | **없음** |
| 토큰 | `--tr-elev-1/2/3` · `--tr-rim` | `--tr-plan-shadow-card/soft/button` (별도) |

`.tr-plan-root .tr-card` 가 룸 카드를 **덮어쓰면서 `--tr-rim` 을 통째로 뺀다.**
이 파일의 주석이 그 rim 을 설명하는 문장이 이렇다 —
*"홈 타일은 물건처럼 보이고 나머지는 색종이처럼 보였던 이유."*
**플래너는 지금 색종이 쪽에 있다.**

## G-2 🔴 다크 모드에서 플래너는 승강이 **0** 이다

    다크 실측:  카드 NONE · 히어로 NONE · 기본 버튼 NONE

`--tr-plan-shadow-{card,soft,button}: none` 이 다크 블록에 박혀 있다.
룸은 다크에서도 승강을 유지한다(다크용 값이 따로 있다). 즉 **다크의 플래너는
버튼조차 눌러지는 물건으로 안 보인다.** 평평한 사각형 평면이다.

## G-3 🔴 버튼 위계가 **뒤집혀 있다** (실측)

| 버튼 | 하는 일 | 실측 높이 | 글자 |
|---|---|---|---|
| **「Use this route」** | **하루 전체를 채우는 커밋** | **36px** | **12px** |
| 「Add」 | 장소 하나 추가 | **44px** | 12px |
| (참고) 룸의 보통 버튼 `.tr-btn-physical` | — | **48px** | **16px** |

가장 결과가 큰 행동이 **가장 작다.** 그리고 플래너에서 제일 큰 버튼조차
룸의 **평범한** 버튼보다 작고 글자가 4px 작다.

CSS 주석은 *"loud tier 는 진짜 커밋에만 배급한다"* 고 옳게 적어 놨는데,
호출부에서 그 커밋에 `--sm` 이 붙어 있다. **의도와 배선이 어긋났다.**

## G-4 모서리 반경이 7종 — 가족처럼 안 보이는 첫째 이유

| | 룸 | 플래너 |
|---|---|---|
| 카드 | **20** | **16** |
| 입력 | 22 | **14** |
| 버튼 | 16 (`btn-physical`) | **13** / sm **11** |
| 히어로 CTA | 18 | (없음) |
| 세그먼트 | — | 14 트랙 / 11 썸 |
| 타일 | — | 16 |
| 칩 / 말풍선 | 9999 / 18 | 9999 / 18 |

**일치하는 건 칩과 말풍선뿐.** 반경은 UI 에서 "같은 집안"을 알리는 가장 강한 신호이고,
지금 플래너는 20·16·14·13·11 을 동시에 쓴다. 룸은 4종으로 끝난다.
그리고 **중첩 규칙이 없다** — 16 안에 11 이 들어가고 20 안에 16 이 들어가는데
둘 사이에 일관된 관계가 없다.

## G-5 🔴 손님의 글자 크기·스킨 설정이 플래너에 **도달하지 않는다** (신규 발견)

같은 `localStorage` 상태로 두 화면을 열어 **대조군까지 측정**했다:

    룸     textScale:5  →  --tr-font-scale 1.35 · data-tr-skin "classic"   OK
    플래너  textScale:5  →  --tr-font-scale 1    · data-tr-skin 없음         X

원인은 한 줄이다. `RoomShell` 은 `useShellSurface()` 가 주는 `surfaceProps`
(`data-tr-skin` + `--tr-font-scale`)를 붙이는데, `PlanEditorClient` 는
`useResolvedTheme()`(테마만) 만 쓰고 **`useShellSurface` 를 아예 부르지 않는다.**

🔴 **이게 §N-9 를 다시 쓰게 만든다.** 플래너에 스킨이 없는 이유는 CSS 특이도가 아니라
**애초에 요청하지 않기 때문**이다. 특이도 문제는 스탬프한 *다음에* 생기는 2차 문제다.

그리고 이건 접근성 회귀다 — 글자를 키운 손님이 **작은 글자가 제일 많은 화면에서** 설정을 잃는다.

## G-6 모션이 어휘 밖에 있다

| | 룸 | 플래너 |
|---|---|---|
| `.tr-press` 사용 | **13개** | **0개** |
| `tr-anim-*` | 1 | 0 |
| 히어로 transition | — | **`all 0s`** (없음) |
| 타일 transition | — | **`all 0s`** (없음) |
| 코스 카드 | — | **160ms** ← 어휘에 없는 값 |
| 헤더 버튼 | 물리 프레스 | **`active:scale-95`** (생 Tailwind) |

M-D3 가 정한 값은 `--tr-dur-fast 120ms` / `--tr-dur-base 200ms` /
`--tr-ease-out cubic-bezier(.2,.8,.2,1)` / `--tr-ease-spring` 뿐이다. 160ms 는 그 밖이다.
그리고 코스 카드의 눌림은 `scale(0.995)` — 358px 카드에서 **한쪽 0.9px**, 사실상 안 보인다.

## G-7 타이포가 아래쪽에 몰려 있다

플래너의 타입 클래스 사용 112회 중 **69회가 가장 작은 두 단계**다
(`tr-label` 12px 43회 · `tr-meta` 11px 26회). 큰 단계는 `tr-display` 2회 · `tr-body-lg` 2회.

"조잡하다"의 실체가 여기 있다 — **작은 글자가 많은 게 아니라, 큰 글자가 없다.**
위계는 작은 것을 더 줄여서가 아니라 **주인공을 키워서** 생긴다.

## G-8 배치

| 항목 | 실측 |
|---|---|
| 히어로 높이 | **164px** (뷰포트의 19%가 장식) |
| 세그먼트 상단 | y=200 — 내용 시작 전에 **29%** 소모 |
| 코스 탭 스크롤 | **4,496px** (코스 3개를 보는 데 5.3화면) |
| `.tr-card` 개수 | **16** — 전부 같은 무게, 주인공 0 |
| `.tr-card-hero` | 룸 1 / 플래너 **0** |
| 제목 잘림 | 「Jeju Tangerine Picking Experi…」 |

---

# §H 프리미엄 스펙 — 무엇을 어떤 값으로

> 원칙: **새 언어를 만들지 않는다.** 룸이 이미 가진 것을 플래너가 상속하고,
> 플래너가 정말로 달라야 하는 곳(밀도)만 한 단계 좁힌다.

## H-1 재질 — 승강 하나로 통합

- `--tr-plan-shadow-card/soft/button` **폐기**. `--tr-rim` + `--tr-elev-1/2/3` 사용.
- 카드 3층으로 정리:

  | 층 | 클래스 | 무엇 |
  |---|---|---|
  | 주인공 | `.tr-card-hero` | **「내 하루」 딱 하나** (룸의 규율 그대로: 한 화면에 하나) |
  | 보통 | `.tr-card` | 코스 카드 · 니즈 · 좌석 |
  | 안쪽 | `.tr-card--flat` | 카드 안의 카드(스톱 행) — 그림자는 이음매로 읽힌다 |

- 🔴 **다크에서 그림자를 `none` 으로 죽이지 않는다.** 룸의 다크 승강을 그대로 상속하고,
  광택은 다크 알파(`rgba(255,255,255,.06)`)로 유지한다. 다크에서 물건이 물건으로 보여야 한다.

## H-2 모서리 — 4종 + 중첩 규칙 하나

    카드 / 히어로 카드     20px   (= --tr-radius-card, 룸과 동일)
    버튼 / 타일 / 썸네일   14px
    버튼 sm / 세그먼트 썸  10px
    칩 / 아바타            9999px

**중첩 규칙: 자식 반경 = 부모 반경 − 부모 패딩.**
(20px 카드에 16px 패딩이면 안쪽 요소는 최대 14px. 지금은 16 안에 11 이 들어가 있고 근거가 없다.)
→ 20 / 14 / 10 이 이 규칙에서 자동으로 나온다. **7종 → 4종.**

## H-3 버튼 — 4티어, 그리고 위계를 되돌린다

| 티어 | 쓰는 곳 | 높이 | 글자 | 재질 |
|---|---|---|---|---|
| **hero** | 「가이드에게 보내기」 · **「이 코스로 하기」** | **56px** | **15px** | `.tr-cta-hero` 문법 — accent-soft 워시 + 1.5px accent 링 + 바닥 닿는 눌림 |
| **primary** | 인라인 커밋 | **48px** | 14px | 그라디언트 + 상단 inset + 컬러 리프트 |
| **soft** | 추가 · 토글 | **44px** | 13px | accent 워시 |
| **quiet** | 취소 · 지우기 | **44px** | 13px | surface + hairline |

🔴 **불변식: 커밋 버튼은 그 화면의 어떤 보조 버튼보다 크다.**
지금 36 < 44 인 것이 정확히 이 규칙 위반이고, **테스트로 고정한다**(§I).

눌림 물리는 룸의 M-D4 를 그대로:
`translateY(1px) + scale(0.98)` + 그림자 압축 + `brightness(.96)`.
(현재 `scale(0.995)` 는 한쪽 0.9px — 없는 것과 같다.)

## H-4 타이포 — 주인공을 만든다

| 자리 | 지금 | 스펙 |
|---|---|---|
| 화면 제목 | `tr-display` 20px | 유지 |
| **「내 하루」 제목** | `tr-title` 15px | **`tr-body-lg` 18px + 주인공 카드** |
| 스톱 이름 | `tr-card-text` 13px | **`tr-title` 15px** — 손님이 읽는 유일한 고유명사다 |
| 스톱 시각 / 체류 | `tr-meta` 11px | 유지 (`tr-num` 유지) |
| 버튼 | 12px 일괄 | **티어별 15 / 14 / 13** (H-3) |
| 섹션 헤딩 | `tr-label` 12px | **`tr-title` 15px** |

🔴 **그리고 `--tr-font-scale` 을 배선한다** (G-5). 위 숫자는 전부
`calc(… * var(--tr-font-scale))` 위에 있으므로, 배선 한 줄이 이 표 전체를 접근성 5단계에 태운다.

## H-5 모션 — 어휘 안으로, 그리고 하루가 살아나게

- 160ms · `all 0s` · `active:scale-95` **전부 제거** → `--tr-dur-fast/base` + `--tr-ease-out/spring`.
- `.tr-press` 를 카드·행·타일 전부에.
- **의미 있는 전이 4개만** (장식 금지):

  | 언제 | 무엇 | 값 |
  |---|---|---|
  | 코스를 고름 | 「내 하루」가 채워짐 — 스톱이 위에서부터 순차 등장 | `tr-anim-panel-in` 파생, stagger 40ms, 총 ≤ 320ms |
  | 스톱 추가 | 그 행만 slide + fade | 200ms `--tr-ease-out` |
  | 스톱 재정렬 | 위치 이동을 transform 으로 | 200ms |
  | 스톱 펼침 | 높이 + 내용 페이드 | 200ms `--tr-ease-spring` |

- `prefers-reduced-motion` 에서 **전부 정지**(룸 블록에 이미 있다 — 새 키프레임을 그 목록에 추가).

## H-6 배치 — 만들고 있는 물건을 위로

    지금                          스펙
    ─────────────────            ─────────────────
    히어로 164px                  히어로 96px (스크롤 시 크롬으로 융합)
    세그먼트                      ▸ 내 하루  ← 주인공 카드, 비어 있어도 자리를 지킴
    검색창                        세그먼트
    장소 목록(스크롤)              고르는 도구(코스 / 장소 / 가이드에게)
    …                            니즈 · 좌석
    내 하루  ← 바닥               고정 하단: 저장 상태 + 보내기

- 코스 카드에 **사진 히어로**(P7.1 배선 위에서). 3장이 서로 달라 보여야 고를 수 있다.
- 제목 잘림은 `line-clamp-2` + `text-cjk-body` 로.

## H-7 여행 감성 레이어 (마지막에, 대비 검증 후)

- **하루의 결**(N2 재사용): 아침/한낮/해질녘 톤을 「내 하루」 카드에만. ⚠ 다크 제외(N2 실측).
- **타임라인 리본**: 스톱 사이 세로 실선 + 번호 뱃지 — 읽기전용 뷰가 이미 이 문법이고 더 낫다.
- 스킨은 §N-9 B안(배경만) — **G-5 배선이 선행**이다.

---

# §I 게이트 (이 스펙이 되돌아가지 않게)

| 게이트 | 무엇을 고정 |
|---|---|
| `plannerButtonHierarchy` | 🔴 **커밋 버튼 ≥ 보조 버튼** (36<44 재발 금지) · 티어별 높이/글자 |
| `plannerRadiusScale` | 반경은 20/14/10/9999 넷뿐 · 중첩 규칙 |
| `plannerElevation` | `--tr-plan-shadow-*` 부재 · 다크에서 `none` 금지 · `.tr-card-hero` 는 화면당 1개 |
| `plannerSurfaceContract` | 🔴 `useShellSurface` 소비 — `--tr-font-scale`·`data-tr-skin` 도달 |
| `plannerMotionVocabulary` | 120/200ms + 정해진 이징만 · 생 `active:scale-*` 금지 · reduced-motion |
| `skinContrast` (확장) | `resolvePlan()` 에 스킨 인자 (기존 §N-9 요구) |

# §J WBS 갱신 — §D 를 이걸로 대체한다

| 단계 | 무엇 | 규모 |
|---|---|---|
| P7.0 | 게이트 선행(`resolvePlan` 스킨 인자) | S |
| **P7.1** 🔴 | 사진 배선 (§B-2) | S |
| **P7.1b** 🔴 | **`useShellSurface` 소비** — 글자 크기·스킨 도달 (G-5). 접근성 회귀 수정 | **S** |
| **P7.2** | 재질·반경·버튼 통합 (H-1·H-2·H-3) + 게이트 3종 | M |
| **P7.3** | 타이포 위계 (H-4) | S |
| **P7.4** | 배치 — 「내 하루」를 주인공으로 (H-6) | M |
| **P7.5** | 모션 (H-5) | M |
| P7.6 | 코스 카드 사진 히어로 · 피커 리듬 | M |
| P7.7 | `PlanEditorClient` 분할 | M |
| P7.8 | 여행 감성 레이어 + §N-9 B안 (H-7) | M |
| P7.9 | 최종 게이트 + 10로케일 + 전후 컷 | S |

**P7.1b 가 P7.2 보다 앞인 이유:** 글자 크기가 안 닿는 상태에서 타이포 스펙을 잡으면
**5단계 중 1단계에서만 맞는 값**을 잡게 된다.

## §F 실행 로그

> 형식 규칙(X19 선행): **상태 칼럼은 `DONE` / `OPEN` / `SKIP` 셋뿐이다.** 취소선·✅·본문 서술을
> 섞지 않는다. 이 표가 파싱 가능한 상태로 유지돼야 로그를 표에서 생성할 수 있다.

| 단계 | 상태 | PR | 실측 / 결과 |
|---|---|---|---|
| P7.0 게이트 선행 | `DONE` | #615 | `resolvePlan(skin, theme)` — 플래너 스코프가 **10스킨 × 2테마 = 20조합**으로 확대. 게이트 19 → **21 pass**. 부수 발견 아래 |
| P7.1 사진 배선 | `OPEN` | | |
| P7.1b 셸 계약 소비 | `OPEN` | | |
| P7.2 재질·반경·버튼 | `OPEN` | | |
| P7.3 타이포 위계 | `OPEN` | | |
| P7.4 배치 | `OPEN` | | |
| P7.5 모션 | `OPEN` | | |
| P7.6 코스 카드 히어로 | `OPEN` | | |
| P7.7 파일 분할 | `OPEN` | | |
| P7.8 감성 레이어 + N-9 | `OPEN` | | |
| P7.9 최종 게이트 | `OPEN` | | |

### P7.0 부수 발견 — 두 가지 (둘 다 잰 것)

**① 파일 순서 병합은 플래너에서 틀린 답을 낸다.** 기존 리졸버는 매칭 블록을 **파일 순서로만**
병합했다. 플래너가 스킨을 쓰기 시작하면 그게 깨진다 — `.tr-root[data-tr-skin='sky']` 는 (0,2,0),
`.tr-plan-root` 는 (0,1,0)인데 **플래너 블록이 파일에서 640줄 더 뒤에 있다.** 파일 순서로 병합하면
스킨 쓴 플래너에 대해 **브라우저가 칠하지 않는 팔레트**를 게이트하게 된다.
→ 병합을 **(특이도, 파일 순서)** 순으로 바꿨다. 타이브레이크가 파일 순서인 건 그대로 유지했다 —
그게 다크-스카이 `safe` 버그(스킨 라이트 블록이 `.dark .tr-root` 와 특이도가 같고 뒤에 있어 라이트
값을 유지하던 것)를 잡는 장치다.
⚠ **기존 스코프에는 영향 없음(확인함)**: 스킨보다 뒤에 있는 `.tr-root` 블록 3개가 선언하는 건
`--tr-font-scale` · 지속시간/이징 4종 · `--tr-chip-quiet-fill` 뿐이고 **어떤 스킨도 이걸 안 건드린다.**
지금까지 맞았던 건 운이었고, 이제 운이 아니다.

**② 🔴 플래너는 대비상으로는 풀 스킨을 견딘다 — 20조합 전부 통과.**
섞인 팔레트(스킨의 canvas/surface/ink + 플래너의 accent 계열)는 **아무도 함께 설계한 적이 없는
조합**인데, `PLAN_PAIRS` 17쌍 × 20조합이 전부 기준을 넘었다.
→ 즉 **§N-9 B안(배경만)은 이제 대비 때문에 강제되는 선택이 아니라 디자인 선택이다.**
사장님 지시대로 B안으로 가되, 그 근거는 "A안이 대비를 깬다"가 아니라 "플래너 팔레트가
이 화면의 정체성"이라는 쪽이다. (A안이 깨졌다면 이 표에 실패 목록이 적혔을 것이다.)

⚠ **거짓 초록 방지:** 리졸버가 스킨 셀렉터를 조용히 매칭 못 하면 10스킨 전부 classic 팔레트로
풀려 **10번 통과했다고 보고**한다. 그래서 "섞임 자체"를 고정하는 테스트를 같이 넣었다 —
스킨이 이기는 토큰(canvas)과 플래너가 지키는 토큰(accent-deep)을 양쪽으로 못박는다.
