# 다음 세션 부트스트랩 — 스마트앱 성능 감사 (2026-08-04)

> **이 트랙을 이어받으면 이 문서를 먼저 읽는다.**
> 플랜 정본: `docs/smartapp-performance-audit-plan-2026-08-03.md`
> **원장 정본: `docs/audit/PERF-LEDGER-2026-08.md`** — 발견은 전부 여기 있다.
> 브랜치: `claude/smartapp-perf-audit-2026-08-03` (커밋 17, **PR 미개설**)

---

## §1 한 문단

§F 성능 예산 **10지표 중 9개를 측정**했다(남은 1개는 코드가 아니라 실투어 트래픽을 기다린다).
그 과정에서 수정 3건(스키마·컨시어지·join)과 **상설 게이트 4종**이 붙었고,
관제 54페이지는 **한 페이지 문제**로 좁혀졌다.

---

## §2 🔴 사장님 결정 대기 3건 — 코드보다 이게 먼저다

| # | 결정 | 막혀 있는 것 |
|---|---|---|
| 1 | **Google Cloud 프로젝트 `389319064084` 결제** | Gemini 가 `403 PERMISSION_DENIED · "Lightning dunning decision is deny"` — **미납이다.** 키 문제가 아니라 계정 문제라 **프로덕션도 막혀 있다.** `translate` · `caption` · `vision` · `concierge` **1차 공급자 전부**가 폴백으로 돌고 있다 (Maps 는 다른 프로젝트라 정상) |
| 2 | **Tier1 · 다이닝 MISS 예산 확정** | ①이 풀린 뒤 재측정해야 진짜 값이 나온다. 지금 값(4.6~5.6초 · 9.5초)은 **폴백 경로**라, 그걸로 예산을 쓰면 고장난 상태를 기준선으로 박제한다 |
| 3 | **룸 첫 화면 SSR 전환 여부** | P-01 의 남은 유일한 레버(아래 §4) |

---

## §3 지금 상태 — 무엇이 닫혔나

**측정 (§F 10지표):**
| 지표 | 결과 |
|---|---|
| 손님 앱 JS 전송 | ✅ 205 KB gzip / 예산 350 |
| 컨시어지 Tier0 | ✅ 1.0 µs / 예산 100 ms |
| **룸 진입 4G** | ❌ **2,909 ms** / 예산 2,500 (slow4g 5,553) — **P-01, 열림** |
| 메시지 전송 → 화면 | ✅ warm 16.7 ms / 예산 300 |
| 카드 탭 → 시트 | ⚠ **첫 탭 ~340 ms** / 예산 150 — **룸 세션당 1회**(실측) |
| 컨시어지 Tier1 | ◐ 코드 버그 수정 후 **답이 온다**(500→201, 4.6~5.6초). 예산은 결제 복구 후 |
| 다이닝 HIT | ✅ p50 454 ms / 예산 600 · **외부 API 0회 계약도 카운터로 검증** |
| 다이닝 MISS | ◐ **9,490 ms · 외부 4회** — 예산은 결제 복구 후 |
| 라우트 p50/p95 | ◐ 8/15 실측(`plan` p95 **973 ms** 편차 4배, 원인 미판별) |
| 투어당 LLM/Kakao | ⏸ 계측은 켜져 있고 **실투어 트래픽이 0** |

**수정 3건:** 차량 배정 예약 삭제 불가(트리거) · 컨시어지 빈 응답(추론 토큰 여유분) ·
join 능력 보고가 스냅샷 되받던 것(`capabilityOnly`, **−214 KB**).

**게이트 4종:** 라우트 예산(`qa-perf-throttled --check`) · 유휴(`qa-perf-idle --check`) ·
시더 태그(`__tests__/audit/simSeederTag.test.ts`) · join 계약(join 테스트).

---

## §4 다음에 할 것 — 순서대로

**[1] P-01 (열림, 최우선).** 룸 진입 4G 예산 초과. **번들은 이미 봤고 레버가 없다**(P-16) —
X13 이 마케팅 세금 ~700 KB 를 이미 걷어냈고 남은 622 KB 는 앱 자기 코드 + React 다.
🔴 **남은 레버는 서버 렌더 하나다.** TTFB 는 **7 ms** 인데 `room/[bookingId]/page.tsx` 는
`force-dynamic` 이면서 **서버에서 아무것도 안 읽는다**. 첫 유의미 셸을 SSR 하면 손님이 JS 를 안 기다린다.
⚠ 초대 토큰이 URL 에 있고 `history.replaceState` 로 지우는 구조(§O-1 ③)라 **설계 결정 먼저**(§2 #3).

**[2] `/admin/tour-ops` (P-18).** 관제의 유일한 문제. 199 KB(2위의 3.2배) · TBT 848~1,214 ms(앱 최악) ·
패널 36개 전부 정적 import. UI/UX 트랙의 «accent 0개» 와 같은 화면이라 **같이 잡는 게 맞다**(그쪽 U9).
🔴 **동적 import 를 처방으로 먼저 쓰지 말 것** — 룸에서 해 본 기록이 *"Real, and small"* 이다.
**TBT 프로파일부터**: 848ms 가 파싱인지 실행인지 렌더인지.

**[3] 라우트 p50/p95 완결.** 8/15 만 2xx 였다. `k4-seed` 20방으로 두껍게 시드하면 나머지가 열린다.

**[4] `plan` 라우트 p95 973 ms** (p50 249) — 4배 편차, 원인 미판별.

---

## §5 🔴 이 트랙에서 값을 치른 것들 — 착수 전에 읽어라

**① 오늘의 지배 결함은 앱이 아니라 "0을 초록으로 읽는 것"이었다 (두 트랙 합쳐 6번).**
유휴 요청 0을 "효율적"으로 · 전 라우트 403 을 "40ms 빠름"으로 · 예약 없는 DB 를 "위반 0"으로 ·
가장 잘 아는 결함을 "깨끗함"으로 · 0px 를 "합격"으로 · `grep -P` 부재로 한글 0자를 "이상 없음"으로.
→ **판정기마다 ①양성 대조(심어둔 결함을 잡는가) ②비-공허 단정(몇 개를 셌는가).**

**② 추론을 측정처럼 쓰지 마라 — 이 감사에서 실제로 틀렸다.**
join 이 전송량의 24% 라는 **사실**에서 "그러니 시간의 큰 몫"을 **추론**해 원장에
"P-01 최대 기여자"라고 적었다. 214 KB 를 없앴는데 **첫 유의미가 안 움직였다** —
그 호출은 마운트 이후라 애초에 첫 렌더를 막고 있지 않았다.

**③ 티켓·원장보다 그 자리의 주석을 먼저 읽어라 — 두 번 걸렸다.**
`locale` deps 를 결함으로 적었는데 **FA-025 가 이미 고쳤고** 바로 그 자리 주석이 경위를 적어 뒀다.
"JS 를 줄여보라"고 적었는데 **X13 이 이미 큰 레버를 당겼고** 레이아웃 주석이 그걸 적어 뒀다.

**④ 천장은 그것이 지킬 코드에서 재라.** 리베이스 **이전** 측정으로 만든 예산을 **이후** 코드에
걸었더니 즉시 초과였다 — 회귀가 아니라 **게이트가 자기를 잡은 것**이었다.

**⑤ 조건 없는 숫자는 쓸모없다.** dev vs 프로덕션, 스로틀 유무, 시드 데이터 양이 전부 값을 바꾼다.
룸 진입 시간은 **방 안의 내용물에 비례**하므로(P-12) 시드 고정 전엔 시간으로 회귀를 판정할 수 없다 —
**회귀 감시의 주력은 `jsKB`**(반복측정 0.1 KB 재현).

**⑥ 시뮬은 공유 자원이다.** `--cleanup` 이 `sim_tag` 로 지우던 시절 내가 **타 세션 예약 5건을
같이 지웠다**. 지금은 워크트리별 태그 + `--all` 로 갈라 뒀지만, **시딩 전에 타 세션에 알리는 규약**은 유지.

---

## §6 하니스

| 스크립트 | 무엇 | 게이트 |
|---|---|---|
| `qa-perf-throttled.mjs` | 라우트별 4G/slow4g 타이밍·바이트 | `--check` |
| `qa-perf-idle.mjs` | 유휴 상시 비용(폴링·WebSocket) | `--check` |
| `qa-perf-payload.mjs` | **무엇을** 내려받는가(종류별·상위 응답) | — |
| `qa-perf-interactions.mjs` | 탭→시트, 전송→화면, 콕핏 전환 | — |
| `qa-perf-routes.mjs` | 손님 라우트 p50/p95 + Tier1 | — |
| `qa-perf-dining.mjs` | 다이닝 HIT/MISS | — |
| `qa-bundle-inspect.mjs` | 애널라이저 산출물을 **숫자로** | — |
| `qa-bundle-baseline.mjs` | 무스로틀 바이트 **정본** | — |

**측정 조건(안 맞추면 비교 불가):** 프로덕션 빌드 + `next start` · 390×844 · 콜드 캐시 ·
4G(4Mbps↓/RTT 70ms/CPU 4x) · 3회 중앙값.

```bash
npm run build && npx next start -p 3182
ALLOW_SIM_SEED=1 npx tsx scripts/sim-tour-day.ts && npx tsx scripts/sim-lobby-booking.ts
WALK_BASE=http://localhost:3182 node scripts/qa-perf-throttled.mjs --profile 4g --runs 3 --check
npx tsx scripts/sim-tour-day.ts --cleanup      # 내 태그만. 전체는 --all
```

⚠ **룸 API 인증은 쿠키가 아니라 `x-tour-room-auth` 헤더**(join 응답의 session). 모르면 전 라우트가
403 을 40~60 ms 에 돌려주고 **그게 "빠른 라우트"처럼 보인다.**
