# 다음 세션 부트스트랩 — 스마트앱 / 관제 통합 트랙 (2026-07-28)

> **이 트랙을 이어받으면 이 문서를 먼저 읽는다. 그 다음 §3의 첫 명령을 친다.**
> 상세 근거·진단·설계는 전부 마스터 플랜에 있다:
> **`docs/ops-staff-design-unification-master-plan-2026-07-27.md`** (Part A~L)
>
> 이 문서는 **어디서부터 무엇을 하는가**만 다룬다. 플랜과 어긋나면 **플랜이 정본**이다.

---

## §1 지금 상태 한 문단

관제 통일(O), 콕핏 개편(C), 실기기 리포트(R), 손님 UX 라운드(H)까지 **머지 완료**.
남은 것은 **열린 티켓 43개**이고, 그중 셋은 성격이 다르다 — **P1 버그 2건이 미착수**로
남아 있고(`P0`·`P1`), **100룸 부하에서 앱이 무너지는 경로가 코드에 그대로 있으며**(`K1`),
**OTA 심사 리스크가 리스팅 한 줄로 열려 있다**(`L1`). 지난 두 라운드는 이것들을 열어둔 채
UI 연마를 했다. **이번 세션은 §4 순서를 그대로 따른다.**

배포된 최근 작업: PR #548(칩·힌트·세그먼트) · #550(Now 뱃지·지도 폴백·서랍 빈섹션) ·
#551(설명 팝업 폐지→홈 물리버튼 · 운영센터 전화 · 서랍 축소 · "나" 표시) ·
#552(룸 코드 스플리팅 782→746KB) · #553(플랜 자체 감사 + 지금카드 설계) · #554(부하·OTA 분석).

---

## §2 작업 환경 (여기서 시간을 제일 많이 잃는다)

| 항목 | 값 |
|---|---|
| 워크트리 | 전용 워크트리에서 작업. **메인 디렉터리는 타 세션과 경합한다** |
| `.env.local` | ⚠ **워크트리에 없다.** `cp C:/Users/sangsong/atockorea/.env.local .env.local` 로 복사하고, `NEXT_PUBLIC_TOUR_MODE_V1=1` 이 있는지 확인 (없으면 앱이 "준비 중" 화면만 뜬다) |
| dev 포트 | **3161은 타 세션이 잡고 있을 수 있다.** 남의 워크트리 코드를 찍는 게 실패보다 나쁘다 — 안 잡히면 3171 등 다른 포트를 쓰고 `WALK_BASE` 로 넘긴다 |
| dev 실행 | `npx next dev --webpack -p 3171` — **Turbopack은 이 레포에서 즉사한다** |
| 빌드 | `npm run build` 만. bare `next build` 는 Turbopack과 충돌 |
| 시드 | `ALLOW_SIM_SEED=1 npx tsx scripts/sim-tour-day.ts` → `scripts/.sim-fixtures.json` (`room1Url`·`guideUrl` 등). 끝나면 `--cleanup` |

### 🔴 이 세션에서 실제로 시간을 태운 함정 5가지

1. **워크트리 dev 서버는 HMR이 안 먹는다.** 컴포넌트/CSS를 고치면 **반드시 재시작**해야 한다.
   재시작 안 하고 "before/after가 똑같다"는 결론을 두 번 냈다. 확신이 안 서면
   브라우저에서 `getComputedStyle` 로 **바뀐 클래스가 실제로 적용됐는지 먼저 찍어라**.
2. **shell 인용이 한글·정규식·백틱에서 깨진다.** `node -e "..."` 로 파일을 고치려 하지 말고
   **스크래치패드에 `.js` 파일을 써서 `node file.js` 로 실행**하라. 이 세션에서 4번 깨졌다.
3. **MCP 브라우저 펜은 숨은 탭을 합성하지 못한다** → 스크린샷이 안 나온다.
   **렌더 검증은 Playwright 헤드리스만** 쓴다(`scripts/qa-*-walk.mjs` 패턴).
4. **Next dev 오버레이(`<nextjs-portal>`)가 클릭을 먹는다.** Playwright에서
   `ctx.addInitScript` 로 `nextjs-portal{display:none}` 를 주입한다
   (`addStyleTag` 는 문서 하나에만 붙어서 네비게이션하면 죽는다).
5. **CDP 네트워크 스로틀은 localhost(루프백)에 안 걸린다.** 속도 측정은 CPU 스로틀만
   유효하거나, 프로덕션 도메인을 상대로 재야 한다.

---

## §3 첫 명령

```bash
git fetch origin main && git reset --hard origin/main && npx tsc --noEmit && npx jest __tests__/components/tour-mode __tests__/lib/tour-room
```

기대값: **tsc 0 · 1217 pass**. 여기서 깨지면 그것부터 고치고 시작한다.

그 다음 **§4의 1순위 첫 줄(`K1`)**부터.

---

## §4 단일 우선순위 — 이 순서대로 한다

파트별 순서는 파트 안에서만 유효하다. **파트를 가로지르는 순서는 이것 하나뿐이다.**

### 🔴 1순위 — 지금 손님이 다치거나, 앱이 무너지거나, 심사가 걸리는 것

| 순 | 티켓 | 한 줄 | 왜 여기 있나 |
|---|---|---|---|
| 1 | **K1** | SSE 폴백에 상한(`maxDuration`·백오프·동시 스트림 캡) | 실시간이 **한 번만 흔들려도** 손님 1명당 서버 함수 1개가 영구 상주 + 2초마다 DB 조회. 100룸까지 갈 것도 없다 (Part K §K-3) |
| 2 | **L1** | 리스팅에 "투어 중 옵션·연장은 현장 현금" 한 줄 | 앱이 OTA 손님에게도 현금 청구를 만드는데 리스팅에 그 말이 없다. **코드 아니고 문구** (Part L) |
| 3 | **P0** | 코스 선택지가 카드로 안 뜸 | **P1 버그, 미착수.** 전세 상품 2종에서 손님이 코스를 못 고른다 (Part D) |
| 4 | **P1** | 플래너 권한 잠금 + "이 기기에서 편집하기" | **P1 버그, 미착수.** 혼자 여행하는 손님이 자기 예약에서 영구히 잠길 수 있다 (Part D) |
| 5 | **X1** | CJK 줄바꿈 불변 규칙 위반 전수 | 스캐너 사각지대부터 고치고 분류. 후보 222건/70파일 (Part G, U-D17) |

### 🟠 2순위 — 손님이 매일 보는 화면 (Part I, 순서 고정)

`I1`(nowCard 리졸버 + 경계 테스트) → `I2`(NowCard + 홈 재구성, **구 X5·X14 흡수**) →
`I3`(전이 모션) → `I4`(관제 홈, **구 X6 흡수**) → `I5`(게이트) → `I6`(사장님 승인)

> **I1이 UI보다 먼저인 이유:** 순수 함수라 화면 없이 일곱 상태를 전부 검증할 수 있다.
> 이 앱에서 UI 버그가 잡힌 곳은 늘 스크린샷이었지 코드 읽기가 아니었다.
> **R7 리스크:** 홈은 손님이 매일 처음 보는 화면 → I2는 단독 PR, 전후 컷 필수,
> `더 보기` 안에서 **기존 목적지 전부가 여전히 닿는지** 테스트로 고정(U-D25).

### 🟡 3순위 — 속도

`K2`(요금제 상한 확인 — **K1의 숫자를 정하려면 이게 필요**) → `X13`(라우트 그룹 분리)
→ `K3`(프레즌스·타이핑 볼륨 다이어트) → `K5`(번역 동시성 상한)

> **X13 ⚠:** 모든 라우트 파일이 움직인다 = 레포에서 충돌이 가장 큰 변경(§E R4).
> **단독 세션·단독 PR.** 실측 상한은 셸 바닥 423KB이지 700KB가 아니다(§I-0 ②).

### 🟢 4순위 — 이상적 앱 갭

`X15`(도착 자동화 — ⚠ **가이드/기사 기기로 트리거**, 손님 위치공유는 기본 OFF라 못 쓴다) ·
`X17`(Timeline 공유) · `X18`(매트릭스 환류) · `X16`(⚠ 30일 퍼지를 뒤집는 **정책 변경**)

### ⚪ 5순위 — 위생·커버리지·잔여

`X7` `X8` `X9` `X10` `X11` `X12` `X19` · `K4`(실측 부하 — 승인 필요) ·
`L2` · `O6` · `T0`~`T5` · `N1` `N2` · `P2`~`P6` · `R6`

---

## §5 사장님 결정 대기 (코드 작업 아님 — 물어보고 넘어간다)

| # | 무엇 | 안 정하면 |
|---|---|---|
| 1 | **U-D20v2** 채팅 하단 시그널줄+퀵리플줄 통합 여부 | 두 줄로 유지. 합치면 탭 결과가 달라지는 게 가려진다 |
| 2 | **I6** `더 보기` 기본 접힘/펼침 | I2를 접힘으로 만들고 승인 때 뒤집기 |
| 3 | **X16** 투어 간 손님 기억 = 30일 퍼지 폐기 | 안 함(프라이버시 유지) |
| 4 | **L3** OTA 예약에 연장/추가 기능 유지 여부 | 유지 + L1 고지 (권장) |
| 5 | **K4** 실제 100룸 부하 테스트 실행 | 안 함(실 LLM 비용 + 실 DB 행) |
| 6 | `claude/ai-agent-booking-channel` 머지 여부 | 안 함(제3자 쓰기 경로) |
| 7 | 🔴 **8/17 예약 `d8e12b1d`** — `pending` + 카드 미저장 | 재승인 크론이 영영 안 집는다. **운영 확인 필요** |

---

## §6 PR 내기 전 게이트 (전부 통과해야 머지)

```bash
npx tsc --noEmit
npx jest __tests__/components/tour-mode __tests__/lib/tour-room
npx jest skinContrast typeDiscipline shellStackingContext
npm run build
```

UI를 건드렸으면 **여기에 렌더 증거를 더한다** — Playwright 전후 컷.
컷 없이 "고쳤다"고 하지 않는다. 이 세션에서 코드는 맞고 화면은 틀린 경우가 세 번 있었다.

**하니스:** `qa-smartapp-walk.mjs`(손님) · `qa-cockpit-walk.mjs`(콕핏) · `qa-ops-walk.mjs`(관제) ·
`qa-cjk-scan.mjs` · `qa-coverage-audit.mjs` · `qa-unfilled-props.mjs` ·
`qa-prod-tour-room.ts` / `qa-prod-surfaces.ts`(프로덕션)

**커밋 규칙:** 푸터는 `Co-Authored-By: Claude <noreply@anthropic.com>` 만 —
**모델 식별자 금지**. 진행 보고는 한국어, 코드·커밋은 영어.
⚠ 공유 워크트리에서는 **`git add -A` 금지, 경로 명시**(외부 커밋이 진행 중 파일을 쓸어간 사례 있음).

---

## §7 티켓 전량 (43 열림 / 24 닫힘)

| 상태 | 티켓 |
|---|---|
| ✅ 완료 | `O0` `O1` `O2` `O3` `O4` `O5` · `G1` · `R1` `R2` `R3` `R4` `R5` `R7` `R8` · `C1`~`C7` · `X3` `X4` · Part H(H-a~H-g) |
| ⊘ 기각 | `X2`(측정 후 — 손님 헤더 제목은 잘려도 투어를 식별시킨다) |
| ⇄ 흡수됨 | `X5`→`I2` · `X6`→`I4` · `X14`→`I2` |
| ⬜ 열림 43 | `K1` `K2` `K3` `K4` `K5` · `L1` `L2` `L3` · `I1`~`I6` · `X1` `X7` `X8` `X9` `X10` `X11` `X12` `X13` `X15` `X16` `X17` `X18` `X19` · `P0`~`P6` · `N1` `N2` · `O6` · `T0`~`T5` · `R6` |

각 티켓의 근거·진단·설계는 마스터 플랜에서 파트 문자로 찾는다:
`O`/`G`=Part A·C·G(관제) · `T`=Part B(기사·가이드) · `R`=Part C(실기기) ·
`P`=Part D(플래너) · `N`=Part E(운영 공백) · `C`=Part F(콕핏) · `X`=Part G·H ·
`I`=Part I(지금카드) · `K`=Part K(부하) · `L`=Part L(OTA 고지)

---

## §8 이 트랙에서 반복해서 나온 결함 유형 (다음 사람이 같은 걸 또 하지 않도록)

1. **"안 채운 인자"** — prop을 정의해놓고 안 넘긴다. `currentIndex`가 리치 카드에 안 갔고,
   `guest_submitted`가 CHECK에 없었고, 운영센터 전화 env가 세 곳 다 비어 있었다.
   → 도구: `scripts/qa-unfilled-props.mjs`
2. **레포에 있다 ≠ 적용됐다** — 마이그레이션 파일이 있어도 라이브에 안 걸려 있을 수 있다.
   **객체 존재로 검증**하라(파일명 매칭은 스크리닝일 뿐).
3. **같은 목록의 두 번째 사본은 반드시 어긋난다** — 로케일 맵을 손으로 베끼지 말고
   기존 것에서 **파생**시켜라(`NOW_LABEL` 이 그 패턴).
4. **문서가 코드보다 오래 산다** — 티켓을 닫을 때 **본문과 실행 로그를 같이** 고쳐야 한다.
   이 실수를 두 라운드 연속 했다. → `X19`(로그를 표에서 생성)
5. **추론을 측정처럼 쓰지 말 것** — "700KB가 셸"이라고 적었다가 실측에서 423KB로 반증됐다.
   숫자를 쓸 거면 **잰 것인지 계산한 것인지 문장에 표시**하라.
