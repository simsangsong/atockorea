# Resend 子域名邮件接收设置指南

## 📋 概述

使用子域名 `mail.atockorea.com` 接收邮件可以：
- ✅ 避免覆盖根域名的现有邮件设置
- ✅ 提高邮件送达率
- ✅ 更好的邮件管理

**⚠️ 注意：** 如果 Resend 添加子域名需要收费，可以直接使用根域名 `atockorea.com` 接收邮件。详见 `docs/RESEND_ROOT_DOMAIN_SETUP.md`

---

## 🎯 设置步骤

### 步骤 1：在 Resend 中添加子域名

1. **登录 Resend Dashboard**
   - 访问 https://resend.com
   - 登录你的账户

2. **添加新域名**
   - 左侧菜单 → **Domains**
   - 点击 **"Add Domain"**
   - 输入：`mail.atockorea.com`
   - 点击 **"Add"**

3. **查看 DNS 记录要求**
   - Resend 会显示需要添加的 DNS 记录
   - 记录会针对 `mail.atockorea.com` 子域名

---

### 步骤 2：在 Namecheap 中添加子域名的 DNS 记录

#### 2.1 添加 A 记录（如果还没有）

子域名需要先有 A 记录指向服务器，或者使用 CNAME。

**方法 A：添加 A 记录**
- **Type:** `A Record`
- **Host:** `mail`
- **Value:** 你的服务器 IP 地址（或使用 CNAME）

**方法 B：使用 CNAME（推荐）**
- **Type:** `CNAME Record`
- **Host:** `mail`
- **Value:** `atockorea.com`（或你的主域名）

#### 2.2 添加 Enable Receiving MX 记录

1. **进入 Advanced DNS**
   - Namecheap → Domain List → `atockorea.com` → Manage → Advanced DNS

2. **使用 Custom MX 选项**
   - 在 Mail Settings 部分，选择 **"Custom MX"**

3. **添加 MX 记录**
   - **Host:** `mail`（注意：不是 `@`）
   - **Value:** 从 Resend Dashboard 复制（针对 `mail.atockorea.com` 的 MX 值）
   - **Priority:** `9`（或 Resend 提供的值）
   - **TTL:** Automatic

4. **保存**
   - 点击 **"SAVE ALL CHANGES"**

---

### 步骤 3：在 Resend 中配置邮件接收

#### 3.1 启用邮件接收

1. **进入域名设置**
   - Resend Dashboard → Domains → `mail.atockorea.com`

2. **启用 Enable Receiving**
   - 找到 **"Enable Receiving"** 部分
   - 打开开关（Toggle ON）

3. **查看 MX 记录要求**
   - Resend 会显示需要添加的 MX 记录
   - 确认 Host 是 `mail`（不是 `@`）

#### 3.2 验证 DNS 记录

1. **等待 DNS 传播**
   - 通常 5-30 分钟

2. **验证记录**
   - 在 Resend Dashboard 点击 **"Verify DNS Records"**
   - 确认所有记录状态为 **"Verified"**

---

### 步骤 4：配置邮件地址

#### 方案 A：直接使用子域名邮箱

**邮件地址格式：**
- `support@mail.atockorea.com`
- `info@mail.atockorea.com`
- 等等

**优点：**
- 简单直接
- 邮件直接发送到子域名

**缺点：**
- 邮箱地址较长

#### 方案 B：使用邮箱转发（推荐）

**设置邮件转发：**
- 接收：`support@mail.atockorea.com`
- 转发到：`support@atockorea.com`（或其他邮箱）

**在 Resend 中配置：**
1. 进入 `mail.atockorea.com` 域名设置
2. 找到 **"Email Forwarding"** 或 **"Aliases"** 选项
3. 添加转发规则：
   - `support@mail.atockorea.com` → `support@atockorea.com`

**优点：**
- 可以使用较短的邮箱地址
- 邮件统一管理

---

## 📊 DNS 记录对比

### 使用根域名（`@`）

| 记录 | Host | 用途 |
|------|------|------|
| Enable Receiving MX | `@` | 接收 `@atockorea.com` 的邮件 |

**影响：**
- ⚠️ 会覆盖现有的邮件接收设置
- ⚠️ 所有发送到 `@atockorea.com` 的邮件都会路由到 Resend

### 使用子域名（`mail`）

| 记录 | Host | 用途 |
|------|------|------|
| Enable Receiving MX | `mail` | 接收 `@mail.atockorea.com` 的邮件 |

**优点：**
- ✅ 不影响根域名的邮件设置
- ✅ 可以同时使用其他邮件服务
- ✅ 更好的隔离和管理

---

## 🔧 完整设置流程

### 1. 在 Resend 中添加子域名

```
Resend Dashboard
  ↓
Domains
  ↓
Add Domain
  ↓
输入: mail.atockorea.com
  ↓
查看 DNS 记录要求
```

### 2. 在 Namecheap 中添加 DNS 记录

```
Namecheap Advanced DNS
  ↓
添加 A 记录或 CNAME（如果还没有）
  Host: mail
  ↓
Custom MX
  ↓
添加 MX 记录
  Host: mail
  Value: inbound-smtp.ap-northe...
  Priority: 9
  ↓
保存
```

### 3. 验证和配置

```
等待 DNS 传播（5-30 分钟）
  ↓
在 Resend Dashboard 验证记录
  ↓
启用 Enable Receiving
  ↓
配置邮件转发（可选）
```

---

## 📋 需要添加的所有记录（子域名）

### 对于 `mail.atockorea.com` 子域名：

1. **Domain Verification (DKIM) - TXT 记录**
   - **Host:** `resend._domainkey.mail`（或 Resend 提供的值）
   - **Type:** `TXT Record`
   - **Value:** 从 Resend Dashboard 复制

2. **Enable Sending SPF - TXT 记录**
   - **Host:** `send.mail`（或 Resend 提供的值）
   - **Type:** `TXT Record`
   - **Value:** 从 Resend Dashboard 复制

3. **Enable Receiving - MX 记录** ✅
   - **Host:** `mail`
   - **Type:** `MX Record`
   - **Value:** `inbound-smtp.ap-northeast-1.amazonaws.com`
   - **Priority:** `9`

---

## ⚠️ 重要提示

### 1. Host 值不同

- **根域名 MX:** Host = `@`
- **子域名 MX:** Host = `mail`

因为 Host 值不同，可以同时存在。

### 2. 邮件地址格式

使用子域名后：
- ✅ 可以接收：`support@mail.atockorea.com`
- ❌ 不能直接接收：`support@atockorea.com`（除非配置转发）

### 3. 邮件转发配置

如果需要使用 `support@atockorea.com` 格式：
- 在 Resend 中配置邮件转发
- 或使用其他邮件服务处理根域名邮件

---

## ✅ 完成检查清单

- [ ] 在 Resend 中添加了 `mail.atockorea.com` 域名
- [ ] 在 Namecheap 中添加了子域名的 A 记录或 CNAME
- [ ] 在 Namecheap 中添加了子域名的 MX 记录（Host: `mail`）
- [ ] 在 Resend 中启用了 Enable Receiving
- [ ] 等待 DNS 传播（5-30 分钟）
- [ ] 在 Resend Dashboard 验证所有记录
- [ ] 配置邮件转发（如果需要）
- [ ] 测试发送邮件到 `support@mail.atockorea.com`

---

## 🎯 推荐配置

### 最佳实践：

1. **使用子域名接收邮件**
   - `mail.atockorea.com` 用于接收邮件
   - 避免影响根域名

2. **配置邮件转发**
   - `support@mail.atockorea.com` → `support@atockorea.com`
   - 统一管理邮件

3. **保留根域名灵活性**
   - 根域名可以用于其他用途
   - 不影响现有邮件服务

---

## 🆘 常见问题

### Q: 子域名和根域名可以同时使用吗？

**A:** 可以！它们使用不同的 Host 值：
- 根域名：Host = `@`
- 子域名：Host = `mail`

### Q: 如何让 `support@atockorea.com` 也能接收邮件？

**A:** 两种方案：
1. **添加根域名的 MX 记录**（Host: `@`）
2. **配置邮件转发**：`support@mail.atockorea.com` → `support@atockorea.com`

### Q: 子域名需要单独验证吗？

**A:** 是的，在 Resend 中需要单独添加和验证子域名。

---

祝你设置顺利！🎉

