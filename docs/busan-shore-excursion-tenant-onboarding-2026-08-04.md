# 부산 기항지 상품 3종 — 채널 리스팅 정합 (2026-08-04, 사장님 지시)

## 0. 이 문서를 이어받으면

**사장님 지시:** *"tenant 이름은 밝히지 말고, 지금 존재하는 상품을 위 정보로 100프로 맞춰"* ·
*"픽업은 북항 혹은 영도항 대중이 없어 둘 다 적어야 돼"* · *"가격 무조건 똑같이"*

🔴 **운영사(테넌트) 이름은 어떤 소비자 표면에도 쓰지 않는다.** 채널이 나중에 사이트를 리뷰할 때
우리를 경쟁자로 인식하면 파트너십에 영향이 가는 걸 막기 위한 결정이다. 이 문서에도 이름을 적지 않는다.
**신규 SKU를 만들지 않는다** — 기존 3종을 리스팅에 맞춘다.

## 1. 매핑 — 1:1로 떨어진다

| 리스팅 패키지 | 우리 슬러그 | 이전 | 이후 |
|---|---|---|---|
| 조인 (인원에 따라 밴 또는 투어버스) | `busan-cruise-shore-excursion-bus-tour` | $49 · **차단** | **$58.79** · 노출 |
| 소그룹 (최대 12명) | `busan-small-group-sightseeing-tour-cruise-passengers` | $84 / $79 | **$68.95** |
| VIP 프라이빗 | `busan-private-car-charter-cruise-shore` | $364/차량 | **$456.99** |

리스팅 공통: 8시간 · 영어/중국어 가이드 · 크루즈 터미널 픽업/드롭 · 배 출항시각에 맞춘 귀항 ·
만 4세 이상 · 24시간 전 무료취소 · 크루즈 승객 전용(비크루즈 예약 거부).

**코스(조인 2종 공통, 9스톱+점심):** 해동용궁사 → UN기념공원 → 점심(자비) → 자갈치시장 →
BIFF광장 → 국제시장 → 감천문화마을 → 송도 구름산책로 → 송도 스카이파크(케이블카 현장 선택) → 용두산공원

**포함:** 입장료(조인 한정) · 가이드 · 왕복 이동 · 통행료 · 주차 · 유류 · 발권수수료
**불포함:** 식사 · 개인경비 · 팁 · 보험 · 부산타워 전망대(현장) · 스카이캡슐&해변열차(현장)
**프라이빗은 전 스팟 입장료 제외** + 시간외 ₩50,000/시간(07:00 이전 시작·20:00 이후 종료) + 부산 외 +₩130,000
**조인 할증:** 08:00 이전 픽업 / 20:00 이후 종료 시 ₩10,000/인

## 2. 이번에 끝낸 것

### 가격 — 센트까지 정확히
`parseListPriceUsd`가 **두 레지스트리 모두에서 `Math.round`로 달러 반올림**을 하고 있어
$58.79가 카드에서 $59로 찍혔다. 센트 반올림(`Math.round(n*100)/100`)으로 바꿨다.
정수 가격 상품은 영향 없다(49 → 49). `formatPrice`는 이미 소수부가 있을 때만 센트를 렌더한다.

**두 SLUG_OVERRIDES 맵이 어긋나 있던 것도 이 참에 맞췄다** — 소그룹이 린 84 / 무거운 79+취소선 85로
갈라져 있었다. 이제 3종 모두 두 맵에 동일하게 들어간다.

🔴 **취소선(정가)은 넣지 않았다.** 리스팅의 "30% off"는 채널 프로모 배지이고 원가가 화면에 없다.
없는 숫자를 지어내는 대신 정가를 비웠다. **정가를 표시하려면 사장님이 값을 주셔야 한다.**

### 픽업 — 두 터미널을 동등하게
우리 3종 모두 **이미 두 터미널을 갖고 있었다.** 문제는 서열이었다 — 영도를 "primary / 대형선용",
초량을 "alternate / secondary / 소형선·페리용"으로 적어 기본값이 있는 것처럼 읽혔다.
사장님 말씀대로 정해진 게 없으므로 **양쪽에 같은 문장**을 넣었다:
*"부산 기항 크루즈는 두 터미널 중 어느 쪽에도 접안합니다. 어느 터미널을 이용할지는 배가 접안하는
곳에 따라 정해지며 예약 후 확정됩니다."* (6로케일)

소그룹 상품은 픽업 아코디언이 **초량 하나만** 적어 놨었다(`preview` + `content[0]`). 두 자리 모두 교체.
「픽업」 글랜스 칩도 단일 터미널 값이었던 걸 "두 곳 중 접안지 — 예약 시 확정"으로 바꿨다.

### 기간 · 정원
- 조인 2종 **9시간 → 8시간** (카드·히어로·글랜스·`matching_profile` 6로케일)
- 소그룹 정원이 **네 값**이었다(레지스트리 8 / 본문 "약 10명" / 한국어 한 곳 "약 14명" / 비교 FAQ "약 12명").
  리스팅의 **하드 12명**으로 통일하고 "약/≈" 헤지도 뺐다.

### 조인 티어 차단 해제
`busan-cruise-shore-excursion-bus-tour`는 **2026-06-29 Klook 12 SKU 축소**로 꺼져 있었다.
3티어를 다 맞추려면 팔려야 하므로 블록리스트에서 제거했다. **DB 쪽 `is_active`/`is_published`
복구가 같이 가야 한다** — §3 SQL에 들어 있다.

### 운영사명 유출 제거
🔴 라이브 번들에 운영사 브랜드가 박혀 있었다 — `busan-outskirts-tongdosa-…`의 픽업 지점명이
**6로케일 전부** `"서면역 4번 출구 (LoveKorea 서면)"` 형태였다. 브랜드 괄호를 제거했다(역+출구면 충분).
소비자 표면(`components/` · `messages/` · `app/` · `lib/`) 잔여 **0**.
남은 히트는 ops 이메일 파서 테스트 픽스처와 캐시 키 상수뿐 — 내부 전용, 노출 아님.

## 2-b. 2차 세션 (2026-08-04 오후) — S1·S2·S3 완료 + DB 양쪽 적용 완료

**DB 적용 끝.** `applied/2026-08-04-10-…`(가격·가시성) + `applied/2026-08-04-11-…`(코스·포함·정책).
11은 2MB라 MCP 한 번에 안 들어가서 **같은 원천(번들)을 읽는 전용 적용기**
`scripts/apply-busan-cruise-course-2026-08.mjs` 로 넣었다(psql 있으면 .sql 이 정본).
세 슬러그 `import-match-v18 --single` 재동기화까지 완료.

**S1 코스** — 조인 2종이 리스팅의 9스톱을 각각 세운다:
용궁사 → 유엔기념공원 → 점심(자비) → 자갈치 → BIFF → 국제시장 → 감천 →
**송도 구름산책로** → **송도 스카이파크(케이블카 현장 선택)** → 용두산.
버스 상품은 송도 1스톱을 2스톱으로 쪼갰고, 소그룹은 8→12엔트리로 재편했다.
**S2 포함/불포함**·**S3 할증·취소·연령·크루즈 전용**은 3종 전부 적용.

🔴 **8시간을 진짜로 만들자 네 개의 주장이 거짓이 됐다.** 카드는 8시간인데 일정표는
08:30→19:15(약 11시간)이었다. 09:00→17:00 스파인으로 다시 짜니 **감천 「골든아워 16:00」**,
**용두산 「황혼 18:30」**, **자갈치 「점심·저녁 인파 사이 14:00」**, 픽업의 **「소그룹과 같은 여덟 스톱」**이
전부 틀렸다. **숫자만 바꿨으면 골든아워가 14:20에 붙었을 것이다** — 문구를 6로케일에서 다시 썼다.

🔴 **실렌더가 세 번 이겼다.** ① `TOUR_PRODUCT_USE_SUPABASE=1` 이라 **번들만 고치면 화면은 안 바뀐다**
(경주와 같은 함정 — 그래서 11 SQL 이 있다) ② 첫 렌더에서 한국어 코스가 깨져 나왔다 →
🔴 **소그룹 비영어 5로케일의 스톱 이름이 자기 `_poi_meta.poi_key` 와 어긋나 있었다.**
`haedong_yonggungsa` 인데 이름은 「유엔기념공원」, `un_memorial_cemetery` 인데 「태종대」…
**한국어 페이지가 영어와 다른 옛 코스를 팔고 있었다.** poi_key 순서는 6로케일 동일해서
소스로는 안 보였다. → 소그룹은 **전 스톱을 도너(버스 상품)에서** 가져오게 바꿨고,
생성기에 `assertNamesMatchKeys` 가드를 붙였다. ③ 그 뒤 렌더로 재확인.

**같이 드러난 손님 대면 오류(전부 수정):**
- 🔴 소그룹 **비영어 5로케일이 「점심 포함, 총 USD $58.50」** — 영어만 「lunch paid directly」였다.
  리스팅은 점심 **불포함**이고 가격도 옛것. 번역 시점에 뒤집힌 채 팔리고 있었다.
- 조인 버스 상품 **CJK 4로케일 + es 의 스톱 `description` 5개가 영어 그대로**였다.
  이번 패스가 건드리는 것(픽업·귀환·송도→신규 2스톱)과 BIFF·국제시장은 현지어로 새로 썼다.
- FAQ 「케이블카는 30분 정차로 못 타니 프라이빗을 사라」 — 스카이파크가 코스에 들어와 **거짓이 됨**.
- 카드·히어로·FAQ의 **「8개 코스」**, 그리고 자기 **옛 가격($49/$79/$58.50)** 이 본문에 남아 있던 것.
- 렌더 안 되는 필드의 리터럴 `**` — 도너 스톱이 갖고 있어 그대로 옮기면 +11 이었다.
  옮기는 대신 떼어냈다: 번들 전체 bold 래칫 **1533 → 1474**.

**게이트:** tsc **0** · jest **5,943 pass / 0 fail**(562 스위트) · `__tests__/audit` **327 pass** ·
두 변환기 모두 **멱등**(재실행 0파일) · 운영사명 소비자 표면 **0**.

## 3. DB 적용 완료 (2026-08-04)

`applied/2026-08-04-10-busan-cruise-listing-alignment.sql` — 가격 58.79/68.95/456.99 ·
`amount_minor` 5879/6895/45699 · `detail_payload` 가격 · `catalog_card.priceLabel` 비우기 ·
조인 티어 `is_active`/`is_published` 복구. **적용 완료.**

`applied/2026-08-04-11-busan-cruise-course.sql` — 코스·포함/불포함·정책 6로케일 `detail_payload`
+ `tours` 사본. **적용 완료** (2MB 라 `scripts/apply-busan-cruise-course-2026-08.mjs` 로 넣음).
세 슬러그 `import-match-v18 --single` 재동기화 완료.

🔴 **번호 주의:** 원래 07로 만들었는데 `applied/2026-08-04-07-gyeongju-recourse.sql` 과 충돌해
**10으로 개번**했다. 새 파일은 12부터.

## 2-c. 3차 세션 (2026-08-05) — R1(태종대 잔재) · R2(프라이빗 모순) 완료

**R1 — 소그룹 태종대 잔재 21건(EN) / 30건(비영어) → 0.**
코스 의존 산문은 **조인 버스 상품에서 도너**했다(두 티어가 이제 같은 코스라 가능하다):
보행 아코디언 · `seasonalVariations` · `practicalWeatherStatic` · FAQ 4종(보행·아이·시니어·날씨).
`q-taejongdae-train`("태종대에서 다누비 열차를 타야 하나요?")은 **`q-cable-car` 로 교체**했다 —
가지도 않는 곳을 묻는 FAQ였다. 소그룹 고유 문구(SEO·키워드·카드 블러브·루트 로직·정원 문구·
접근성 아코디언)만 6로케일로 새로 썼다.
🔴 접근성 아코디언이 **「만 8세 이상」**이라 §2-b 에서 넣은 정책(**만 4세 이상**)과 충돌했다 — 같이 고쳤다.

🔴 **갤러리 캡션도 스톱 이름과 똑같이 어긋나 있었다.** EN 은 0건인데 비영어는 10~17건 —
`un-memorial-cemetery` 사진에 「태종대 해안 절벽」, 용궁사 사진에 「유엔기념공원」 캡션이 붙어 있었다.
캡션을 **사진에서 파생**하도록 바꿨다(옮기지 않는다). 매칭 앵커(`poi_tags`/`anchor_poi_keys`)도
코스에서 파생 — 둘 다 `taejongdae` 를 들고 있어 추천엔진이 태종대 질의에 이 상품을 물어 왔다.

**R2 — 프라이빗 픽업 모순.** 번들 자신의 `pickup_dropoff.notes` 가 「호텔 픽업은 이 SKU 에서
제공하지 않음」이라고 못박아 두었는데 SEO·포함 불릿·귀환 단계·`mp.pickup_base` 가 여전히
호텔/KTX 픽업을 팔고 있었다. 전부 크루즈 터미널로 정리. 귀환 단계는 같은 객체 안에서
`detail` 90분 / `description` 60분이라 60분으로 통일. 「English-speaking」→ 영·중.
🔴 **비영어 5로케일은 집합지가 3곳**(터미널+KTX역+시내 호텔 아무 곳)이었다 — 산문이 아니라
**구조로** 호텔 픽업을 팔고 있었다. 호텔·KTX 지점을 걷어내고, 사장님 지시대로 **두 터미널을 복원**했다
(걷어내고 나니 비영어는 터미널이 하나뿐이었다).

🔴 **실렌더가 번들에 없는 결함을 하나 더 잡았다** — `_shared/privateSampleItinerary.ts` 의
「샘플 일정」 섹션. 크루즈 기항 상품인데 **「호텔 픽업 (기본)」**이 뜨고, 슬롯 5개가
**「방문지 추후 추가 예정」**이라는 자리표시자 그대로 손님에게 나갔으며, 「경주 포함 시 **70,000원**」이
같은 페이지의 정책(**부산 외 130,000원**)과 충돌했다. 크루즈 터미널 픽업/하차로 바꾸고,
슬롯을 **레포 안의 실제 코스**(이 상품 번들 + 경주 당일투어 번들)로 채우고, 130,000원으로 통일.
게이트 추가: 호텔·자리표시자 금지 테스트.

**게이트:** tsc **0** · jest **5,944 pass / 0 fail** · `__tests__/audit` **327 pass** ·
변환기 멱등 · 운영사명 소비자 표면 **0** · DB 적용(`applied/2026-08-05-12-…`)·재동기화 완료.

## 4. 아직 안 맞춘 것 (남은 일)

**S1·S2·S3·R1·R2 는 끝났다.** 남은 것:

1. 🔴 **프라이빗은 EN 과 비영어가 서로 다른 상품이다** (이번에 손대지 않음).
   EN 스톱: 픽업 → 「가이드와 코스 상의」 → 「동해안 옵션」 → 「역사·마을·시내 옵션」 →
   **「부산항 귀환」(중간에!)** → 용두산 → 남포동/자갈치 → 귀환. 즉 EN 은 옵션 선택형이고
   **자기 목록 한가운데 귀환이 한 번 더 들어 있다.**
   비영어는 구체적 6스톱 고정 코스(유엔→태종대→점심→감천→용두산→남포동).
   아코디언 스키마도 다르다(EN `{preview, content[]}` vs 비영어 `{icon, details[]}`).
   **이건 「무엇을 파는 상품인가」 결정이 필요하다** — 고정 코스인가 선택형인가.
2. **자매 상품에 같은 결함** — `incheon-seoul-private-car-shore-excursion-cruise` 도
   크루즈 기항 상품인데 `seoulConfig()`(호텔 픽업 기본 + 자리표시자 슬롯)를 쓴다.
   같은 종류지만 다른 트랙이라 건드리지 않았다. 위치: `_shared/privateSampleItinerary.ts:271`.
3. **번들 전체 미번역/번역잔해 트랙** — 조인 버스 상품에서 CJK+es 의 스톱 `description` 이
   영어 그대로인 걸 5개 찾았고 이번에 건드리는 것만 고쳤다. 나머지(자갈치 등)와
   `**注：**` 꼴 번역기 메타노트는 별도 트랙(bold 래칫 1474 가 그 원장이다).

⚠ 1·2·3 은 이번 변경으로 생긴 게 아니라 **원래 있던 것**이다.
⚠ **프라이빗의 태종대 52건은 결함이 아니다 — 실제 정차다. 건드리지 말 것.**

## 4-b. 🔴 사장님 결정 2건 (진행 안 함)

1. **취소선(정가)** — 리스팅의 "30% off" 는 채널 프로모 배지고 원가가 화면에 없다.
   지어내지 않고 비워 뒀다. 표시하려면 값을 주셔야 한다.
2. **프라이빗 요금 티어** — 번들의 8시간 1–6인/7–9인/10–13인 티어가 헤드라인 $456.99 와
   안 맞는다. 티어를 지울지, 리스팅에 맞출지.

## 5. 게이트 (이번 변경 후 실측)

`npx tsc --noEmit` **0** · `npx jest` **5,926 pass / 0 fail** (559 스위트, 21 skip) ·
상설 게이트 `__tests__/audit` **35 스위트 310 tests 전부 green**.
정합 스크립트 `scripts/align-busan-cruise-2026-08.mjs`는 **멱등** — 재실행 시 diff 0 확인.

## 6. 조사 근거

- 노출 판정: `lib/tour-consumer-visibility.ts`
- 가격 이중 출처: `components/product-tour-static/catalog/staticTourProductRegistry.ts` ·
  `catalogRegistrationBuilder.ts` (둘 다 `parseListPriceUsd`)
- 12 SKU 축소: `supabase/migrations/20260629000000_klook_prep_activate_12_tours.sql` ·
  `20260629001000_klook_prep_price_plus5_remove_discount.sql`
- 상품 본문: `components/product-tour-static/<slug>/<slug>.<locale>.json`

### 참고 — 레포에 테넌트 상품 개념은 없다
`merchants` 테이블 · 업체 포털 · 정산 RPC · `tours.merchant_id`는 **살아 있지만** 레거시
`/tour/[id]` 경로에 묶여 있고 상품 CRUD는 TODO 스텁이다. 지금 모든 실상품이 사는 정적 번들
파이프라인에는 운영사 필드가 **없다**. 반면 약관·마케팅 카피는 이미 마켓플레이스로 선언돼 있다
("독립적인 제3자 투어 제공자가 투어를 제공"). **법무 레이어가 스키마보다 앞서 있다.**
이번 지시는 "이름을 밝히지 말라"이므로 **운영사 필드를 만들지 않는 쪽이 지시와 정합**한다.
