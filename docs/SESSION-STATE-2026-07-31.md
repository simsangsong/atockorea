# 세션 기록 — 2026-07-31 (P7 플래너 전면 재편, 야간 6회차)

> 앞 세션: `docs/SESSION-STATE-2026-07-30.md`
> 정본 플랜: `docs/planner-overhaul-p7-master-plan-2026-07-30.md` (§F 실행 로그가 이 세션의 결과)

## 한 줄

**P7 전 단계(P7.0 ~ P7.9) 완주 · PR 11건 머지(#615~#625).** 플래너가 "사진 0장짜리 폼"에서
"내 하루가 주인공인 화면"이 됐다. 그 과정에서 **게이트가 스스로 조용해질 뻔한 사고를 두 번** 잡았다.

## 무엇이 끝났나

| PR | 단계 | 실측 |
|---|---|---|
| #615 | P7.0 게이트 선행 | 플래너 스코프 **10스킨 × 2테마**로 확대. 게이트 19 → 21 |
| #616 | **P7.1 사진 배선** | 피커 썸네일 **0장 → 25장** · 커버리지 **8.9% → 60.5%** · 죽은 URL 22개 정리 |
| #617 | **P7.1b 셸 계약** | 글자 크기 설정이 **처음으로 도달**(1 → 1.35, 렌더 픽셀로 확인) |
| #618 | P7.2 재질·반경·버튼 | 커밋 버튼 **36 → 56px**(보조 44px 위로) · 다크 승강 **NONE → 있음** · 반경 7종 → 4종 |
| #619 | P7.3 타이포 | 「내 하루」 15 → **18px** · 스톱 이름 13 → **15px** · 행 높이 **+0.75px** |
| #620 | P7.4 배치 | 「내 하루」를 **탭·검색·목록 위로** · 화면 유일 hero 카드 |
| #621 | P7.5 모션 | 160ms·생 `active:scale-95` 제거 · 눌림 0.995 → **0.98** · 신규 키프레임 **0** |
| #622 | P7.6 코스 카드 | 코스 탭 사진 **0장 → 12장** · 제목 잘림 해소 |
| #623 | P7.7 파일 분할 | `PlanEditorClient` **3,750 → 2,491줄** (−34%) |
| #624 | P7.8 감성 + N-9 | 🔴 **B안(배경만)은 구현 불가로 판명** — 아래 |
| #625 | P7.9 최종 게이트 | 10로케일 **데이터 게이트** · 전 게이트 통과 |

## 상태

- `tsc --noEmit` **0**
- `npx jest __tests__` **5,439 pass** (기준선 5,378 → **+61**) · 실패 **0**
- 디자인 게이트 **75 pass** (세션 시작 **19**)
- `npm run build` **exit 0** (종료 코드로 확인)
- 시뮬레이션 **완전 드레인** (sim 예약 0 · 고아 0)

## 신규 상설 게이트

| 게이트 | 무엇을 막나 |
|---|---|
| `plannerButtonHierarchy` | 커밋 버튼이 보조 버튼보다 작아지는 것(36<44 재발) |
| `plannerRadiusScale` | 반경이 20/14/10/9999 밖으로 새는 것 · 중첩 규칙 |
| `plannerElevation` | 플래너 전용 그림자 부활 · **다크에서 `none`** · rim 박탈 |
| `plannerTypeHierarchy` | 주인공이 사라지고 전부 작아지는 것 · 스케일 단계 붕괴 |
| `plannerLayoutOrder` | 도구가 결과 위로 올라오는 것 · hero 카드 2장 · **방향 카피 부패** |
| `plannerMotionVocabulary` | 어휘 밖 지속시간/이징 · 생 `active:scale-*` · reduced-motion 누락 |
| `plannerCourseCards` | 코스 카드에서 사진이 사라지는 것 · resolve 전 slice |
| `plannerFileSplit` | 카피가 컴포넌트로 되돌아오는 것 · 2,800줄 상한 |
| `plannerLocales` | 로케일이 조용히 영어로 폴백하는 것 · 키 누락 |
| `skinContrast`(확장) | 플래너 스코프 20조합 · **미해결 `var()`** · 섞임 자체 |
| `shellSurfaceContract`(확장) | 플래너 4개 루트 전부 셸 계약 소비 |

## 🔴 이번 라운드에서 배운 것 — 20~24

### 20. 🔴 게이트를 조용하게 만드는 방법이 두 가지 더 있다 (둘 다 이 세션이 저지를 뻔했다)

**① 주석 안의 중괄호.** P7.2 에서 `--tr-plan-shadow-{soft,card,button}` 이라고 **주석에** 적었다.
`skinContrast` 의 블록 파서는 `{`/`}` 로 쪼개므로 **주석 한 줄이 뒤 규칙 전부의 파싱을 어긋나게 했고**,
플래너 토큰이 통째로 사라져 게이트가 "팔레트를 재는 것"에서 **"아무것도 안 재는 것"** 이 됐다.
→ 브레이스 스캔 **전에** 주석 제거. 적대적 케이스 `/* { probe } */` 로 확인.

**② `var()` 별칭.** P7.8 에서 값을 한 번만 두려고 `--tr-plan-v-*` 로 별칭을 걸었더니,
게이트가 읽는 `--tr-surface` 가 **문자열 `var(--tr-plan-v-surface)`** 가 됐다. WCAG 계산은 **NaN**,
그리고 **이 테스트들은 NaN 을 건너뛴다.** → 리졸버에 역참조 + **미해결 `var()` 가 남으면 실패**하는 테스트.

**공통 교훈: 게이트를 조용하게 만드는 건 보통 코드가 아니라 그 옆의 것이다** — 산문, 별칭, 리팩터.

### 21. 🔴 "배경만 스킨"은 팔레트가 가질 수 없는 관계를 요구한다

§N-9 B안을 곧이곧대로 넣자 **sky/light 에서 `ink-3` on `canvas` = 3.31**.
**캔버스와 잉크는 한 쌍**이다 — 스킨은 저마다 *자기 배경에서* 읽히는 잉크를 고르므로,
한 화면의 잉크를 고정한 채 배경만 갈면 **두 값을 안전하게 만들던 유일한 관계**가 끊긴다.
→ 선을 **팔레트가 실제로 접히는 곳**에 그었다: **중립 계열은 통째로 스킨**, **플래너는 accent 정체성**.
20조합 전부 통과. **디자인 결정을 실측이 뒤집은 사례**이고, 뒤집힌 이유가 취향이 아니라 구조다.

### 22. 스크린샷이 코드 리뷰가 못 본 걸 잡는다 (또)

P7.6 첫 초안이 코스 카드에 **DB 키 첫 글자**로 만든 스와치 "U"·"M" 을 띄웠다 —
바로 아래 미리보기 줄은 **전혀 다른 세 곳**을 나열하는 채로. 클래스명·계산값으로는 안 보였다.
원인: 미리보기 줄은 `poiByKey` 로 **필터**하는데 내 스트립은 안 했다. **같은 데이터에 두 필터**.

### 23. 섹션을 옮기면 **방향을 가리키는 카피가 썩는다**

P7.4 가 「내 하루」를 위로 올리자 빈 상태 문구가 *"위에서 골라보세요"* 인데 피커가 아래가 됐다 —
**10로케일 전부.** 코드는 멀쩡하고 문장만 틀린다. → 방향 중립으로 다시 쓰고 **9개 언어의 위쪽
지시어를 금지하는 게이트**를 넣었다.

### 24. 플랜의 진단도 낡는다 — 구현하면서 정정할 것

§G-7 의 *"섹션 헤딩 12px → 15px"* 는 **이미 15px 였다**(측정함). 12px 42회는 칩·입력·힌트다.
§B-2 의 *"73건 해결 가능"* 은 실제로 **75건**이었다(체인이 죽은 `images[0]` 을 구제해서).
**들고 가지 말고 원문 자리에 고칠 것.**

## 🔴 사장님이 결정하실 것 (2건)

1. **커밋 버튼의 색.** §H-3 스펙대로 **accent 워시 + accent 링**(룸의 「운행 시작」과 같은 문법)으로
   갔다. 이전의 **작고 진한 초록 슬래브**보다 **크고 차분**하다. 크기·위계는 확실히 이겼지만
   "더 연해졌다"고 느끼실 수 있다. **되돌리면 한 줄**(`--hero` → `--primary`, 높이 56 유지).
   전후 컷: `docs/audit/shots/planner-{before,p72}-en-light-01-top.png`
2. **사진 없는 POI 49곳.** 배선을 고쳐도 남는 **콘텐츠 게이트**다(그중 2개는 `_metadata` 유사행이라
   실제 47곳). 목록은 `npx tsx scripts/qa-poi-image-coverage.ts` 가 출력한다.
   레포에 파이프라인은 이미 있다(`wire-poi-images.mjs` · `import-atoc-modified-photos.mjs`).

## 남은 것 — 다음 세션

**P7 잔여(작음, 명시적으로 안 한 것):**
- **타임라인 리본**(스톱 사이 세로 실선). 리스트 **재정렬 애니메이션**(P7.5 의 남은 ④)과 같이 설계할 것
- **히어로 96px + 스크롤 시 크롬 융합**(§H-6). 여백만 조였고 **스크롤 연동 축소는 미구현**
- 코스 탭 스크롤 4,496 → **5,019px**(사진값). 줄이려면 스트립 높이(92px)가 한 줄

**트랙 잔여 19건** — `docs/SESSION-STATE-2026-07-30.md` 의 목록 그대로 유효:
K4v2 Phase 2~4 · X15 Phase 1 · X18 · X20 · N3잔여 · N8 · X1 잔여(576+428) ·
X7 · X12 · X10 · X9 · X8 · X11 · X19 · T0 · T1v2 · T3 · T5 · O6 · R6

⚠ **플래너를 다시 볼 때 게이트가 셋이다**(둘이 아니라):
① 전세 상품이어야 편집기가 뜬다 ② **룸 토큰 `?rt=` 없으면 "링크를 열 수 없어요"** ③ lead 여야 편집된다.
전부 `scripts/qa-planner-walk.mjs` 에 박아 뒀다 — 편집기가 아니면 **exit 2** 로 죽는다.
⚠ 그 하니스는 `--apply` 로 코스를 적용하면 **시드를 변형**한다. 적용한 다음 실행 전에 **재시드**할 것.
