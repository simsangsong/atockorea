# 사장님 확인 6건 — 조사 결과와 실행 상태 (2026-07-29)

> 사장님이 6건을 확인하고 "추천방향으로 진행"을 지시했다. 이 문서는 **그 조사 결과**다.
> 병렬 감사 + **각 위험 항목에 반박 에이전트**를 붙였고, **둘 다 refuted=true**로 돌아왔다 —
> 방향은 옳고 **실행 범위가 좁았다**. 그 교정이 이 문서의 값어치다.

---

## ① 콘솔 두 개 (K2) — 파킹, 그리고 코드는 이제 이것에 안 걸린다

수치가 필요하지만 코드는 막히지 않는다. **K1b가 건 것은 요금제 상한이 아니라 모양 제한**이다
(스트림 시작 12/분, 예약+클라이언트 키). 라이브 수요는 하루 최대 손님 16명 · 방 5개(실측).

## ② 홈 대표 상품 — 완료 (#586)

`jeju-grand-highlights-loop`. 바꾸려면 `lib/home/home-cta-routes.ts` 한 줄.

## ⑤ 다크 대비 — 완료 (#598 → **#600에서 정정·완결**)

두 번에 걸쳐 고쳤고, **첫 번째가 불완전했고 부정확했다.**

- eyebrow는 `--tr-surface`뿐 아니라 **`--tr-surface-2`에도** 앉는다(LobbyCard 픽업·차량, GuideGuestCard `unseated`).
  그 쌍이 **다크 9스킨 4.12~4.32**, 그리고 **플래너는 라이트 4.43**으로 실패 중이었다.
- 🔴 플래너가 중요한 이유: `skinContrast`의 플래너 테스트가 **같은 PAIRS 배열을 펼친다.**
  이 배열은 **11개 스코프**를 게이트한다 — "다크만"으로 읽으면 게이트를 빨갛게 배포한다.
- 🔴 그리고 #598 주석의 `ink 14.4 > ink-2 8.9 > ink-3 4.8`은 **재현 불가**였다.
  실측 **13.62 / 7.25 / 4.82**. **추론을 측정의 모양으로 쓴 것** — #600에서 원문 자리에 정정.

최종: 10값 조정, 브라우저 재측정 **최악 4.85 · 4.5 미만 0건**.

## X18 — 원인 확정 (#599)

쓰기 경로는 **정상**(1회 호출 → 1행 증명). 매트릭스가 빈 건 **`poi_key`가 없어서**다 —
플라이휠은 키 없는 도착을 버리고, `tourId` 스윕(#593) 전까지 스톱에 키가 **0/130**이었다.
**#593이 실제 해금**이고 매트릭스는 지금부터 찬다.

---

# ④ 스토리지 — 🔴 **실행 대기, 범위가 이미 정해져 있다**

## 지금 노출은 0이다 (실측)

- `public` 스키마의 **모든** text/varchar/json/jsonb 컬럼을 `query_to_xml`로 훑어
  `tour-room-photos`·`tour-audio` 참조 → **0행**.
- `tour_room_messages` 41행 중 image/file **0** · `tour_room_extras` **0행**.
- 객체 12개 = **70바이트 PNG 10개 + 10바이트 PDF 2개**.
  `qa-prod-surfaces.ts`의 `tinyPng()`(정확히 70B)와 `Buffer.from('QA voucher')`(정확히 10B)와
  **바이트 단위로 일치** — 출처가 증명된 QA 산출물이다. 손님 사진이 아니다.
- 그 경로의 roomId 6개는 **전부 존재하지 않는 룸**(고아). `--cleanup`이 DB만 지우고 스토리지는 안 지운다.

**그래서 지금이 가장 싸다.** 저장된 URL이 0건이므로 **백필이 필요 없다.**
실 트래픽이 하루 흐를 때마다 이건 0행 변경에서 **되돌릴 수 없는 능력 URL의 N행 백필**로 바뀐다.

## 범위는 두 버킷뿐 — "전부 비공개"가 아니다

| 버킷 | 판정 | 이유 |
|---|---|---|
| `tour-room-photos` | **비공개 + 서명 URL** | `att/`는 손님 사진, 그리고 **영수증은 손님 금융 데이터** |
| `tour-audio` | **비공개 + 서명 URL** | 한 줄 변경 · **마이그레이션 없음**(이미 path를 저장하고 URL은 읽을 때 발급) |
| `tour-videos`·`tour-images`·`tour-gallery`·`product-images`·`email-assets` | **공개 유지** | 마케팅/CDN 콘텐츠 |

⚠ `qr/`는 같은 버킷에 있지만 **기사가 스캔해야 한다** — 경로 단위로 갈라야지 버킷 통째로 하면 깨진다.

## 🔴 반박이 잡은 것 — 이걸 빼면 조용히 깨진다

`metadata.attachment.url`을 `path`로 바꾸면 **세 곳이 아니라 다섯 곳**이 걸린다:

1. `GET /messages` — 인증돼 있으니 서명하기 자연스러운 지점
2. **POST 응답**
3. **`broadcastToRoom` 페이로드** — 실시간으로 클라이언트에 직접 간다.
   **GET만 서명하면 실시간으로 온 이미지가 깨진다**
4. 🔴 **`GET /media`** (룸 서랍의 사진/파일 모음) — `drawer.ts:74-80`의 `toAttachmentItem`이
   `metadata.attachment.url`이 **비어 있지 않은 문자열일 때만** 값을 돌려주고 라우트가 `.filter(Boolean)` 한다.
   → **서랍이 영원히 빈 배열을 돌려준다. 조용히. 에러도 400도 콘솔도 없이.**
5. 🔴 `buildRoomSnapshot`(`snapshot.ts:252`)이 `select('*')`로 메시지를 실어 나른다

## 실행 순서

1. **플래그와 재생성 버그를 같이 고친다.** `attachments.ts:85`와 `vision-ask/route.ts:81`이
   `createBucket({ public: true })`를 **하드코딩**한다 — 버킷이 지워졌다 다시 만들어지면
   **비공개 설정이 조용히 되돌아간다.** 둘 중 하나만 하면 안 된다.
2. 저장값을 URL → **path**. 기존 행이 0이라 백필 불필요. 단 레거시 `url` 읽기는 한 줄이니 남긴다.
3. **다섯 출구 전부**에서 읽을 때 서명. (위 목록)
4. `tour-audio`: `getPublicUrl` → `createSignedUrl` (tts-server.ts:79·:100, tts/route.ts:86).
5. 패턴은 **새로 만들지 말고** `lib/ops/seating/layoutPhoto.ts`를 복사
   (private 버킷 보장 + `createSignedUrl` + 실패 시 슬롯만 비는 fail-soft).
   ⚠ 그 세 private 버킷은 **라이브에 존재하지 않는다** — 코드는 있으나 프로덕션에서 돈 적 없다. 첫 실행 이슈를 예상할 것.
6. 고아 12개 삭제 + `sim-tour-day.ts --cleanup`이 **스토리지도** 비우도록.
7. `docs/video-scripts-and-briefing-copy-2026-07-26.md`가 **공개 버킷 안에** 있다 — 옮길 것.

---

# ③ 개인정보 문구 — 초안 완성, 결정 1건

## 정책이 사는 곳 (실측)

`app/privacy/page.tsx` 하나. **URL에 로케일이 없고**, 문장은 전부 `t('privacy.…')` → `messages/<locale>.json`.
**10개 로케일 전부 `privacy` 아래 정확히 19개 키**를 갖고 있다.
불릿을 하나 더하려면 **JSON과 JSX 둘 다** 고쳐야 한다(키를 map 하지 않고 하드코딩).

## 권고

새 섹션 **`privacy.s16`** 을 10로케일에 추가하고 `googleApi` 블록 뒤에 렌더.
🔴 **번호를 다시 매기지 말 것** — `googleApi`의 "15."는 **구글 OAuth 심사 제출서에 인용돼 있다.**

`s2`/`s3`/`s4`에 불릿으로 흡수시키지 말 것: 가장 중요한 문장
— **"이건 다음에 당신을 알아보는 데 쓰지 않는다"** — 은 수집 항목 목록에 있을 자리가 없다.

## 담을 내용 (요지)

- 저장 **전에** 제거: 이름·예약·룸·참가자 → **역추적 불가**
- 양쪽에서 마스킹: 이메일·전화·식별코드·우리 링크의 토큰 → `[email]` `[phone]`
- 남기는 것: 질문·답변 텍스트, 각각의 언어, **도달한 스팟**, **시각(시)**.
  **날짜는 안 남긴다** — 날짜 + 투어는 아주 작은 그룹을 가리키기 때문
- **사람이 승인하기 전엔 안 쓴다**: 비활성 초안으로 저장, 스태프 검수
- **당신을 알아보는 데 쓰지 않는다**: 계정·예약에 연결하지 않고, 다시 오셔도 다르게 대하지 않는다

## 🔴 결정 필요 — 옵트아웃 문장(`li6`)을 넣을 것인가

10로케일 초안은 다 있다. **넣으면 지금 없는 작업을 약속하는 것**이다.
기술적으로는 가능하다(원본이 `tour_room_messages`에 있고 `fingerprint()`가 메시지 id에서 재계산된다) —
그러나 **오늘 도구가 없고** `/admin/qa-review`에 "손님으로 찾기"가 없다.

**추천: 넣지 말 것.** 지킬 수 없는 약속보다, 승인 전 초안 상태라는 사실을 명확히 적는 편이 정직하다.
도구가 생기면 그때 문장을 추가한다.

---

# ⑥ K4v2 — 설계 완료, 결정 1건

## 커버리지 원장 = **라우트 45개 파일 · (메서드, 경로) 54쌍** (실측)

`app/api/tour-rooms/**`와 `app/api/tour-mode/**`의 `export GET|POST|PUT|PATCH|DELETE`를 파싱해 뽑았다.
**이 목록이 원장이다.**

## 10개 중 8개는 이미 있다 — 새로 만들 것은 둘뿐

| 필요 | 재사용 | 바꿀 것 |
|---|---|---|
| sim 격리 판정 | `lib/ops/sim/simScope.ts` | 없음 |
| 시드 + 정리 + FK 순서 | `scripts/sim-tour-day.ts` | 하드코딩 2예약 → `--rooms=20 --mix=live` |
| 룸 콘텐츠 사전 적재 | `scripts/sim-populate.ts` | N방으로 파라미터화 |
| HTTP 드라이버(페이싱·기대목록·PASS/FAIL·JSON 리포트·계약 함정) | `scripts/qa-prod-tour-room.ts` | `call()`과 헤더의 함정 목록을 **그대로** 들어올림 |

## 단계

0. **드레인 + 게이트**(비용 0) — `--cleanup` 후 `sim_tag='sim'` 0 확인.
   `qa-prod-tour-room.ts`의 `extend` 바디와 **POST 전용에 GET 하던 5건**을 고친다 —
   이 둘만으로 **가짜 결과 6건이 진짜가 된다.**
1. **원장**(비용 0, 네트워크 0) — `lib/audit/k4Coverage.ts` + `docs/audit/K4-coverage.md`
   (`route | method | actor | tier | 배정된 방 | 건너뛴 사유`) + 스테일 게이트. **단독으로 먼저 착지.**
   원장이 틀리면 그 아래가 전부 틀리고, 제대로 만드는 데 비용이 안 든다.
2. **시더**(행은 쓰지만 LLM 0) — 20방, 참가자 로케일 집합은 **고정**(바뀌면 번역 캐시가 무력화된다).
3. **할당 + 주행** — 53쌍을 20그룹(~2.7개)으로, `join POST`는 20방 전부(문이니까).
   문장 코퍼스 ~14개를 **20방에 그대로 재사용**.
4. **출력 후 정리** — 커버리지 표 + `ops_ai_usage` 델타(purpose별) +
   🔴 **"이 주행이 재지 않은 것"을 명시**: 동시 실시간 연결(§K-2) · SSE 폴백 증폭(§K-3).

## 🔴 결정 필요 — `POST /sos`

54쌍 중 **유일하게 살아 있는 사람을 호출하는 것**이다(관리자 이메일 + 관제 웹푸시).

(a) 사장님이 보고 계실 때 실행 · (b) 수신자를 임시 주소로 · (c) **설계상 건너뜀**으로 사유와 함께 출력.

**추천: (a).** 가장 결과가 큰 경로이므로 한 번은 진짜로 울려 봐야 한다.
(b)는 **비상 경로의 설정을 바꾸는 것**이라 그 자체가 위험이다.
