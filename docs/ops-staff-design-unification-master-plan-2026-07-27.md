# 관제센터 디자인 언어 통일 + 기사·가이드 앱 재감사 — 마스터 플랜 (2026-07-27)

**SoT = 이 문서.** 사용자 지시: ① 투어관제센터 UI를 스마트앱과 맞출 것 — 배경·버튼 스타일·폰트·UX 등 디자인 언어 전반 계승 리디자인 ② 기사·가이드 앱 UI 전반 재감사로 카톡 수준까지 남은 업그레이드 확정. **평가축 3키워드 = 컴팩트 · 고급스러움 · 실용성.**

선행 SoT 계승: `docs/smartapp-chat-ui-theme-master-plan-2026-07-27.md`(크롬/스킨/카톡 리스트 문법) · `docs/smartapp-ui-premium-upgrade-master-plan-2026-07-26.md`(딥파인·타입 규율·아이콘 5단) · PR #495~#498(파스텔 스킨 4종+풍경 SkinScenery — 스킨은 이제 **10종**).

---

## §A 현황 진단 (2026-07-27 코드 실측)

### A-1 관제센터 (`components/tour-ops/` 23개 컴포넌트, 7,873줄)
| 축 | 상태 | 근거 |
|---|---|---|
| 토큰 엔진 | ✅ 이미 tr-root (W1.2에서 .ops-light 은퇴) | OpsApp L351 |
| **크롬** | ❌ 구문법 — 헤더/탭바가 `bg-surface + border-b + backdrop-blur` (흰 바+구분선). 스마트앱은 `--tr-chrome`(월페이퍼 융합) | OpsApp L354·L495 |
| **스킨** | ❌ `data-tr-skin` 미스탬프 — 가이드 콘솔은 10종 스킨을 타는데 관제만 회색 고정 | OpsApp root |
| **타이포** | ❌ raw `text-[11/12/13px]` **100+곳** — tr-* 스케일 밖 = 글자크기 슬라이더 무반응·드리프트 무방비 (최다: RoomManager 22·VehiclePanel 27·ManifestView 23) | grep 실측 |
| **색** | ❌ 하드코딩 tailwind **100+곳** (amber 17+13+11+8…, red, emerald) — 시맨틱 의도는 있으나 토큰/스킨/다크 튜닝 우회 | grep 실측 |
| **리스트 문법** | ❌ 룸 모니터링·매니저가 카톡 리스트 문법 이전의 카드 더미 | OpsHomeTab/RoomManager |
| 검증 | ❌ 관제 전용 walk 하니스 없음 (구글 OAuth 게이트 — sim adminSession shape 부족 기록 있음) | 메모리 |

### A-2 기사 콘솔 (`DriverConsole.tsx` 365줄)
- **셸 부재**: PIN 게이트 → "운행 시작" 단일 히어로 → 콕핏. 탭 없음 → **설정(테마·스킨·글자크기) 접근 불가, 대화 리스트 없음(콕핏 밖에서 손님 메시지 못 봄), 손님안내(왓츠앱 D-1) 진입 없음**. §O 후속티켓 ② "기사 콘솔 StaffShell 수렴" 그대로 미결.
- 주행 대형 타이포(2xl~4xl)는 안전 목적의 의도된 예외(typeDiscipline allowlist) — 유지.

### A-3 콕핏·가이드 잔여 (§O 후속티켓 + 이번 세션 관찰)
- ① **콕핏 채팅 = 자체 렌더러** — ChatFeed 미임베드라 리액션·인용·읽음·첨부 위계가 카톡 수준 미달 (지도카드는 이번에 잡음).
- ⑥ 서랍 미디어 페이지네이션 UI 없음 · 서랍 내 Lightbox Esc 중첩 닫힘.
- ③ 손님 읽기전용 좌석 보기 미결(명단은 스태프 전용).
- 가이드 콘솔은 이번 세션에 카톡 문법 완료(#490~#493) — Part B 감사에서 미세 폴리시만 재확인.

---

## §B 바인딩 결정 (U-D1 ~ U-D8)

- **U-D1 관제 = 같은 언어, 같은 밀도 아님.** 스마트앱의 토큰·크롬·리스트 문법을 계승하되, 관제는 정보밀도 우선 도구다: 풍경(SkinScenery)은 **관제 미탑재**(실용), 스킨은 탑재(가이드↔관제 오가는 동일 인물의 연속성 — 고급).
- **U-D2 관제 타이포 = tr-* 스케일 강제.** `text-[11px]→tr-meta · 12px→tr-label · 13px→tr-card-text` 기계 치환 + **`typeDiscipline`을 `components/tour-ops`로 확장**(가드 없이는 반드시 되돌아간다 — 스마트앱에서 이미 증명된 패턴). 예외는 SeatMap 좌석번호 등 고정 지오메트리만 allowlist.
- **U-D3 색 = 시맨틱 토큰 매핑.** amber(주의)→`--tr-accent-soft/deep` 또는 신규 `--tr-warn`(1토큰 신설, 라이트/다크/스킨별 정의), red(SOS/위험)→`--tr-danger`(불변), emerald(성공/온)→`--tr-safe`. **SOS 레드 강도 불변**(§H 예외 유지). 100+곳은 파일 단위 배치 치환 + 스킨별 육안 스윕.
- **U-D4 관제 크롬/버튼.** 헤더·하단탭 `--tr-chrome`+`tr-chrome-line-*` 이관(backdrop-blur 제거 — 융합 문법에선 무의미), 버튼은 `tr-btn-physical/raised/flat` 3재질로 수렴, 토글은 이번 컴팩트 스위치(44×26) 재사용.
- **U-D5 관제 리스트 = 카톡 문법 이식.** 룸 모니터링(홈)·룸 매니저를 GuideConsole 대화탭과 같은 행 문법(휴 아바타·미리보기·시각·언리드 도트·⋮ 액션 시트)으로 — **GuideConsole의 행 컴포넌트를 추출·공용화**(`components/tour-mode/chatlist/` → 양쪽 소비)해서 두 벌 유지 금지.
- **U-D6 기사 콘솔 = StaffShell 수렴.** 기사도 [대화·운행·설정] 3탭(좌석·명단은 driver overview 권한 재사용해 조건 노출). 대화 탭 = 같은 카톡 리스트, 설정 탭 = StaffSettings 재사용(테마·스킨·글자크기 즉시 획득). PIN 게이트·주행 대형 타이포·"운행 시작" 히어로는 **유지**(실용 — 운전 직전 화면은 크게가 정답).
- **U-D7 콕핏 채팅 = ChatFeed 임베드.** `variant="cockpit"` prop(다크 고정·폰트 확대·pinch-zoom 유지)으로 임베드해 리액션/인용/읽음/첨부를 한 번에 획득. 주행 안전상 **입력은 현행 원버튼·음성 유지**, 임베드는 피드 렌더만.
- **U-D8 검증 = 하니스 우선.** Part A 착수 전에 관제 walk를 만든다: sim adminSession shape 보강(기록된 실패 원인 해소) 또는 가이드 토큰이 닿는 표면 우선 + 어드민 표면은 세션 주입 재시도. 스크린샷 없는 리디자인 완료 선언 금지(이 세션 규율 그대로).

## §C WBS — Part A 관제센터 통일 (O1~O6)

| 티켓 | 내용 | 규모 |
|---|---|---|
| **O0** | 관제 walk 하니스(U-D8) + 현재 화면 10컷 베이스라인 | S |
| **O1** | 크롬·스킨: OpsApp 헤더/탭바 `--tr-chrome` 이관 + `data-tr-skin` 스탬프(풍경 제외) + 컴팩트 토글 잔여 확인 | S |
| **O2** | 타이포 이관: raw px→tr-* 배치 치환(23파일) + typeDiscipline tour-ops 확장 + allowlist 최소화 | M |
| **O3** | 색 토큰화: `--tr-warn` 신설(라이트/다크/스킨 10종 정의+대비 검증 스크립트 재사용) + amber/red/emerald 배치 매핑 | M |
| **O4** | 리스트 문법: 공용 chatlist 행 추출 → 관제 홈 룸 모니터링·룸 매니저 이식(⋮ 시트에 기존 액션 전부 보존 — 편의성 무손실 원칙) | L |
| **O5** | 손님안내(M4)·매니페스트 등 폼 표면 폴리시: tr-btn 재질·시트 문법·크루즈 칩 스타일 정돈 | M |
| **O6** | 게이트: tsc·jest·빌드·관제 walk 스킨×다크 매트릭스 + 적대적 시각 리뷰 라운드 | S |

## §D WBS — Part B 기사·가이드 재감사 (T0~T5)

| 티켓 | 내용 | 키워드 근거 | 규모 |
|---|---|---|---|
| **T0** | **재감사 라운드**: 기사·가이드·콕핏 전 표면 walk 촬영 → 3키워드 채점표(컴팩트=밀도·잘림, 고급=위계·마감, 실용=탭수·장갑끼고 조작) → 티켓 확정. 아래 T1~T4는 사전 확정분 | — | S |
| **T1** | 기사 콘솔 StaffShell 수렴(U-D6): 3탭+대화 리스트+설정 접근, PIN·히어로 유지 | 실용·컴팩트 | L |
| **T2** | 콕핏 ChatFeed 임베드(U-D7): 리액션·인용·읽음·첨부 위계 획득, 입력은 원버튼 유지 | 고급·실용 | M |
| **T3** | 서랍 미디어 페이지네이션 + Lightbox Esc 중첩 수정(§O ⑥) | 실용 | S |
| **T4** | 손님 읽기전용 좌석 보기(§O ③ — "내 좌석 몇 번?"을 채팅 안 물어도) | 실용 | M |
| **T5** | T0 감사 발견분(예상 후보: 콕핏 야간 눈부심 완화, 기사 칩 한손 도달권, 가이드 콘솔 미세 폴리시) | 3키워드 | M |

**실행 순서 제안**: O0→O1→O2→O3(관제 골격) → T0(감사) → T1→T2(기사 코어) → O4→O5(관제 리스트) → T3~T5 → O6 최종 게이트. 관제 골격(O1~O3)과 T0 감사는 병렬 가능.

## §E 불가침·리스크
- 불변: SOS 레드 · ActionGrid 트레이 · 44px 터치(컴팩트 스위치처럼 시각↓+히트영역 유지 패턴) · pruneTarget · 미리보기=발송 · 주행 대형 타이포 allowlist.
- R1 관제 색 치환 회귀 → 파일 단위 커밋+스킨 10종 대비 스크립트+walk 비교컷. R2 chatlist 공용화가 가이드 콘솔 회귀 → 기존 테스트ID 보존+jest. R3 관제 walk 인증 → U-D8 이중 경로. R4 병렬 세션 경합 → 착수 시 git pull·파일 단위 커밋 유지.

## §F 실행 로그
- [ ] O0 · [ ] O1 · [ ] O2 · [ ] O3 · [ ] O4 · [ ] O5 · [ ] O6 · [ ] T0 · [ ] T1 · [ ] T2 · [ ] T3 · [ ] T4 · [ ] T5
