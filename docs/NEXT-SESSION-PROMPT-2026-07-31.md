# 다음 세션 실행 프롬프트 — 그대로 복사해서 붙여넣기

> 이 파일은 **붙여넣을 프롬프트 한 덩어리**다. 아래 코드블록 전체를 복사한다.
> 2026-07-30 야간 라운드(PR #603~#612, 10건 머지) 이후 상태로 다시 썼다.

## 붙여넣기 전에 알아야 할 것 (사람용 요약)

- **main 초록.** `tsc` 0 · `jest __tests__` **5,378 pass / 504 suites**(사전존재 fixture 2건 제외) ·
  `build` exit 0 · 디자인 게이트 19 pass
- **다음 세션의 주제는 하나다 — 플래너 전면 재편(P7).** 사장님이 직접 지시하신 건이고,
  재리뷰·디자인 감사·값 스펙·게이트·WBS 가 **전부 준비돼 있다.** 남은 건 만드는 것뿐이다
- **정본이 갈라졌다:** 플래너는 `docs/planner-overhaul-p7-master-plan-2026-07-30.md` 가 SoT.
  마스터 플랜 Part D 는 이제 **역사**(진단·실행 기록)다
- **사장님 결정 3건 대기** — 아래 프롬프트 §0

---

```text
스마트앱/관제 통합 트랙을 이어서 진행해줘. 이번 세션의 주제는 플래너 전면 재편(P7)이야.
나는 자러 가고 아침에 결과를 볼 거야. 모든 권한을 위임한다.
선택지가 나오면 네 추천 방향으로 결정하고 계속 가. 멈추지 말고, 생략 없이,
건너뛰기 없이, 기능 축소나 UI 다운그레이드 없이.

■ 먼저 이 넷을 본문 전체로 읽어 (요약본 말고)
  1. docs/planner-overhaul-p7-master-plan-2026-07-30.md   ← 🔴 이번 세션의 정본.
     §A 플랜이 틀린 곳 · §B 실측 · §C 원칙 · §G 디자인 감사(계산값) · §H 값 스펙 ·
     §I 게이트 5종 · §J WBS 10단계. 여기 없는 걸 새로 정하지 마.
  2. docs/SESSION-STATE-2026-07-30.md      직전 라운드가 뭘 했고 뭘 배웠나 (교훈 13~19)
  3. docs/OWNER-DECISIONS-2026-07-29.md    사장님 결정 8건 (§8 이 플래너)
  4. docs/NEXT-SESSION-SMARTAPP-2026-07-28.md  환경 · 충돌규칙 · 게이트
  마스터 플랜(ops-staff-design-unification-…)은 배경으로만 봐라 —
  플래너에 관한 한 Part D 는 역사이고, ①이 정본이다.

■ §0 사장님 결정 3건 — 물어보지 말고 아래 추천대로 가고, 아침 보고에 적어
  1. P-D20(스톱 행을 더 줄인다)을 P7-D2가 뒤집는다 → 뒤집는 쪽으로 간다.
     근거: 컴팩트 처방은 이미 실행됐고(실측 44px) 불만이 남았다. 그리고 타입 사용
     112회 중 69회가 가장 작은 두 단계다 — 조잡함은 작은 글자가 많은 게 아니라
     큰 글자가 없는 것이다. ⚠ 단 한 줄 행을 없애지는 마라. 접힌 기본값은 유지하고
     주인공만 키운다.
  2. 사진 없는 POI 51곳(41%)은 콘텐츠 게이트다 → 코드로 메우려 하지 말고
     스와치 폴백(#335 패턴, 이니셜+카테고리 색)으로 정직하게 처리하고 목록만 보고해라.
  3. §N-9 스킨은 B안(배경만) → 단 P7.0 게이트와 P7.1b 배선이 선행이다. 마지막 단계다.

■ 셋업
  1) 🔴 pwd 확인. 전용 워크트리에서 작업해라 — 지난 세션에 Bash cwd 가 메인 저장소로
     드리프트했고 거기 node_modules 는 손상 상태다.
  2) .env.local 복사 · NEXT_PUBLIC_TOUR_MODE_V1=1 확인
  3) git fetch origin main && git reset --hard origin/main
  4) 기준선: npx tsc --noEmit
            npx jest __tests__
     → tsc 0 / 5,378 pass 가 기준선. 안 맞으면 그것부터.

■ 🔴 플래너 편집기를 보려면 게이트가 둘이다 (이거 모르면 다른 화면을 보고 감사한다)
  ① 기본 시뮬레이션은 **고정 일정 상품**을 심는다 → 읽기전용 뷰가 뜬다.
     전세 상품으로 바꿔야 편집기가 나온다:
       ALLOW_SIM_SEED=1 npx tsx scripts/sim-tour-day.ts
       (booking1 의 tour_id 를 75e6d604-3c9e-4380-8356-cbd656525fda 로 UPDATE, sim_tag='sim' 조건)
  ② 새 브라우저 프로필은 **비-lead** 라 배너만 보인다 →
     [data-testid="plan-claim-lead"] 를 눌러야 편집기가 열린다.
  끝나면 반드시: npx tsx scripts/sim-tour-day.ts --cleanup  (드레인 게이트라 잔여 있으면 exit 1)

■ 🔴 실제로 물린 것들 — 같은 걸 또 하지 마 (19개)
  1. 빌드 게이트는 grep 이 아니라 **종료 코드**다.
  2. 게이트 목록에 __tests__/audit 를 반드시 포함.
  3. 플랜의 "미착수"도 "경고"도 믿지 말고 **재라.**
  4. 워크트리 dev 서버는 pkill 로 안 죽는다. 포트로 죽여라.
  5. Python heredoc / node -e 로 정규식·한글·백틱 든 파일 고치지 마. Edit 도구를 써라.
  6. 스크린샷으로 안 보이는 결함이 있다(가림). elementFromPoint 히트 테스트.
  7. EventSource 는 429 를 못 본다. 상한은 스로틀이어야 한다.
  8. "코드에 있다" ≠ "작동한다" — 순서 때문에.
  9. 감성 레이어가 대비를 깎으면 결함이다.
  10. 스캐너를 고치는 게 스캔 결과를 훑는 것보다 싸다.
  11. 내가 방금 배포한 것도 감사 대상이다.
  12. 배열 하나가 몇 개 스코프를 게이트하는지 세어라(skinContrast PAIRS = 11 스코프).
  13. 실패 방식이 "조용함"이면 게이트를 **소스**에 걸어라.
  14. 브라우저 워크가 "3로케일 통과"라고 해도 **같은 로케일 3번**일 수 있다
      (룸 언어 = 예약별 조인 선택). 로케일 검증은 렌더 테스트로.
  15. getComputedStyle 이 visible 이어도 화면엔 없을 수 있다(포지션드 의사요소).
  16. 측정 전에 포트가 비었는지 확인. 안 죽은 서버가 옛 HTML 을 서빙한다.
  17. 🔴 이 플랜은 "미착수"를 **다섯 번** 틀리게 적었다(P0·P1·P2·P3·P4).
      착수 전 5분 grep 을 규칙으로 삼아라. 이번엔 그게 티켓을 뒤집었다.
  18. 🔴 **디자인 문제로 보이는 것의 절반은 배선이다.** "사진이 없다"는 다른 컬럼을 읽는
      것이었고(9.7 vs 58.9%), "스킨이 안 먹는다"는 특이도가 아니라 셸 계약 미소비였다.
      **디자인을 고치기 전에 배선부터 재라.**
  19. 🔴 구조적으로 검수를 비껴가는 화면이 있다(위 편집기 게이트 둘).

■ 실행 순서 — §J 그대로. 한 단계씩 PR 내고 머지해라

  P7.0  게이트 선행 — skinContrast 의 resolvePlan() 이 스킨 인자를 받게.
        🔴 그 게이트 없이 플래너 팔레트를 건드리지 마라(전에 게이트 밖에서 ink-3 미달로 배포된 적 있다).

  P7.1  🔴 사진 배선 — poi.default_image_url || poi.images?.[0] (2곳: PlanEditorClient:2816, :3473)
        + 스와치 폴백 + 깨진 default 1건(yongmeori_coast) 정리 + 커버리지 회귀 게이트.
        실측 8.9% → 58.9%. 같은 레포 POICatalogGrid:121 이 이미 그 패턴이고 API 는 이미 images 를 보낸다.

  P7.1b 🔴 useShellSurface 소비 — --tr-font-scale 과 data-tr-skin 이 플래너에 도달하게.
        접근성 회귀 수정이고, P7.3(타이포)의 선행조건이다.
        (글자 크기가 1.0 에 묶인 채 타이포 스펙을 잡으면 5단계 중 1단계에서만 맞는 값이 된다.)

  P7.2  재질·반경·버튼 통합 (§H-1·H-2·H-3) + 게이트 3종
        🔴 불변식: 커밋 버튼은 그 화면의 어떤 보조 버튼보다 크다 (지금 36 < 44).
        --tr-plan-shadow-* 폐기하고 --tr-rim + --tr-elev-* 로. 다크에서 none 금지.
        반경은 20/14/10/9999 넷. 규칙: 자식 반경 = 부모 반경 − 부모 패딩.

  P7.3  타이포 위계 (§H-4) — 「내 하루」 18px 주인공, 스톱 이름 13→15px, 섹션 헤딩 12→15px.
  P7.4  배치 (§H-6) — 만들고 있는 물건(내 하루)을 도구(검색·목록) 위로.
  P7.5  모션 (§H-5) — 160ms·all 0s·생 active:scale-95 제거. 의미 있는 전이 4개만. reduced-motion 정지.
  P7.6  코스 카드 사진 히어로 · 피커 리듬 · 제목 잘림.
  P7.7  PlanEditorClient 3,591줄 분할 (구 P5) — 앞 단계가 뭘 만질지 안 뒤에 쪼갠다.
  P7.8  여행 감성 레이어 (§H-7) — 하루의 결(N2 재사용, ⚠다크 제외) · 타임라인 리본 · §N-9 B안.
  P7.9  최종 게이트 + 10로케일 + 전후 컷 (구 P6).

■ P7 이 끝나고 시간이 남으면
  K4v2 Phase 2~4(원장은 이미 있다 · 실 LLM 비용 · POST /sos 는 계속 SKIPPED-BY-DESIGN) →
  X15 Phase 1 → X18 → X20 → N3잔여 → N8 → 5순위

■ 매 티켓마다
  - 게이트: npx tsc --noEmit · npx jest __tests__ ·
    npx jest skinContrast typeDiscipline shellStackingContext chipBoundary bottomEdgeOwnership ·
    npm run build (종료 코드로 확인)
  - UI를 건드렸으면 Playwright 전후 컷으로 증명. 단 교훈 14·15·16·19 를 먼저 읽어라 —
    컷도 거짓말할 수 있고, 애초에 다른 화면을 찍고 있을 수 있다.
  - 대비를 잴 땐 scripts/qa-lib/contrast-inject.js 를 써라. 새로 만들지 마.
  - 숫자를 쓸 땐 잰 건지 계산한 건지 문장에 표시해. 주석에 쓰는 숫자도 마찬가지.
  - 티켓 하나 끝나면 PR 내고 머지하고, **P7 문서 §F 실행 로그를 같이 갱신**.
  - 커밋 푸터는 Co-Authored-By: Claude <noreply@anthropic.com> 만. 모델 식별자 금지.
  - 진행 보고는 한국어, 코드·커밋은 영어. git add -A 금지, 경로 명시.

■ 막히면
  블로킹이면 그 단계만 사유 적고 건너뛰고 다음으로. 전체를 멈추지 마.
  건너뛴 건 전부 아침 보고에 모아서.

■ 아침 보고에 넣을 것
  끝낸 단계 · 건너뛴 단계와 사유 · 내가 결정해야 할 것 · UI 변경은 전후 컷 ·
  측정한 수치는 조건과 함께.
  🔴 사장님이 읽을 문장으로 써라 — 티켓 코드를 표의 첫 칸에 놓지 마.
```
