# 다음 세션 실행 프롬프트 — 그대로 복사해서 붙여넣기

> 이 파일은 **붙여넣을 프롬프트 한 덩어리**다. 아래 코드블록 전체를 복사한다.
> 2026-07-30 야간 5라운드(PR #603~#607) 이후 상태로 다시 썼다.

## 붙여넣기 전에 알아야 할 것 (사람용 요약)

- **main 초록.** `tsc` 0 · `jest __tests__` **5,352 pass / 503 suites**(사전존재 fixture 2건 제외) ·
  `build` exit 0 · 디자인 게이트 19 pass
- **사장님 게이트 3건이 닫혔다** — 스토리지·개인정보 문구·K4v2 원장
- **손님 앱이 41% 가벼워졌다** (763KB → 447KB, 실측 클린빌드)
- **열린 PR은 #67·#68 둘뿐**, 의도적 보류
- **사장님 결정 대기 2건** — 아래 프롬프트 §0

---

```text
스마트앱/관제 통합 트랙을 이어서 끝까지 진행해줘.
나는 자러 가고 아침에 결과를 볼 거야. 모든 권한을 위임한다.
선택지가 나오면 네 추천 방향으로 결정하고 계속 가. 멈추지 말고, 생략 없이,
건너뛰기 없이, 기능 축소나 UI 다운그레이드 없이.

■ 먼저 이 넷을 본문 전체로 읽어 (요약본 말고)
  1. docs/SESSION-STATE-2026-07-30.md    ← 직전 라운드가 뭘 했고 뭘 배웠나 (교훈 13~16)
  2. docs/OWNER-DECISIONS-2026-07-29.md  사장님 결정 8건
  3. docs/NEXT-SESSION-SMARTAPP-2026-07-28.md  환경 · 함정 · 충돌규칙 · 게이트
  4. docs/ops-staff-design-unification-master-plan-2026-07-27.md  Part A~O (왜)
  어긋나면 마스터 플랜이 정본, 단 결정 문서가 플랜보다 위다.

■ §0 사장님 결정 2건 — 물어보지 말고 아래대로 가고, 아침 보고에 적어
  1. K4v2 Phase 2~4 는 실 LLM 비용이 든다. 사장님 승인은 이미 받았으니 진행하되,
     POST /sos 는 계속 SKIPPED-BY-DESIGN 으로 두고 커버리지 표에 사유를 출력해라.
     (사장님이 보고 계실 때 한 번 진짜로 울려 보는 건 여전히 유효한 다음 단계다.)
  2. 플래너 §N-9 는 선택지 B(배경만 스킨)로 간다. 단 skinContrast 의 resolvePlan() 에
     스킨 인자를 먼저 넣어라 — 그 게이트 없이 팔레트를 건드리지 말 것.

■ 셋업
  1) 전용 워크트리. .env.local 복사 — NEXT_PUBLIC_TOUR_MODE_V1=1 확인
  2) git fetch origin main && git reset --hard origin/main
  3) 기준선: npx tsc --noEmit
            npx jest __tests__
     → tsc 0 / 5,352 pass 가 기준선. 안 맞으면 그것부터.
  4) 시드가 필요하면: ALLOW_SIM_SEED=1 npx tsx scripts/sim-tour-day.ts
     끝나면 반드시 npx tsx scripts/sim-tour-day.ts --cleanup
     (이제 드레인 게이트라 행·고아룸·고아미디어가 남으면 exit 1 이다)

■ 🔴 실제로 물린 것들 — 같은 걸 또 하지 마 (16개, 앞 12개는 이전 세션 것)
  1. 빌드 게이트는 grep 이 아니라 **종료 코드**다.
  2. 게이트 목록에 __tests__/audit 를 반드시 포함.
  3. 플랜의 "미착수"도 "경고"도 믿지 말고 **재라**.
  4. 워크트리 dev 서버는 pkill 로 안 죽는다. 포트로 죽여라.
  5. Python heredoc / node -e 로 정규식·한글·백틱 든 파일 고치지 마. Edit 도구를 써라.
  6. 스크린샷으로 안 보이는 결함이 있다(가림). elementFromPoint 히트 테스트.
  7. EventSource 는 429 를 못 본다. 상한은 스로틀이어야 한다.
  8. "코드에 있다" ≠ "작동한다" — 순서 때문에.
  9. 감성 레이어가 대비를 깎으면 결함이다.
  10. 스캐너를 고치는 게 스캔 결과를 훑는 것보다 싸다.
  11. 내가 방금 배포한 것도 감사 대상이다.
  12. 배열 하나가 몇 개 스코프를 게이트하는지 세어라(skinContrast PAIRS = 11 스코프).
  13. 🔴 **실패 방식이 "조용함"이면 게이트를 소스에 걸어라.** 행동 테스트는 누가 테스트를 쓴
      출구만 덮는데, 그런 결함은 애초에 **쓰는 걸 잊는 것**이다. (스토리지 7출구 중 6개가 그랬다)
  14. 🔴 **브라우저 워크가 "3로케일 통과"라고 해도 같은 로케일 3번일 수 있다.** 룸 언어는 기기
      언어가 아니라 예약별 조인 선택이다. 로케일 검증은 렌더 테스트로.
  15. 🔴 **getComputedStyle 이 visible 이어도 화면엔 없을 수 있다.** 포지션드 의사요소
      (body::before)가 통상 흐름 위에 칠해진다. DOM 이 맞고 페인트가 틀린 건 스크린샷만 잡는다.
  16. 🔴 **측정 전에 포트가 비었는지 확인.** 안 죽은 이전 빌드 서버가 없는 CSS 를 가리키는
      HTML 을 서빙해서 수치가 두 배 튀었다. 튄 수치는 폐기하고 재현되는 것만 남겨라.

■ 실행 순서 — 이 순서 그대로, 끝까지

  [A] 🔴 플래너 전면 재편 P7~ (사장님 지시, 이 트랙 최대 건)
      착수 전 §N-9 를 먼저 읽어라. P2~P6 은 이 재편에 흡수. PlanEditorClient 3,585줄
      분할이 전제. §0-2 대로 게이트(resolvePlan 에 스킨 인자) 먼저.
      🔴 skinContrast 의 PAIRS 는 플래너 테스트가 그대로 펼쳐 쓴다 = 11개 스코프.

  [B] K4v2 Phase 2~4
      원장(Phase 1)은 이미 있다 — lib/audit/k4Coverage.ts 의 54쌍 위에 얹으면 된다.
      Phase 2 시더(20방, 참가자 로케일 집합 고정 — 바뀌면 번역 캐시가 무력화된다) →
      Phase 3 할당·주행(문장 코퍼스 ~14개 재사용) → Phase 4 리포트.
      🔴 "이 주행이 재지 않은 것"은 K4_NOT_MEASURED 에 이미 있으니 그대로 출력해라.

  [C] X17(Timeline 공유 — 스토리지 선행조건 충족됨) · X15 Phase 1(스태프 지오펜스는 없다,
      새로 만들어야 한다) · X18(매트릭스는 #593 이후 찰 수 있지만 아직 빈 테이블이다)

  [D] X20 대화 코퍼스 — 개인정보 문구는 #604 로 준비됐다(privacy.s16 10로케일).
      🔴 needs 30일 퍼지(P-D11)는 유지. 참가자 식별자는 **저장 시점에** 뗀다.

  [E] 감성 마감 — N3잔여 · N8(콕핏 제외 확정)

  [F] 5순위 — X1 잔여(certain 576 + suspect 428) · X19 · X7~X12 · O6 · T0~T5 · R6

■ 매 티켓마다
  - 게이트: npx tsc --noEmit · npx jest __tests__ ·
    npx jest skinContrast typeDiscipline shellStackingContext chipBoundary bottomEdgeOwnership ·
    npm run build (종료 코드로 확인)
  - UI를 건드렸으면 Playwright 전후 컷으로 증명. 코드 읽고 "고쳤다"고 하지 마.
    단 교훈 14·15·16 을 먼저 읽어라 — 컷도 거짓말할 수 있다.
  - 대비를 잴 땐 scripts/qa-lib/contrast-inject.js 를 써라. 새로 만들지 마.
  - 숫자를 쓸 땐 잰 건지 계산한 건지 문장에 표시해. 주석에 쓰는 숫자도 마찬가지.
  - 티켓 하나 끝나면 PR 내고 머지하고, **플랜 본문과 §F 실행 로그를 같이 갱신**.
  - 커밋 푸터는 Co-Authored-By: Claude <noreply@anthropic.com> 만. 모델 식별자 금지.
  - 진행 보고는 한국어, 코드·커밋은 영어. git add -A 금지, 경로 명시.

■ 막히면
  블로킹이면 그 티켓만 사유 적고 건너뛰고 다음으로. 전체를 멈추지 마.
  건너뛴 건 전부 아침 보고에 모아서.

■ 아침 보고에 넣을 것
  끝낸 티켓 · 건너뛴 티켓과 사유 · 내가 결정해야 할 것 · UI 변경은 전후 컷 ·
  측정한 수치는 조건과 함께.
  🔴 사장님이 읽을 문장으로 써라 — 티켓 코드를 표의 첫 칸에 놓지 마.
```
