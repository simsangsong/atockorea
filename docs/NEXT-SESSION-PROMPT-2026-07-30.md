# 다음 세션 실행 프롬프트 — 그대로 복사해서 붙여넣기

> 이 파일은 **붙여넣을 프롬프트 한 덩어리**다. 아래 코드블록 전체를 복사한다.

```text
스마트앱/관제 통합 트랙을 이어서 끝까지 진행해줘.
나는 자러 가고 아침에 결과를 볼 거야. 모든 권한을 위임한다.
선택지가 나오면 네 추천 방향으로 결정하고 계속 가. 멈추지 말고, 생략 없이,
건너뛰기 없이, 기능 축소나 UI 다운그레이드 없이.

■ 먼저 이 넷을 본문 전체로 읽어 (요약본 말고)
  1. docs/OWNER-DECISIONS-2026-07-29.md        ← 🔴 사장님 결정 8건. 이게 최우선 제약이다
  2. docs/SESSION-STATE-2026-07-29.md          직전 세션이 어디까지 했나 · 살아 있는 환경
  3. docs/NEXT-SESSION-SMARTAPP-2026-07-28.md  순서 · 환경 · 함정 · 충돌규칙 · 게이트
  4. docs/ops-staff-design-unification-master-plan-2026-07-27.md  Part A~O 전부 (왜)
  어긋나면 마스터 플랜이 정본, 단 결정 문서는 플랜보다 위다.

■ 셋업
  1) 전용 워크트리. .env.local 복사 (워크트리엔 없다) — NEXT_PUBLIC_TOUR_MODE_V1=1 확인
  2) node_modules 없으면 npm ci
  3) git fetch origin main && git reset --hard origin/main
  4) npx tsc --noEmit
     npx jest __tests__/components/tour-mode __tests__/lib/tour-room __tests__/audit __tests__/scripts
     → tsc 0 이 기준선. 안 맞으면 그것부터.
  5) 시드 토큰은 하루면 만료된다. 만료면 재시드:
     ALLOW_SIM_SEED=1 npx tsx scripts/sim-tour-day.ts

■ 🔴 지난 세션이 실제로 물린 것 — 같은 걸 또 하지 마
  1. 빌드 게이트는 grep이 아니라 **종료 코드**다.
     next build 는 "Compiled successfully" 를 찍고 나서 실패한다.
       npm run build > /tmp/build.log 2>&1; echo "EXIT=$?"
     이걸 안 해서 프로덕션 배포가 깨진 채 몇 시간 갔다.
  2. 게이트 목록에 __tests__/audit 를 반드시 포함. 새 컴포넌트는 docs/audit/A1-coverage.md 에 행 추가.
  3. 플랜의 "미착수"를 믿지 말고 코드부터 grep. P0·P1 둘 다 이미 배포돼 있었다.
  4. 픽스처는 "이미 아는 데이터"다. 실 시드 룸으로 한 번은 걸어라 —
     마지막 스톱 버그는 워크가 잡았고 단위 테스트는 전부 초록이었다.
  5. node -e 로 한글·정규식·백틱 파일 수정 금지. .mjs 파일을 써서 node 로 실행.
  6. 워크트리 dev 는 HMR 안 먹는다. 고치면 서버 재시작.

■ 실행 순서 — 이 순서 그대로, 끝까지

  [A] 사장님 결정에서 새로 생긴 것 (결정 문서가 정본)
    I7    홈 「더 보기」를 **엿보기**로: 타일 3개 펼침 + 다음 줄 아이콘 머리만 희미하게
          + [더 보기] 누르면 전량. 앱 사용설명서는 채팅 바로 밑 고정.
          ⚠ U-D24 유지(탭바에 있는 건 여전히 제외) · U-D25 유지(펼치면 전량, toBeVisible 잠금)
    X20   대화 코퍼스 — 손님 질문·가이드 답변을 **참가자 식별자 떼고** 수집.
          🔴 개인화는 안 한다. needs 30일 퍼지(P-D11)는 그대로 유지.
          기존 rag:harvest 를 넓히는 것이지 두 벌 만드는 게 아니다.
    K4v2  20방(프라이빗·조인 골고루) × **전 기능 무작위 커버리지** 주행.
          🔴 커버리지를 출력해라 — 안 돌린 기능을 말하지 않는 주행은 초록으로 오독된다.
          sim_tag 격리 + --cleanup 필수. 실 LLM 비용 발생하니 반복 문장으로.
    L2    확정 메일·바우처에 "투어 중 옵션·연장은 당일 가이드에게 현금" 한 줄.
    B6    상품 2종 숨김 — main에서 이미 숨겨져 있는지 먼저 확인하고, 그러면 브랜치는 사유 적고 닫아.

  [B] 3순위 속도
    K2    요금제 실시간 상한 확인 (사장님/콘솔 필요하면 물어보고 넘어가)
    K1b   동시 SSE 스트림 전역 상한값 (K2 이후에만)
    X13   🔴 라우트 그룹 분리 — **단독 세션·단독 PR**. 시작 전 다른 브랜치 전부 머지/정지.
          실측 셸 바닥 423KB. 700KB 아님.
    K3    프레즌스·타이핑 볼륨 다이어트 (K1a와 같은 파일 — 리베이스 주의)
    K5    번역 동시성 상한

  [C] 감성 마감
    N3잔여(풍경/콘텐츠 레이어 분리) → N8(광원을 가이드 콘솔·플래너·진입까지.
    🔴 콕핏은 제외 확정 — 야간 글레어) → N2(하루의 결) → N4(빈 채팅 + 두 칩 줄 재질 통일)

  [D] 4순위 이상적 앱 갭
    X15(도착 자동화 — 손님 위치공유는 기본 OFF라 못 쓴다. 가이드/기사 기기로 트리거) →
    X17(Timeline 공유·저장) → X18(학습한 매트릭스를 가이드에게 환류)

  [E] 4.5순위 Part O 잔여 — 🔴 기본값은 폐기
    B2 → B5 → B4 → B3
    편입하려면 셋 다 통과: ①main에 없는 걸 더하나 ②main보다 안 뒤쳐졌나 ③기능·UI 후퇴 없나.
    하나라도 걸리면 사유 한 줄 적고 닫아 — 그게 마무리다.
    어떤 브랜치도 "theirs"로 통째 수용 금지. main 위에 의도만 옮겨 심어.
    (참고: B1은 지난 세션에 폐기 처리 완료, archive/* 태그로 보존)

  [F] 🔴 플래너 전면 재편 P7~ (사장님 신규 지시 — 큰 건)
    "UI 전면 재편해서라도 가독성 최상 + 디자인 감성 최상"
    ⚠ 착수 전 §N-9(플래너 팔레트/스킨 특이도) 먼저 읽어라. 전면 재편이면 선택지 B를 고를 수 있다.
    🔴 선행: skinContrast 의 resolvePlan() 을 스킨 인자 받도록 확장 — 플래너는 게이트 밖이라
       ink-3 미달로 배포된 전력이 있다.
    P2~P6(한 줄 스톱·TimeWheel·좌석 보기·셸 정리·게이트)은 이 재편에 흡수. 따로 하면 두 번 만든다.
    PlanEditorClient 3,585줄 분할이 전제.

  [G] 5순위 위생·커버리지
    X1 잔여(certain 603 — 대부분 text-cjk-body 패스 + suspect 537) ·
    X19(표 형식 정규화 먼저 → 로그 생성기) · X7 X8 X9 X10 X11 X12 ·
    O6 · T0~T5 · R6

■ 아직 안 고친 실기기 결함 (사장님 스크린샷 2026-07-29에서 나옴)
  - 🔴 아포스트로피 뒤가 벌어진다: "You’ ve" / "today’ s".
    진단 완료 = U+2019 가 CJK 폰트 스택에서 전각으로 렌더된다(globals.css 가 lang 별로 body 폰트 교체).
    데이터는 깨끗하다(DB·리터럴 hex 확인함). 폰트 스택에서 라틴 구두점을 라틴 폰트가 잡게 하는 게 답.
  - 🔴 같은 시스템 메시지가 3번 중복. 진단 완료 = plan 라우트의 delegate 경로에 **멱등성이 전혀 없다**.
    submit 에는 dedupe 가 있는데(existingStatus === 'guest_submitted') delegate 는 그 조건에 안 들어갔고,
    delegate 는 status 를 안 바꿔서 A8 잠금도 안 걸린다. 라이브 한 방에 6개 있다.
  - 🟠 퀵리플 줄이 양쪽 다 잘린 채 시작 · 시그널 줄이 지도 카드 하단을 가림.
  셋 다 진단은 끝났고 수정만 남았다. [A] 전에 처리해도 좋다.

■ 매 티켓마다
  - 게이트: npx tsc --noEmit · npx jest (tour-mode/tour-room/audit/scripts) ·
    npx jest skinContrast typeDiscipline shellStackingContext chipBoundary ·
    npm run build (종료 코드로 확인)
  - UI를 건드렸으면 Playwright 전후 컷으로 증명. 코드 읽고 "고쳤다"고 하지 마.
  - 대비를 잴 땐 scripts/qa-lib/contrast-inject.js 를 써라. 새로 만들지 마 —
    그거 없이 만든 계측기가 이 트랙에서 네 번 틀렸다.
  - 숫자를 쓸 땐 잰 건지 계산한 건지 문장에 표시해.
  - 티켓 하나 끝나면 PR 내고 머지하고, 플랜 본문과 §F 실행 로그를 같이 갱신.
  - 커밋 푸터는 Co-Authored-By: Claude <noreply@anthropic.com> 만. 모델 식별자 금지.
  - 진행 보고는 한국어, 코드·커밋은 영어. git add -A 금지, 경로 명시.

■ 막히면
  블로킹이면 그 티켓만 사유 적고 건너뛰고 다음으로. 전체를 멈추지 마.
  건너뛴 건 전부 아침 보고에 모아서.

■ 아침 보고에 넣을 것
  끝낸 티켓 · 건너뛴 티켓과 사유 · 폐기한 브랜치와 사유 · 내가 결정해야 할 것 ·
  UI 변경은 전후 컷 · 측정한 수치는 조건과 함께.
  🔴 그리고 사장님이 읽을 문장으로 써라 — 티켓 코드를 표의 첫 칸에 놓지 마.
```
