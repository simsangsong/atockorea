/**
 * Staggered delays (ms) for legacy mobile hero “matching” animation steps.
 * Same values as previously inline in `HeroPremium`.
 */
export const HERO_MATCH_LOAD_DELAYS_MS = [1600, 1600, 1000] as const;
