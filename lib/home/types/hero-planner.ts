/** Legacy hero planner destination tiles (homepage). */
export type HeroDestination = "jeju" | "seoul" | "busan";

/** Mobile-only simulated match phases (no backend recommendation). */
export type HeroMatchPhase = "planner" | "loading" | "result";
