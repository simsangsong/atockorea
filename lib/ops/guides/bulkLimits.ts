/**
 * 배정 일괄 작업의 상한.
 *
 * 🔴 라우트 파일이 아니라 여기 사는 이유: Next 라우트 모듈은 **핸들러와 정해진
 * 설정 값(`dynamic` 등) 외의 export를 금지**한다. `tsc --noEmit`은 이것을 잡지
 * 못하고 `next build`에서만 터지므로, 빌드까지 돌리지 않으면 배포에서 드러난다.
 *
 * 상한에 닿으면 **잘라내지 않고 거절한다.** 잘라내고 성공했다고 하면 나머지가
 * 조용히 빠지고, 그 사람들은 정산이나 안내에서 사라진다.
 */

/** 한 번에 상태를 바꾸는 배정 수. 화면이 한 달치를 다 보여줘도 넉넉하다. */
export const ASSIGNMENT_BULK_LIMIT = 500;

/** 한 번에 안내 메일을 보내는 배정 수. */
export const ASSIGNMENT_NOTIFY_LIMIT = 500;
