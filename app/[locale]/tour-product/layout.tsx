/**
 * Localized tour-product detail shares the exact layout of the canonical
 * bare-path route (fonts + SitePageShell + v2 scope CSS) — re-export so the
 * two trees can never drift.
 */
export { default } from "@/app/tour-product/layout";
