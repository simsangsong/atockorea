import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'East Jeju Signature — Small Group | AtoCKorea',
  description: 'East Jeju small-group day tour — live booking.',
};

export default function EastSmallGroupV2LiveLayout({ children }: { children: React.ReactNode }) {
  return children;
}
