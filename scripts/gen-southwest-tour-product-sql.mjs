/**
 * Reads components/.../southwest-tour-product-full-page.en.json
 * Writes supabase/manual/insert-southwest-hallasan-osulloc-aewol-product.sql
 *
 * Run: node scripts/gen-southwest-tour-product-sql.mjs
 */

import { readFileSync, writeFileSync } from "fs";
import { dirname, join } from "path";
import { fileURLToPath } from "url";

const __dirname = dirname(fileURLToPath(import.meta.url));
const root = join(__dirname, "..");
const jsonPath = join(
  root,
  "components/product-tour-static/southwest-hallasan-osulloc-aewol/southwest-tour-product-full-page.en.json",
);
const outPath = join(root, "supabase/manual/insert-southwest-hallasan-osulloc-aewol-product.sql");

function sqlEscapeLiteral(s) {
  return String(s).replace(/'/g, "''");
}

const raw = JSON.parse(readFileSync(jsonPath, "utf8"));
if (raw.page_sections) delete raw.page_sections;

const detailPayloadJson = JSON.stringify(raw);
const detailPayloadSql = sqlEscapeLiteral(detailPayloadJson);

const SLUG = "southwest-hallasan-osulloc-aewol";

const sql = `-- =============================================================================
-- Jeju Southwest — Hallasan, Jusangjeolli, Osulloc & Aewol (tour_product v2)
-- =============================================================================
-- Slug: ${SLUG}
-- Idempotent: tours ON CONFLICT (slug); tour_product_pages ON CONFLICT (slug, locale);
--            tour_matching_profiles ON CONFLICT (product_id)
-- Web: /tour-product/${SLUG}
--
-- Run in Supabase SQL editor (or psql). Requires prior migrations through
-- 20260413100000_tour_product_product_id_matching_profiles_intent_cache.sql
--
-- Generated by: node scripts/gen-southwest-tour-product-sql.mjs
-- =============================================================================

BEGIN;

-- ---------------------------------------------------------------------------
-- 1) tours — 예약·/tour/[id] 체크아웃 연동용 (가격 USD 58 / person)
-- ---------------------------------------------------------------------------
INSERT INTO public.tours (
  title, slug, city, tag, subtitle, description, highlight,
  price, original_price, price_currency, price_type, image_url, gallery_images,
  duration, difficulty, group_size, lunch_included, ticket_included,
  pickup_info, notes, badges, highlights, includes, excludes,
  schedule, itinerary_details, faqs,
  rating, review_count, pickup_points_count, dropoff_points_count,
  is_active, is_featured, translations, seo_title, meta_description
) VALUES (
  'Jeju Southwest Hallasan, Osulloc & Aewol Small Group Tour',
  '${SLUG}',
  'Jeju',
  'Small group · Southwest Jeju',
  'A balanced Southwest Jeju day with a short Hallasan trail, Jusangjeolli''s basalt coast, Cheonjeyeon Falls, O''Sulloc tea fields, and a relaxed Aewol finish.',
  'Hallasan morning air, volcanic coastline, waterfall paths, tea-field calm, and a café-coast close—structured pacing for first-time visitors.',
  'Southwest Jeju in one day: mountain start, iconic south coast, waterfall and tea, Aewol release.',
  58.00,
  72.00,
  'USD',
  'person',
  'https://images.unsplash.com/photo-1518509562904-e7ef99cdcc86?w=1200&q=85',
  '["https://images.unsplash.com/photo-1506744038136-46273834b3fb?w=800&h=600&fit=crop","https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?w=800&h=600&fit=crop","https://images.unsplash.com/photo-1441974231531-c6227db76b6e?w=800&h=600&fit=crop","https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?w=800&h=600&fit=crop","https://images.unsplash.com/photo-1507525428034-b723cf961d3e?w=800&h=600&fit=crop","https://images.unsplash.com/photo-1518509562904-e7ef99cdcc86?w=800&h=600&fit=crop"]'::jsonb,
  'Approx. 8 hours · Southwest Jeju',
  'Moderate',
  'Small group',
  false,
  false,
  'Pickup time varies by hotel zone; final guidance after booking and again the evening before.',
  'Walking: moderate overall—short Hallasan segment and stairs at Cheonjeyeon. Wind on Jusangjeolli/Aewol. Weather may adjust order.',
  '["First-Time Friendly","Southwest Jeju"]'::jsonb,
  '["Best for: First-time Jeju visitors, couples, families who can manage stairs and a full sightseeing day.","Southwest flow: Hallasan → basalt coast → lunch → waterfall → tea → Aewol.","Photo value: high; pacing: balanced with a relaxed late-day finish."]'::jsonb,
  '["Licensed guide","Small-group vehicle","Jeju City area pickup/drop-off (as booked)","Admission fees as per itinerary"]'::jsonb,
  '["Meals unless stated in fare","Personal expenses","Tips"]'::jsonb,
  '[{"time":"09:00","title":"Hallasan Eoseungsaengak","description":"Short mountain start."},{"time":"10:50","title":"Daepo Jusangjeolli Cliff","description":"Basalt coastline."},{"time":"11:50","title":"Lunch","description":"Midday reset."},{"time":"13:00","title":"Cheonjeyeon Falls","description":"Waterfall & forest walk."},{"time":"14:20","title":"O''Sulloc Tea Museum","description":"Tea fields & museum."},{"time":"15:50","title":"Aewol Cafe Street","description":"Coastal café finish."}]'::jsonb,
  '[]'::jsonb,
  '[]'::jsonb,
  4.80,
  127,
  2,
  0,
  true,
  false,
  '{"en":{"title":"Jeju Southwest Hallasan, Osulloc & Aewol Small Group Tour","subtitle":"Short Hallasan hike. Basalt cliffs. Waterfall paths. Tea fields and a relaxed Aewol finish.","description":"A balanced Southwest Jeju day with Hallasan, Jusangjeolli, Cheonjeyeon, O''Sulloc, and Aewol.","pickup_info":"Final pickup after booking confirmation.","badges":["First-Time Friendly","Southwest Jeju"]}}'::jsonb,
  'Jeju Southwest Hallasan, Jusangjeolli, Osulloc & Aewol Small Group Tour | AtoC Korea',
  'A balanced Southwest Jeju day with Hallasan Eoseungsaengak, Jusangjeolli, Cheonjeyeon Falls, O''Sulloc Tea Museum, and Aewol Cafe Street.'
)
ON CONFLICT (slug) DO UPDATE SET
  title = EXCLUDED.title,
  city = EXCLUDED.city,
  tag = EXCLUDED.tag,
  subtitle = EXCLUDED.subtitle,
  description = EXCLUDED.description,
  highlight = EXCLUDED.highlight,
  price = EXCLUDED.price,
  original_price = EXCLUDED.original_price,
  price_currency = EXCLUDED.price_currency,
  price_type = EXCLUDED.price_type,
  image_url = EXCLUDED.image_url,
  gallery_images = EXCLUDED.gallery_images,
  duration = EXCLUDED.duration,
  difficulty = EXCLUDED.difficulty,
  group_size = EXCLUDED.group_size,
  lunch_included = EXCLUDED.lunch_included,
  ticket_included = EXCLUDED.ticket_included,
  pickup_info = EXCLUDED.pickup_info,
  notes = EXCLUDED.notes,
  badges = EXCLUDED.badges,
  highlights = EXCLUDED.highlights,
  includes = EXCLUDED.includes,
  excludes = EXCLUDED.excludes,
  schedule = EXCLUDED.schedule,
  itinerary_details = EXCLUDED.itinerary_details,
  faqs = EXCLUDED.faqs,
  rating = EXCLUDED.rating,
  review_count = EXCLUDED.review_count,
  pickup_points_count = EXCLUDED.pickup_points_count,
  dropoff_points_count = EXCLUDED.dropoff_points_count,
  is_active = EXCLUDED.is_active,
  is_featured = EXCLUDED.is_featured,
  translations = EXCLUDED.translations,
  seo_title = EXCLUDED.seo_title,
  meta_description = EXCLUDED.meta_description,
  updated_at = NOW();

DELETE FROM pickup_points
WHERE tour_id = (SELECT id FROM tours WHERE slug = '${SLUG}');

INSERT INTO pickup_points (tour_id, name, address, lat, lng, pickup_time)
SELECT
  t.id,
  v.name,
  v.address,
  v.lat,
  v.lng,
  v.pickup_time::TIME
FROM tours t
CROSS JOIN (VALUES
  (
    'Jeju Airport — domestic terminal (example)',
    'Jeju International Airport — meet at the agreed domestic gate/area (confirmed after booking)',
    33.51130000::numeric,
    126.49300000::numeric,
    '08:20:00'
  ),
  (
    'Jeju City hotels (example zone)',
    'Jeju-si hotel zone — exact lobby pickup confirmed after booking',
    33.49960000::numeric,
    126.53120000::numeric,
    '08:40:00'
  )
) AS v(name, address, lat, lng, pickup_time)
WHERE t.slug = '${SLUG}';

-- ---------------------------------------------------------------------------
-- 2) tour_product_pages — marketing 상세 + detail_payload (앱 뷰모델)
-- ---------------------------------------------------------------------------
INSERT INTO public.tour_product_pages (
  slug,
  locale,
  product_id,
  is_published,
  sort_order,
  tour_id,
  title,
  subtitle,
  region_label,
  duration_label,
  stops_count,
  rating_avg,
  review_count,
  badges,
  hero_image_url,
  thumbnail_url,
  card_short_description,
  seo_title,
  meta_description,
  headline_line_1,
  headline_line_2,
  price_amount_label,
  price_currency,
  price_per,
  detail_payload
) VALUES (
  '${SLUG}',
  'en',
  '${SLUG}',
  TRUE,
  1,
  (SELECT id FROM public.tours WHERE slug = '${SLUG}' LIMIT 1),
  'Jeju Southwest Hallasan, Osulloc & Aewol Small Group Tour',
  'Short Hallasan hike. Basalt cliffs. Waterfall paths. Tea fields and a relaxed Aewol finish.',
  'Southwest Jeju',
  '8 hrs',
  6,
  4.8,
  127,
  ARRAY['First-Time Friendly', 'Southwest Jeju']::text[],
  'https://images.unsplash.com/photo-1518509562904-e7ef99cdcc86?w=1920&q=80',
  'https://images.unsplash.com/photo-1518509562904-e7ef99cdcc86?w=600&q=80',
  'A structured Southwest Jeju day with a short Hallasan trail, Jusangjeolli''s basalt coast, Cheonjeyeon Falls, O''Sulloc tea fields, and a relaxed finish at Aewol Cafe Street.',
  'Jeju Southwest Hallasan, Jusangjeolli, Osulloc & Aewol Small Group Tour | AtoC Korea',
  'A balanced Southwest Jeju day with Hallasan Eoseungsaengak, Daepo Jusangjeolli Cliff, Cheonjeyeon Falls, O''Sulloc Tea Museum, and a relaxed finish at Aewol Cafe Street.',
  'Jeju Southwest Hallasan, Tea & Aewol',
  'Small Group Tour',
  '58',
  'USD',
  'person',
  '${detailPayloadSql}'::jsonb
)
ON CONFLICT (slug, locale) DO UPDATE SET
  product_id = EXCLUDED.product_id,
  is_published = EXCLUDED.is_published,
  sort_order = EXCLUDED.sort_order,
  tour_id = EXCLUDED.tour_id,
  title = EXCLUDED.title,
  subtitle = EXCLUDED.subtitle,
  region_label = EXCLUDED.region_label,
  duration_label = EXCLUDED.duration_label,
  stops_count = EXCLUDED.stops_count,
  rating_avg = EXCLUDED.rating_avg,
  review_count = EXCLUDED.review_count,
  badges = EXCLUDED.badges,
  hero_image_url = EXCLUDED.hero_image_url,
  thumbnail_url = EXCLUDED.thumbnail_url,
  card_short_description = EXCLUDED.card_short_description,
  seo_title = EXCLUDED.seo_title,
  meta_description = EXCLUDED.meta_description,
  headline_line_1 = EXCLUDED.headline_line_1,
  headline_line_2 = EXCLUDED.headline_line_2,
  price_amount_label = EXCLUDED.price_amount_label,
  price_currency = EXCLUDED.price_currency,
  price_per = EXCLUDED.price_per,
  detail_payload = EXCLUDED.detail_payload,
  updated_at = NOW();

-- ---------------------------------------------------------------------------
-- 3) tour_product_offers — 스티키 바·체크아웃 (USD 58.00 = 5800 minor units)
-- ---------------------------------------------------------------------------
INSERT INTO public.tour_product_offers (
  tour_product_page_id, label, amount_minor, currency, stripe_price_id, is_active, is_default
)
SELECT
  p.id,
  'Default (seed)',
  5800,
  'USD',
  NULL,
  TRUE,
  TRUE
FROM public.tour_product_pages p
WHERE p.slug = '${SLUG}' AND p.locale = 'en'
  AND NOT EXISTS (
    SELECT 1 FROM public.tour_product_offers o
    WHERE o.tour_product_page_id = p.id AND o.is_default = TRUE AND o.is_active = TRUE
  );

-- ---------------------------------------------------------------------------
-- 4) tour_matching_profiles — 추천/매칭 (southwest)
-- ---------------------------------------------------------------------------
INSERT INTO public.tour_matching_profiles (
  product_id,
  product_type,
  route_type,
  region_type,
  region_tags,
  theme_tags,
  poi_tags,
  pace_level,
  walking_level,
  scenic_level,
  photo_level,
  culture_level,
  relax_level,
  first_time_fit,
  family_fit,
  senior_fit,
  couple_fit,
  active_traveler_fit,
  one_day_fit,
  same_day_flight_fit,
  rain_fit,
  value_for_money_fit,
  iconic_landmark_fit,
  cafe_fit,
  pickup_base,
  return_time_band,
  duration_band,
  min_recommended_age,
  hard_constraints,
  walking_notes,
  keywords,
  synonym_hints,
  profile_version,
  is_active
)
VALUES (
  '${SLUG}',
  'small_group',
  'fixed_route',
  'southwest',
  '["jeju_southwest"]'::JSONB,
  '["hallasan", "basalt", "waterfall", "tea", "coast", "cafe"]'::JSONB,
  '["eoseungsaengak", "jusangjeolli", "cheonjeyeon", "osulloc", "aewol"]'::JSONB,
  3,
  3,
  4,
  4,
  3,
  4,
  5,
  4,
  4,
  4,
  3,
  3,
  2,
  3,
  5,
  4,
  3,
  'jeju_city',
  '17:50-18:20',
  '8h',
  8,
  '{"avoidIf":[],"notIdealFor":["very_low_mobility","stroller_heavy_on_trail"]}'::JSONB,
  '["moderate overall","stairs at Hallasan trail and Cheonjeyeon"]'::JSONB,
  '["southwest jeju","hallasan","jusangjeolli","osulloc","aewol","first time"]'::JSONB,
  '["balanced southwest","tea and coast finish"]'::JSONB,
  1,
  TRUE
)
ON CONFLICT (product_id) DO UPDATE SET
  product_type = EXCLUDED.product_type,
  route_type = EXCLUDED.route_type,
  region_type = EXCLUDED.region_type,
  region_tags = EXCLUDED.region_tags,
  theme_tags = EXCLUDED.theme_tags,
  poi_tags = EXCLUDED.poi_tags,
  pace_level = EXCLUDED.pace_level,
  walking_level = EXCLUDED.walking_level,
  scenic_level = EXCLUDED.scenic_level,
  photo_level = EXCLUDED.photo_level,
  culture_level = EXCLUDED.culture_level,
  relax_level = EXCLUDED.relax_level,
  first_time_fit = EXCLUDED.first_time_fit,
  family_fit = EXCLUDED.family_fit,
  senior_fit = EXCLUDED.senior_fit,
  couple_fit = EXCLUDED.couple_fit,
  active_traveler_fit = EXCLUDED.active_traveler_fit,
  one_day_fit = EXCLUDED.one_day_fit,
  same_day_flight_fit = EXCLUDED.same_day_flight_fit,
  rain_fit = EXCLUDED.rain_fit,
  value_for_money_fit = EXCLUDED.value_for_money_fit,
  iconic_landmark_fit = EXCLUDED.iconic_landmark_fit,
  cafe_fit = EXCLUDED.cafe_fit,
  pickup_base = EXCLUDED.pickup_base,
  return_time_band = EXCLUDED.return_time_band,
  duration_band = EXCLUDED.duration_band,
  min_recommended_age = EXCLUDED.min_recommended_age,
  hard_constraints = EXCLUDED.hard_constraints,
  walking_notes = EXCLUDED.walking_notes,
  keywords = EXCLUDED.keywords,
  synonym_hints = EXCLUDED.synonym_hints,
  profile_version = EXCLUDED.profile_version,
  is_active = EXCLUDED.is_active,
  updated_at = NOW();

COMMIT;

SELECT slug, title, is_active FROM public.tours WHERE slug = '${SLUG}';
SELECT slug, locale, is_published, product_id FROM public.tour_product_pages WHERE slug = '${SLUG}';
`;

writeFileSync(outPath, sql, "utf8");
console.log("Wrote", outPath);
