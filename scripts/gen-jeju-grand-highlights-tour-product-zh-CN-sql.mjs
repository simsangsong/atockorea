/**
 * 简体中文 tour_product_pages upsert SQL（DB locale = `zh`，与 NEXT_LOCALE zh-CN 对应）
 * - 源: components/.../jeju-grand-highlights-loop-tour-product-full-page.zh-CN.json（去除 page_sections）
 * - sectionUi: east zh-CN full-page + 环岛快节奏路线 fit 文案覆盖
 *
 * Run: node scripts/gen-jeju-grand-highlights-tour-product-zh-CN-sql.mjs
 */

import { readFileSync, writeFileSync } from "fs";
import { dirname, join } from "path";
import { fileURLToPath } from "url";

const __dirname = dirname(fileURLToPath(import.meta.url));
const root = join(__dirname, "..");

const zhPath = join(
  root,
  "components/product-tour-static/jeju-grand-highlights-loop/jeju-grand-highlights-loop-tour-product-full-page.zh-CN.json",
);
const eastZhPath = join(root, "supabase/manual/generated/east-signature-nature-core-full-page.zh-CN.json");
const outPath = join(root, "supabase/manual/upsert-jeju-grand-highlights-loop-zh.sql");

const SLUG = "jeju-grand-highlights-loop";
/** DB 列 locale；resolveTourProductDbLocale: zh-CN cookie → zh */
const LOCALE = "zh";

function sqlEscapeLiteral(s) {
  return String(s).replace(/'/g, "''");
}

const raw = JSON.parse(readFileSync(zhPath, "utf8"));
if (raw.page_sections) delete raw.page_sections;
raw.locale = "zh";

const eastZh = JSON.parse(readFileSync(eastZhPath, "utf8"));
const baseSectionUi = eastZh.sectionUi && typeof eastZh.sectionUi === "object" ? { ...eastZh.sectionUi } : {};

raw.sectionUi = {
  ...baseSectionUi,
  fitFamiliesText: "建议 10 岁及以上、体力较好的家庭；需适应上坡、台阶与较长车程。",
  fitSeniorsText: "若可放慢步调、避开最陡段，部分旅客仍可参加；请先评估台阶、风势与步行习惯。",
};

const cc = raw.catalog_card;
const seo = raw.seo;
const price = raw.price;

const detailPayloadJson = JSON.stringify(raw);
const detailPayloadSql = sqlEscapeLiteral(detailPayloadJson);

const badgesArray = (cc.badges || []).map(sqlEscapeLiteral).join("', '");

const stopsN = Number(cc.stopsCount);
const stopsSql = Number.isFinite(stopsN) ? stopsN : 5;
const ratingN = cc.rating != null ? Number(cc.rating) : 0;
const reviewN = cc.reviewCount != null ? Number(cc.reviewCount) : 0;

const sql = `-- =============================================================================
-- Jeju Grand Highlights Loop — tour_product_pages (简体中文, locale=zh)
-- =============================================================================
-- Slug: ${SLUG} | locale: ${LOCALE} （应用内 zh-CN 对应此列，见 resolveTourProductDbLocale）
-- Idempotent: ON CONFLICT (slug, locale) DO UPDATE
-- Prerequisites: en 行 + tours 行（insert-jeju-grand-highlights-loop-product.sql 已执行）
--
-- Generated by: node scripts/gen-jeju-grand-highlights-tour-product-zh-CN-sql.mjs
-- =============================================================================

BEGIN;

INSERT INTO public.tour_product_pages (
  slug,
  locale,
  product_id,
  is_published,
  sort_order,
  tour_id,
  title,
  subtitle,
  region_label,
  duration_label,
  stops_count,
  rating_avg,
  review_count,
  badges,
  hero_image_url,
  thumbnail_url,
  card_short_description,
  seo_title,
  meta_description,
  headline_line_1,
  headline_line_2,
  price_amount_label,
  price_currency,
  price_per,
  detail_payload
)
SELECT
  '${SLUG}',
  '${LOCALE}',
  '${SLUG}',
  TRUE,
  COALESCE((SELECT sort_order FROM public.tour_product_pages WHERE slug = '${SLUG}' AND locale = 'en' LIMIT 1), 1),
  (SELECT id FROM public.tours WHERE slug = '${SLUG}' LIMIT 1),
  '${sqlEscapeLiteral(cc.title)}',
  '${sqlEscapeLiteral(cc.subtitle)}',
  '${sqlEscapeLiteral(cc.region)}',
  '${sqlEscapeLiteral(cc.duration)}',
  ${stopsSql},
  ${ratingN},
  ${reviewN},
  ARRAY['${badgesArray}']::text[],
  '${sqlEscapeLiteral(cc.heroImage)}',
  '${sqlEscapeLiteral(cc.thumbnail)}',
  '${sqlEscapeLiteral(cc.shortCardDescription)}',
  '${sqlEscapeLiteral(seo.pageTitle)}',
  '${sqlEscapeLiteral(seo.metaDescription)}',
  '${sqlEscapeLiteral(raw.headlineLine1)}',
  '${sqlEscapeLiteral(raw.headlineLine2)}',
  '${sqlEscapeLiteral(String(price.amountLabel))}',
  '${sqlEscapeLiteral(price.currency)}',
  '${sqlEscapeLiteral(price.per)}',
  '${detailPayloadSql}'::jsonb
ON CONFLICT (slug, locale) DO UPDATE SET
  product_id = EXCLUDED.product_id,
  is_published = EXCLUDED.is_published,
  sort_order = EXCLUDED.sort_order,
  tour_id = EXCLUDED.tour_id,
  title = EXCLUDED.title,
  subtitle = EXCLUDED.subtitle,
  region_label = EXCLUDED.region_label,
  duration_label = EXCLUDED.duration_label,
  stops_count = EXCLUDED.stops_count,
  rating_avg = EXCLUDED.rating_avg,
  review_count = EXCLUDED.review_count,
  badges = EXCLUDED.badges,
  hero_image_url = EXCLUDED.hero_image_url,
  thumbnail_url = EXCLUDED.thumbnail_url,
  card_short_description = EXCLUDED.card_short_description,
  seo_title = EXCLUDED.seo_title,
  meta_description = EXCLUDED.meta_description,
  headline_line_1 = EXCLUDED.headline_line_1,
  headline_line_2 = EXCLUDED.headline_line_2,
  price_amount_label = EXCLUDED.price_amount_label,
  price_currency = EXCLUDED.price_currency,
  price_per = EXCLUDED.price_per,
  detail_payload = EXCLUDED.detail_payload,
  updated_at = NOW();

COMMIT;

SELECT slug, locale, is_published, product_id, left(meta_description, 60) AS meta_preview
FROM public.tour_product_pages
WHERE slug = '${SLUG}' AND locale = '${LOCALE}';
`;

writeFileSync(outPath, sql, "utf8");
console.log("Wrote", outPath);
