/**
 * Single-line cancellation copy for booking rail + mobile bar.
 * TODO(i18n): move to messages; TODO(data): replace "48 hours" with policy deadline from API/tour when available.
 */
export const BOOKING_CANCELLATION_SUMMARY_LINE =
  'Free cancellation up to 48 hours before departure.';
